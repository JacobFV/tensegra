# Handoff: extended-03 ACTIVE on the pro6000 (resumed 2026-09-26)

History:
- Paused on 2026-09-25 for the user's GB10 robot-policy training; no extended-03 GB10 job was ever launched.
- Resumed on 2026-09-26 on the **pro6000**, because both GB10s are busy. **Do not train on either GB10.** The peer GB10 may be used over ssh only to copy files.
- Host setup, launch mechanics and teardown: [infrastructure.md](infrastructure.md).

## Done and validated
- **Campaign root and design.** [campaign.md](campaign.md), [world-spec.md](world-spec.md), [protocol-P1.md](protocol-P1.md) including its freeze amendment, [decisions.md](decisions.md).
- **Stage A, depworld-v1** (`src/tensegra/campaign03_depworld.py`). Builder notes are in [depworld-notes.md](depworld-notes.md).
  - Leverage gate (progress trigger, dev seeds 2e9+):
    - reuse .976 = recompute .976 at −11% cost;
    - greedy .228;
    - reuse without revision .866;
    - naive reuse .809 with foreign records.
- **P1 configs.** Generated by `research/tools/campaign03_p1_configs.py` into `configs/campaign03/p1-boot-*.json`.
- **pro6000 environment.** 173/173 campaign tests pass; the smoke bootstrap ran (receipts in `research/results/campaign-03/`).

## In progress
- **Stage B preflight: PASSED and merged** (a0fd65ab; [review/stage-b-preflight.md](review/stage-b-preflight.md)). `campaign/e03-preflight` (caf1a785) is superseded.
- **P1 bootstraps:** 12 running on the pro6000.

## Next (resume checklist)
1. Merge Stage B once every required d1 distinction passes and the ablation masks erase exactly the intended distinctions. If a distinction fails, register an encoder fix before training.
2. Snapshot the source to the pro6000 (`source-<sha>`) and launch the 12 P1 bootstraps.
3. Generate and launch the RL configs: `campaign03_p1_configs.py rl --banks ...`.
4. Run the sealed evaluation: `... sealed --checkpoints ... --references dep_greedy,dep_recompute,dep_reuse,dep_naive_reuse,dep_reuse_norevise`.
5. Commission an independent raw-row audit, then write the report.

## Budget
- The wall window was re-declared on resume: 2026-09-26T04:54Z to 2026-09-27T04:54Z. The pause is not counted.
- Ceilings are unchanged: 48 CPU core-h and 12 GPU device-h. The ledger is in [budget.json](budget.json).
