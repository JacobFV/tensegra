# Extended campaign 01: independent review

Baseline `123299a7c5312e1f75019fa491618943f54d0de2`; reviewer works in isolated `campaign/review`. Existing Stage10/11 audits remain authoritative for their frozen sources and artifacts; this campaign does not rerun those experiments merely to reproduce audit work.

## Scope and promotion

The campaign authorizes adaptive development, with failures informing bounded follow-ups rather than ending every track. Exploratory outcomes select recipes; only frozen confirmation on at least three paired seeds and fresh constructions (at least512 per meaningful cell, preferably1024) supports promotion. Track elapsed and experiment-process GPU occupancy, including unsuccessful runs and exports, within12GPU-hours/24elapsed-hours and preserve20% reserve.

Historical gates are unchanged. A newly declared narrow composition contract may advance at >=98% across three seeds on fresh validation only for the interfaces actually consumed. Exact copy/selection is a different contract from workspace-only six-field reconstruction. Formal validity and interventions remain necessary; no gold resets. Independent attention research does not require unrelated return or semantic gates.

## Preflight questions

- **R01:** frozen wide-backbone features; paired nuisance/events and head-training labels; training-only normalization; value-class support; identical selection rules; delay32 excluded from fitting/selection. Report optimizer exposure separately from distinct labels, and distinguish readout benefit from changes to representation or exact copying.
- **S01:** audit available and visited alpha-distinct constructions, not just surfaces; independent presentation and corpus-size axes; observable targets/copy identities/capacity; frozen semantic splits and deployment masks/thresholds. The direct-text alternative, if used, is a separate architectural intervention.
- **A01:** supplied directed typed adjacency with explicit node binding; order-sensitive task targets; paired generators and initializations; strong graph-context and message-passing comparators receiving equivalent public information. Exact traversal is a programmed reference, not learned reasoning. Report parameter, attention-token and compute differences.

## Independent verification

Reconstruct primary raw metrics, per-seed denominators, paired intervention outcomes, calibration/selection provenance and gate decisions without calling producer metric helpers. Verify source/config/data/checkpoint hashes and historical blob preservation. Challenge target ambiguity, identity shortcuts, missing evaluation coverage and unsupported universal claims early. Audits do not consume GPU unless the coordinator explicitly releases a slot.

Current status: protocols requested; no campaign inference or training reviewed yet.

## Initial protocol review

R01 separates fresh fit/calibration/validation events, uses the same1024-dimensional frozen wide-backbone features for ridge/CE, and selects across covered-delay cells without exposing32. Optimizer exposure differs and is disclosed. A later consumer promotion must still demonstrate dependence on supplied values through interventions; prediction accuracy alone does not establish this.

S01 distinguishes nested corpus audits from completed scaling arms and proposes meaningful eight-epoch exposure to8192 alpha-distinct graphs. The128 TRAIN calibration subset is valid but relation support/fallback must be recorded. All target categories must remain publicly identifiable; canonical bookkeeping is not semantic evidence. Dev/confirmation must stay alpha-disjoint from every training prefix.

A01 explicitly supplies reverse-path dynamic programming and all-node intermediate supervision to all arms. This tests propagation interfaces rather than learned planning. Strong message passing and keyed graph context are appropriate comparators. Requested explicit continuation decisions rather than an ambiguous 'losses improve' rule, clearly labeled zero-lambda ablation, and corruption normalization/serialization that preserves equivalent supplied information.

These are protocol-level findings. Source preflight, resource profiling and coordinator release remain pending.

### R01 source preflight

Source `da66531` verifies the independently audited Stage11 wide-backbone checkpoint SHA, freezes the backbone and non-value heads, captures paired event features, and enforces split-hash disjointness. Ridge and CE consume identical TRAIN-standardized workspace features; CE warm-start equivalence is asserted. Only calibration scores choose regularization/checkpoints, with declared worst-cell then pooled tie-breaking. Development rejects delay32 outright. Mechanical profile seeds overlap subsequent fit/calibration only, not the untouched development validation seed. No source-level blocker to the bounded profile/development release; no confirmation or composition claim follows from this check.

### A01/S01 source preflight

A01 `53aa9f6` keeps gold suffix labels outside forward computation, supplies the same public reverse schedule, and scores corruption against clean targets. Context retrieves individual neighbor addresses before averaging, avoiding mean-key compression for multi-edge corruption. Initial tensor/configuration hashes are recorded, and procedural draw counts no longer imply canonical deduplication. The keyed context path explicitly supplies graph-indexed address wiring; it is a structured retrieval comparator. Mean-head argmax paths are diagnostics, not a unique trajectory for multihead computation. No blocker to clean development.

S01 `e6d1d28` keeps cached graph labels outside actor inputs, uses predicted node presence for TRAIN calibration and deployment, and does not recalibrate on development. Actual graph visits and presentation counts are recorded independently. The full8192-mixture coordinate-frequency baseline has no public-text features. The cache audit finds9728 distinct alpha constructions in9954 draws; canonical target-signature compatibility verification is completing before main freeze. No blocker to the bounded profile.

### R01 development raw audit

Independent reconstruction verifies90 profile and90 development cells, every field/group/scalar error count, frozen non-value outputs, and calibration-only selection. Development selects ridge alpha.01 and CE step900. Worst covered validation accuracy is1016/1024 for CE,865/1024 unchanged, and828/1024 ridge. This is promising one-backbone development, not three-seed confirmation or a historical six-field pass. CPU audit walltimes are .067/.839seconds. Cache/coefficient byte and feature replay remain separate checks.

R01 frozen-cache CPU replay completes in1.78seconds: compressed/uncompressed cache hashes, checkpoint/readout bytes, TRAIN normalization and split disjointness pass. All90 scalar logit cells replay with zero argmax disagreement, without backbone inference or refitting.

A01 profile archive independently reconstructs four task/suffix/mean-head-path cells in.036seconds. Edge-mass aggregate consistency and bounds are checked, but the compact archive lacks full attention weights; independent edge-mass reconstruction is not claimed. The profile remains a mechanical test.

### R01 prospective confirmation

Source `0df707b` freezes CE900/ridge.01 and asserts the endpoint rather than selecting among confirmation checkpoints. Delay32 is captured only after readout weights are finalized; fitting/calibration/validation remain through16. All three historical wide backbones are retained; backbone10 was already used in development, so this is fresh-event/readout confirmation, not three unseen backbone initializations. The prospective narrow scalar threshold is >=98% in every covered validation condition and seed. Causal returned-fact use requires a separate intervention study; neither a scalar pass nor exact copying passes historical six-field reconstruction.

S01 profile compact audit verifies128 raw/calibrated development graph decisions and26 TRAIN threshold records in8.81CPU-seconds, including calibration labels reconstructed from the independently stored sparse graph edges. Raw/calibrated exact recovery is zero at both profile endpoints; this is instrumentation verification, not a failed main acquisition experiment. Subsequent source `2c7e987` adds compact TRAIN predictions/targets before any main outcome, enabling independent acquisition-versus-transfer audits.

### R01 confirmation and uncertainty

All396 confirmation cells and all three frozen-cache replays pass independent reconstruction; CE remains at fixed900 and ridge at.01. Covered validation CE minima are1018/1000/1012 of1024; covered test minima1016/1000/1013. The prespecified three-seed>=98% contract **fails**, because seed11 at16 is1000/1024 in both distractor conditions (four correct answers short of the empirical cutoff). Historical CE six-field retention at16 passes seeds10/12 but fails11 on value; there is no all-seed pass. Cross-run cache identities verify27840 distinct events across18 profile/development/confirmation populations without overlap.

Per-seed Wilson intervals and4000-replicate paired event bootstraps quantify sampling uncertainty without changing gates. Event indices are resampled jointly across delays/distractors/heads within each split, while distinct seed populations stay separate. For validation16/eight distractors, seed11 CE95% Wilson interval is[.9654,.9842]; this does not turn its strict empirical failure into a pass. CE-minus-unchanged paired differences are positive in all seeds; gains are distinct from satisfying the requested absolute accuracy contract. Intervals are marginal/descriptive, not simultaneous guarantees or independence assumptions over delays.

### A01 development and next diagnostics

All175 development cells (five arms × seven checkpoints × five conditions) independently reconstruct task, all-node, suffix-value and mean-head pointer-path metrics. Initial tensor hashes match across all five arms. Hard/message/context reach perfect task accuracy under the supplied schedule; soft4 retains perfect argmax pointer paths yet only63/256 task accuracy onN32/D8. This localizes a value-propagation versus routing-argmax distinction, without proving its cause from correlation alone. A02 `83b4a6a` separately registers paired strength/size-prior development and frozen-weight strength interventions. No advantage over strong context/message passing is established. Corrupted topology can remove information needed to identify clean targets, so corruption sensitivity is not automatically failure of a soft recovery mechanism.

R02 `5eb6c9f` is a valid failure-directed development follow-up: original seed11 wide backbone/head, one maximal16384-event cache with nested4096/8192/16384 pools, fixed900 updates, fresh common calibration/validation, no32, and frozen recurrent weights. Actual visits and in-pool versus untouched-tail fitting distinguish insufficient exposure from novel-context errors. It does not reopen the failed R01 confirmation or narrow its contract afterward.

### Further localized development and confirmation preflight

Independent A02 reconstruction verifies 120 condition/checkpoint rows. On identical frozen A01 weights, replacing structural strength with 8 restores all five development cells to exact task accuracy; unchanged N32/D8 is 81/256 while its mean-head identity path remains correct. This supports finite attention concentration as a mechanism in this deliberately supplied traversal algorithm. It does not establish superiority over the already perfect message/context baselines. A03 uses separate final data, paired arms and three seeds; trainable strength initialized at 8 is distinct from its separately registered literal frozen-8 intervention. Clean-target and supplied-topology mass must remain separately named.

S01 N128 independently reconstructs 5,120 raw/calibrated graph decisions over four checkpoints and 52 TRAIN-only threshold records. Final complete recovery remains 0/128 TRAIN and 0/512 development. Gold-edge replacement yields 126/128 TRAIN; adding gold slots yields 128/128. Development remains 0/512 under either replacement. This localizes fixed-set failure but does not establish that edge-only repair will fix unseen language.

R02 independently reconstructs all 120 rows, nested event prefixes, six-field and group counts, and packed optimizer visitation. The minimum validation scalar counts increase from 2004 to 2014 to 2019 of 2048 as the fit pool grows 4096→8192→16384 at fixed 900 updates. All events are sampled, but only 88,839/98,304 rows in the largest pool; this supports the separately declared R03 exposure diagnostic. R03 must exactly replay the 900-step model, normalization, sample visitation and cached predictions before continuing its optimizer to 1800/3600. Its reused validation set remains development evidence; endpoint selection uses calibration only, with no delay-32 features.
