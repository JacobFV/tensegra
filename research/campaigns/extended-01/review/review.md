# Extended campaign 01: independent review

Baseline `123299a7c5312e1f75019fa491618943f54d0de2`; reviewer works in isolated `campaign/review`. Existing Stage10/11 audits remain authoritative for their frozen sources and artifacts; this campaign does not rerun those experiments merely to reproduce audit work.

## Scope and promotion

The campaign authorizes adaptive development, with failures informing bounded follow-ups rather than ending every track. Exploratory outcomes select recipes; only frozen confirmation on at least three paired seeds and fresh constructions (at least512 per meaningful cell, preferably1024) supports promotion. Track elapsed and experiment-process GPU occupancy, including unsuccessful runs and exports, within12GPU-hours/24elapsed-hours and preserve20% reserve.

Historical gates are unchanged. A newly declared narrow composition contract may advance at >=98% across three seeds on fresh validation only for the interfaces actually consumed. Exact copy/selection is a different contract from workspace-only six-field reconstruction. Formal validity and interventions remain necessary; no gold resets. Independent attention research does not require unrelated return or semantic gates.

## Preflight questions

- **R01:** frozen wide-backbone features; paired nuisance/events and head-training labels; training-only normalization; value-class support; identical selection rules; delay32 excluded from fitting/selection. Report optimizer exposure separately from distinct labels, and distinguish readout benefit from changes to representation or exact copying.
- **S01:** audit available and visited alpha-distinct constructions, not just surfaces; independent presentation and corpus-size axes; observable targets/copy identities/capacity; frozen semantic splits and deployment masks/thresholds. The direct-text alternative, if used, is a separate architectural intervention.
- **A01:** supplied directed typed adjacency with explicit node binding; order-sensitive task targets; paired generators and initializations; strong graph-context and message-passing comparators receiving equivalent public information. Exact traversal is a programmed reference, not learned reasoning. Report parameter, attention-token and compute differences.

## Independent verification

Reconstruct primary raw metrics, per-seed denominators, paired intervention outcomes, calibration/selection provenance and gate decisions without calling producer metric helpers. Verify source/config/data/checkpoint hashes and historical blob preservation. Challenge target ambiguity, identity shortcuts, missing evaluation coverage and unsupported universal claims early. Audits do not consume GPU unless the coordinator explicitly releases a slot.

Current status: campaign ongoing. Audited results and remaining limitations are recorded below; individual receipts distinguish raw reconstruction from cached inference replay.

## Initial protocol review

R01 separates fresh fit/calibration/validation events, uses the same1024-dimensional frozen wide-backbone features for ridge/CE, and selects across covered-delay cells without exposing32. Optimizer exposure differs and is disclosed. A later consumer promotion must still demonstrate dependence on supplied values through interventions; prediction accuracy alone does not establish this.

S01 distinguishes nested corpus audits from completed scaling arms and proposes meaningful eight-epoch exposure to8192 alpha-distinct graphs. The128 TRAIN calibration subset is valid but relation support/fallback must be recorded. All target categories must remain publicly identifiable; canonical bookkeeping is not semantic evidence. Dev/confirmation must stay alpha-disjoint from every training prefix.

A01 explicitly supplies reverse-path dynamic programming and all-node intermediate supervision to all arms. This tests propagation interfaces rather than learned planning. Strong message passing and keyed graph context are appropriate comparators. Requested explicit continuation decisions rather than an ambiguous 'losses improve' rule, clearly labeled zero-lambda ablation, and corruption normalization/serialization that preserves equivalent supplied information.

These are protocol-level findings. Source preflight, resource profiling and coordinator release remain pending.

### R01 source preflight

Source `da66531` verifies the independently audited Stage11 wide-backbone checkpoint SHA, freezes the backbone and non-value heads, captures paired event features, and enforces split-hash disjointness. Ridge and CE consume identical TRAIN-standardized workspace features; CE warm-start equivalence is asserted. Only calibration scores choose regularization/checkpoints, with declared worst-cell then pooled tie-breaking. Development rejects delay32 outright. Mechanical profile seeds overlap subsequent fit/calibration only, not the untouched development validation seed. No source-level blocker to the bounded profile/development release; no confirmation or composition claim follows from this check.

### A01/S01 source preflight

A01 `53aa9f6` keeps gold suffix labels outside forward computation, supplies the same public reverse schedule, and scores corruption against clean targets. Context retrieves individual neighbor addresses before averaging, avoiding mean-key compression for multi-edge corruption. Initial tensor/configuration hashes are recorded, and procedural draw counts no longer imply canonical deduplication. The keyed context path explicitly supplies graph-indexed address wiring; it is a structured retrieval comparator. Mean-head argmax paths are diagnostics, not a unique trajectory for multihead computation. No blocker to clean development.

S01 `e6d1d28` keeps cached graph labels outside actor inputs, uses predicted node presence for TRAIN calibration and deployment, and does not recalibrate on development. Actual graph visits and presentation counts are recorded independently. The full8192-mixture coordinate-frequency baseline has no public-text features. The cache audit finds9728 distinct alpha constructions in9954 draws; canonical target-signature compatibility verification is completing before main freeze. No blocker to the bounded profile.

### R01 development raw audit

Independent reconstruction verifies90 profile and90 development cells, every field/group/scalar error count, frozen non-value outputs, and calibration-only selection. Development selects ridge alpha.01 and CE step900. Worst covered validation accuracy is1016/1024 for CE,865/1024 unchanged, and828/1024 ridge. This is promising one-backbone development, not three-seed confirmation or a historical six-field pass. CPU audit walltimes are .067/.839seconds. Cache/coefficient byte and feature replay remain separate checks.

R01 frozen-cache CPU replay completes in1.78seconds: compressed/uncompressed cache hashes, checkpoint/readout bytes, TRAIN normalization and split disjointness pass. All90 scalar logit cells replay with zero argmax disagreement, without backbone inference or refitting.

A01 profile archive independently reconstructs four task/suffix/mean-head-path cells in.036seconds. Edge-mass aggregate consistency and bounds are checked, but the compact archive lacks full attention weights; independent edge-mass reconstruction is not claimed. The profile remains a mechanical test.

### R01 prospective confirmation

Source `0df707b` freezes CE900/ridge.01 and asserts the endpoint rather than selecting among confirmation checkpoints. Delay32 is captured only after readout weights are finalized; fitting/calibration/validation remain through16. All three historical wide backbones are retained; backbone10 was already used in development, so this is fresh-event/readout confirmation, not three unseen backbone initializations. The prospective narrow scalar threshold is >=98% in every covered validation condition and seed. Causal returned-fact use requires a separate intervention study; neither a scalar pass nor exact copying passes historical six-field reconstruction.

S01 profile compact audit verifies128 raw/calibrated development graph decisions and26 TRAIN threshold records in8.81CPU-seconds, including calibration labels reconstructed from the independently stored sparse graph edges. Raw/calibrated exact recovery is zero at both profile endpoints; this is instrumentation verification, not a failed main acquisition experiment. Subsequent source `2c7e987` adds compact TRAIN predictions/targets before any main outcome, enabling independent acquisition-versus-transfer audits.

### R01 confirmation and uncertainty

All396 confirmation cells and all three frozen-cache replays pass independent reconstruction; CE remains at fixed900 and ridge at.01. Covered validation CE minima are1018/1000/1012 of1024; covered test minima1016/1000/1013. The prespecified three-seed>=98% contract **fails**, because seed11 at16 is1000/1024 in both distractor conditions (four correct answers short of the empirical cutoff). Historical CE six-field retention at16 passes seeds10/12 but fails11 on value; there is no all-seed pass. Cross-run cache identities verify27840 distinct events across18 profile/development/confirmation populations without overlap.

Per-seed Wilson intervals and4000-replicate paired event bootstraps quantify sampling uncertainty without changing gates. Event indices are resampled jointly across delays/distractors/heads within each split, while distinct seed populations stay separate. For validation16/eight distractors, seed11 CE95% Wilson interval is[.9654,.9842]; this does not turn its strict empirical failure into a pass. CE-minus-unchanged paired differences are positive in all seeds; gains are distinct from satisfying the requested absolute accuracy contract. Intervals are marginal/descriptive, not simultaneous guarantees or independence assumptions over delays.

### A01 development and next diagnostics

All175 development cells (five arms × seven checkpoints × five conditions) independently reconstruct task, all-node, suffix-value and mean-head pointer-path metrics. Initial tensor hashes match across all five arms. Hard/message/context reach perfect task accuracy under the supplied schedule; soft4 retains perfect argmax pointer paths yet only63/256 task accuracy onN32/D8. This localizes a value-propagation versus routing-argmax distinction, without proving its cause from correlation alone. A02 `83b4a6a` separately registers paired strength/size-prior development and frozen-weight strength interventions. No advantage over strong context/message passing is established. Corrupted topology can remove information needed to identify clean targets, so corruption sensitivity is not automatically failure of a soft recovery mechanism.

R02 `5eb6c9f` is a valid failure-directed development follow-up: original seed11 wide backbone/head, one maximal16384-event cache with nested4096/8192/16384 pools, fixed900 updates, fresh common calibration/validation, no32, and frozen recurrent weights. Actual visits and in-pool versus untouched-tail fitting distinguish insufficient exposure from novel-context errors. It does not reopen the failed R01 confirmation or narrow its contract afterward.

### Further localized development and confirmation preflight

Independent A02 reconstruction verifies 120 condition/checkpoint rows. On identical frozen A01 weights, replacing structural strength with 8 restores all five development cells to exact task accuracy; unchanged N32/D8 is 81/256 while its mean-head identity path remains correct. This supports finite attention concentration as a mechanism in this deliberately supplied traversal algorithm. It does not establish superiority over the already perfect message/context baselines. A03 uses separate final data, paired arms and three seeds; trainable strength initialized at 8 is distinct from its separately registered literal frozen-8 intervention. Clean-target and supplied-topology mass must remain separately named.

S01 N128 independently reconstructs 5,120 raw/calibrated graph decisions over four checkpoints and 52 TRAIN-only threshold records. Final complete recovery remains 0/128 TRAIN and 0/512 development. Gold-edge replacement yields 126/128 TRAIN; adding gold slots yields 128/128. Development remains 0/512 under either replacement. This localizes fixed-set failure but does not establish that edge-only repair will fix unseen language.

R02 independently reconstructs all 120 rows, nested event prefixes, six-field and group counts, and packed optimizer visitation. The minimum validation scalar counts increase from 2004 to 2014 to 2019 of 2048 as the fit pool grows 4096→8192→16384 at fixed 900 updates. All events are sampled, but only 88,839/98,304 rows in the largest pool; this supports the separately declared R03 exposure diagnostic. R03 must exactly replay the 900-step model, normalization, sample visitation and cached predictions before continuing its optimizer to 1800/3600. Its reused validation set remains development evidence; endpoint selection uses calibration only, with no delay-32 features.

R02 CPU replay additionally verifies compressed/uncompressed feature and logit hashes, all three readout hashes, original backbone bytes, train-only prefix normalization, split event disjointness, and all 120 logit cells. Every argmax agrees. CPU wall time 5.88 seconds; no backbone inference or GPU work. S02 preflight now includes an expected parent-checkpoint hash before loading and explicitly distinguishes edge-only gradient clipping from the former joint norm; exact public-forward/cache parity remains mandatory before optimization.

A03 final preflight keeps the initial-strength and literal evaluation-override interventions distinct. Final events are paired across all arms and initialization seeds; repeated evaluations do not increase the number of independently drawn graph examples. Corruption diagnostics score the original clean target, with separately logged mass on supplied edges. Missing/wrong topology may erase the only available identifying information, so failure under corruption is not evidence that finite bias ought to recover the clean answer. Repeated payload values also make output accuracy insufficient to establish correct entity traversal; the independent identity-path diagnostic remains necessary.

S01 N1024 independently passes the same four-checkpoint metric/calibration audit (5,120 graph decisions, 52 threshold records). Every development complete-graph score remains 0/512. Its final 128-example TRAIN diagnostic subset reaches only 41/128 with oracle edges, or 47/128 with oracle edges and slots; all replacements still yield 0/512 development. This differs from N128's nearly pure fixed-set edge deficit. Both arms start from the same tensor hash and receive 65,536 presentations, with exactly512 versus64 visits per construction; token exposures differ slightly (3,892,224 versus3,864,704). This is matched presentations, not equal semantic diversity or equal memorization opportunity.

R03 independently reconstructs90 endpoint rows and calibration selection. The prescribed selection keeps900 updates (worst calibration1010/1024, versus1006 and1009); validation minima at900/1800/3600 are2019/2020/2016 of2048. The900 endpoint exactly matches R02's full predictions and visited-row bitset. CPU replay additionally confirms all90 logit cells, every argmax, feature/readout/backbone hashes, train normalization, split disjointness, and bitwise equality of the900 readout weights/normalization to R02. This is an adaptive development result: more fitting steps did not produce monotonic fresh performance, and no confirmation gate is evaluated here.

The compact semantic population receives a separate producer-independent audit: 9,728 distinct public texts and 9,728 distinct ordered node/edge alpha-keys across8192/512/1024 splits, with no cross-split overlap, incompatible identical-public targets, or conflicting slot labels on one node pair. This verifies the cached corpus contract, not universal linguistic identifiability or graph-isomorphism equivalence. R04's prospective balanced diagnostic selects unchanged generator events rather than modifying returns:52 legal value/type strata with64 contexts each, preserving exact operand witnesses. It is separate from the primary mixture gate and cannot be used for consumer selection.

A03 confirmation is independently audited across15 trained models:456 curve/final cells,276 final cells, all checkpoint byte hashes, matched shared initialization per seed, source/config hashes, and paired clean graph arrays. At the primary N64/D16 condition soft4 yields123/122/111 correct of1024; soft8, context, message and hard each achieve1024/1024 for every seed. Literal frozen override8 also restores each soft4 model to1024/1024. These share1024 graph draws across seeds, not3072 independent examples. Paired graph-index bootstrap across the fitted seeds gives an88.41percentage-point frozen-override advantage,95% interval86.85–89.84points; it does not quantify unseen-initialization uncertainty. Perfect1024/1024 cell Wilson lower95% bound is99.626%.

The final permutation audit explicitly retains one seed203 soft4 intermediate payload-argmax change among1,048,576 node/time entries. Routes and final task decisions do not change; all other arms have zero such discrepancies. Numerical boundary sensitivity is plausible but unverified without those logits. No bitwise-equivariance claim is made. This does not alter the prescribed task/path result.

N8192 completes the same independent four-checkpoint audit (5,120 graph decisions and52 threshold records). Whole-graph development remains0/512 despite stronger components. Gold edges now recover115/512 development graphs; gold slots add no further recoveries. TRAIN diagnostic128 has24 edge-only and26 edge-plus-slot recoveries. S03's prespecified exposure continuation preserves model/optimizer/pair and schedule RNG, checks parent hashes and initial prediction/threshold replay, and retains the cumulative curriculum. It is an adaptive development follow-up, not an independent replication or architectural change.

R04 independent review verifies432 raw cells and432 frozen-feature logit replays, all source/cache/checkpoint/readout hashes, nested populations and train-only normalization. The16k consumer passes every registered covered mixture validation cell across all three backbones: minima4079/4038/4049 of4096; covered test minima4086/4034/4057. Historical six-field marginal thresholds at16 also hold in the12 named mixture validation/test/distractor cells. These are new R04 mixture results, not retroactive historical passes or a uniform-value guarantee. Compared with4096 fitting events, the16k-minus4096 validation16/eight-distractor paired differences are0.049/1.221/0.708 percentage points; their95% event-bootstrap intervals are[-0.146,0.244]/[0.781,1.685]/[0.439,1.001].

The balanced grid exposes a concrete acquisition failure hidden by aggregate accuracy: seed12 float zero at delay0 is0/64, recovering to62/64 at1 and16. Seed11 float-7.5,-6,+7.5 each score49/64 at16. Every grid event remains a valid original generator event:9,984 selections and operand witnesses independently replay;36 R01/R02/R04 populations totaling134,080 events are disjoint. R03's deliberate reuse is excluded. Each value stratum has only64 contexts, a diagnostic rather than a precise population guarantee. MAE/signed means use a disclosed FP32 tolerance on non-power-of-two support3328; integer correctness counts are exact.

R05 preflight is restricted to an oracle-first-return, frozen scalar accessor, and newly learned comparator; it is not recurrent autonomous orchestration. Before any R05 outcomes, the exact query-only Bayes ceiling4324/5445 justified replacing a nearly maximal20point absence-drop requirement with15points plus80% of matched oracle-minus-query gain, retaining positive oracle gain, changed-fact tests and later98%-across-three-seeds confirmation. The prior draft remains history. Removing postselection delay32 is a prospective scope tightening, not a detected selection leak. Supplied swap metadata, dependency hashes, population disjointness and clean capture parity are now explicit.

### Acquired returned-fact use and the next attention control

R05 development independently reconstructs all126 raw cells and calibration-selected endpoints2000/1000/2000. Cached CPU replay verifies the frozen scalar accessor and actual learned, query-only and exact-value comparator inputs with zero argmax discrepancies. Learned clean accuracy is2041–2044/2048, versus2043 for the exact-value comparator and1634 for query-only. Removing the return reduces accuracy19.43–19.97 points; changed-answer wrong/swap controls follow supplied facts on867–868/871 and571–574/580 cases. These pass the prospective development checks, supporting frozen three-population confirmation rather than an autonomous-composition claim. The selected query-only endpoint has half the final optimizer presentations; identical development search budgets must not be described as identical selected exposure.

A04's320 compact cells pass independent target/path/supplied-fact reconstruction. Its learned content matching is meaningful, but generator-only analysis finds long paths coalesce toward one terminal: mean1.383 identities at depth32. The unrun A05 confirmation was therefore held for a separately versioned blockwise-bijective generator. This prevents a depth claim from relying on an effectively start-independent task; historical A04 outcomes remain unchanged.

S02's final edge-only refit reaches raw2/calibrated4 exact TRAIN graphs out of128, with zero development graphs; its midpoint34/128 calibrated result is not substituted for the registered final endpoint. S03's additional exposure still yields zero exact development graphs, but a privileged gold-edge replacement raises the ceiling to272/512 (280 with slots). These are localization results and motivate explicitly labeled compiler-contract diagnostics, not claims that the edge relation was learned.

### Restricted composition entry, not a historical gate override

R05 confirmation passes the original-mixture learned binary-consumer contract in all three fresh consumer/data populations: minimum covered validation counts4080/4081/4076 of4096. Independent audits reconstruct513 raw cells, replay513 cached actor/accessor outputs, verify weight/source bytes and23 disjoint populations, and preserve paired event uncertainty. Drop and supplied-fact interventions satisfy the prospective use checks. This is a learned decision from predicted scalar scores after an oracle first return, not yet a complete autonomous chain; balanced numerical and near-threshold tails remain diagnostic limitations.

P01 confirmation independently passes typed dispatch across three fresh seeds:4096/4096,4096/4096,4095/4096, with all five supplied-control cells meeting the same contract. Required binary operands are learned; unary null is canonicalized by the predicted primitive's supplied arity rule. Fresh-name replacement is an actor-input no-op. Regenerated public populations and selected-model CPU replay match all12288 validation records. Actual exact execution reproduces every field of the original return event for all12287 correct proposals. The remaining wrong multiplication is refused at the numeric bound, retained as failure, and never replaced with the gold operation.

These measured interfaces justify reviewing a narrow fixed-schedule composition under the campaign's new authority. They do not change historical Stage8–11 gates, establish language lowering, or authorize a claim that scheduling, primitive discovery or autonomous planning was learned. The composed experiment still needs strong public-information-matched neural comparators and causal runtime/return interventions.

### R07, S07/S08, and A06 raw reconstruction

R07 independently passes 84 archived-cell and 84 frozen-feature CPU replay checks. The residual decoder fails its fixed-budget acquisition criterion; the linear control exactly reproduces R06. This does not establish information absence or that every optimizer recipe fails.

S07 independently passes 5,120 raw/calibrated graph-decision checks and 52 training-only threshold reconstructions; complete recovery remains zero. S08 independently reconstructs 1,280 records and 32,472 node outcomes. DEV occurrence alignment increases from 3,118/9,780 to 8,698/9,780, while only 6,848 of those 8,698 correctly aligned identifiers have their required incoming ordered edge correct. This association localizes a remaining boundary without proving causality.

A06 all 405 curve/final cells independently reproduce task, path, supplied-target, and changed-outcome counts. Curve and final seed namespaces are checked separately. Full attention tensors are not archived, so attention-mass audit remains restricted to saved means/bounds. Paired confirmation uncertainty and checkpoint-byte checks are separate pending receipts.

A06 confirmation follow-up verifies all twelve checkpoint byte hashes, paired initial tensors, and nine clean populations shared across arms and initialization seeds. Resampling the same 1,024 event indices jointly across seeds confirms the paired concentration improvements. Corrected soft, exact-neighbor, and keyed-context methods each achieve 1,024/1,024 on the joint condition in every seed; these are not three independently sampled test datasets. The matched strongest controls remove a unique soft-bias advantage.

The S08 isomorphism diagnostic independently rejects all 3,840 archived decisions using necessary attributed-node or edge-label/slot multiset mismatches. No expensive search, semantic relaxation, or node-order-only explanation is needed for these failures.

C01 hybrid development independently reproduces all 65 archived cells and verifies six frozen checkpoint hashes. Direct public-operand arithmetic, types, argument keys, and destination provenance reproduce all 8,192 paired return events; no rejected proposal was repaired. Saved scalar scores passed through the frozen consumer reproduce every prediction in twelve clean cells. Joint lowering-and-answer accuracy is 4,081/4,096 at short delays and 4,076/4,096 at16, with all original lowering proposals correct in this development population. This is one frozen-interface development combination, not fresh three-seed composition confirmation; neural baselines remain pending.

S09 preflight finds no privileged forward input in the proposed copy-conditioned contextual read. Shared parameters do not equal shared compute; the preregistered additional-workspace-pass control remains required before attribution/confirmation. R09 preflight is a public binary ingestion/recurrent phase intervention, with two readouts and unequal per-head supervised exposure; it is not a per-delay oracle.

R08 follow-up CPU replay verifies all 210 feature/decoder cells, unchanged frozen reference weights, and cache/checkpoint hashes. All argmax predictions match archived GPU results. Calibration selects learning rate .0001; its minimum grid improves 59→60/64 while mixture validation declines 4088→4085/4096 versus the linear reference. This remains development tuning, not a confirmation or universal nonlinear-readout result. C01 N1 calibration-only LR/step selection and all six validation/control cells also independently reproduce; N2 remains pending.

On the shared C01 development population, N1 answers 4,056/4,096 correctly versus hybrid 4,076/4,096 after16updates. There are38 hybrid-only successes and18 neural-only successes. Paired-event bootstrap yields a +0.146 to +0.854 percentage-point interval, around +0.488 pp. Both methods perform strongly; this does not establish general symbolic necessity or equal training compute. All comparisons here use answer correctness, separately from the hybrid full-lowering gate.

S10 independently reconstructs all 15,360 decoder outcomes, output tensor hashes, graph metrics, and edge error changes. On calibrated DEV, combined supplied schema/bookkeeping yields 33/512 exact graphs for S01, 100/512 for S03, and 19/512 for S04. For S03 it removes 6,764 edge errors but introduces 702; predicted attributes remain fallible. This is an engineered compiler contract applied posthoc to inspected data, not learned semantic recovery or confirmation. The compact export retains exact hash verification without enormous inactive-edge lists.

C01 N2 calibration selection and six final/control cells independently reproduce: LR .0003, step 2000, 2,940/4,096 validation answers. The strongest acquired neural comparison remains N1, not N2's weaker acquisition. R09 all 84 archived cells reproduce, including the failed worst-calibration-stratum criterion 59→57/64 despite modest improvements in some aggregate metrics.

R09 cached-feature follow-up verifies all 84 binary-phase/linear predictions and exact R06 reference weights. All saved argmax predictions agree with independent CPU linear algebra. No confirmation or promotion follows the failed calibration criterion.

The immutable integration snapshot e08a489 passes 644 tests and six subtests in13.73 seconds with CUDA hidden and the existing CPU environment. S10 historical matched-comparator replay independently verifies10,240 additional decisions: S04 at196,608 presentations reaches183/512 calibrated combined graphs (5,094 edge errors removed,283 introduced), while its learned baseline remains zero. This supplies the matched-exposure comparator for prospective S11, not a newly selected historical checkpoint.

S11 preflight preserves AdamW moments, steps, sampler/order/RNG, and cumulative curriculum while changing restored optimizer LR to1e-5. Compare its fixed196k endpoint against the matched S04 endpoint. S10's frequency-plus-grammar control is an input-independent TRAIN8192 position/frequency prior; raw/calibrated labels are aliases, and no per-event copy inventory or token length is consumed.

The matched frequency-plus-S10 control independently verifies 5,120 decoder outcomes and 640 exact constant predictions. Its TRAIN hash, immutable baseline, wrapper source, and graph target populations match; no event text, copy inventory, or length enters the prediction. All variants remain zero exact TRAIN/DEV graphs. Therefore supplied bookkeeping alone does not reproduce the learned-actor-plus-contract result on this population, while it still must not be credited as learned semantics.

S11 independently verifies 3,840 archived graph decisions, 39 TRAIN threshold records, all three checkpoint bytes and the parent checkpoint. The initial model, optimizer moments/steps, and complete sampling state exactly match the parent; only optimizer-group LR changes. At matched 196,608 presentations, learned calibrated DEV exactness increases from 0/512 to122/512 (raw 0→13). This is an optimizer intervention in one inspected development initialization, not three-seed confirmation.

The unchanged supplied S10 contract independently verifies 5,120 more S11 outcomes. Calibrated combined exactness increases from183/512 in matched S04 to372/512:165 remain correct,18 regress,207 improve,122 remain wrong. Raw combined reaches381/512. Both policies remain reported; the better raw result is not substituted for the registered learned-calibrated endpoint. The constant frequency-plus-contract control stays at zero. Learned prediction, calibration, and supplied compiler guarantees remain separate categories.

C02 profile preflight verifies target-free public identity rekeying, a separate nuisance RNG, and fixed4000-step primary versus calibration-selected secondary outcomes. The strengthened historical-label/file guards must hold before a deterministic replay claim; any numerical parity deviations require coordinator review.

C02 static replay independently matches all seven historical calibration logit/label tensors and the selected N2 state, with three checkpoint hashes verified. All18 endpoint/control cells reproduce. Public rekey controls preserve exact identity links, null positions, labels, and every non-key input. Primary step4000 gives2,862/4,096 fresh answers; selected step2000 gives2,898/4,096. The paired rekey-trained arm remains pending, and both populations are development.

S12 preflight confirms three fresh parent initializations and paired state-preserving LR forks, with reserved model inference only at fixed final endpoints. The prospective competence threshold is103/1,024 per decay seed; replicated advantage requires positive gain in every seed and the declared shared-event bootstrap interval. Novel-spelling diagnostics keep semantic equality/copy positions unchanged but produce novel hash vectors; visible capitalization is not a separately encoded case feature.

C02 paired audit reproduces both arms’ data, initialization, 4,000-step sample stream and visitation, plus all archived endpoint/control outcomes. At the fixed primary endpoint, fresh development answer accuracy rises from 2,862 to 3,943/4,096: 1,169 rekey-only successes, 88 static-only successes, 2,774 shared successes, and 65 shared failures. The paired-event 95% bootstrap interval for the gain is 25.00–27.93 percentage points. Exact value accuracy rises from 557 to 3,385/4,096. This supports public identity-code augmentation in this development setup; it remains below the 98% threshold and is not confirmation or a symbolic-necessity result.

Integration snapshot 6c69914 passes 659 tests and six subtests in 12.49 seconds. The first export omitted a newly imported campaign analysis script, producing two file-not-found harness failures alongside 657 passes; the corrected export uses identical source. Both attempts are recorded (26.68 CPU test seconds), separately from experiment occupancy. C03 public role augmentation and A08 fixed-degree topology replacement pass source preflight for bounded profiles; neither profile substitutes for main evidence.

The prospective multilingual copy validator passes three isolated CPU fixtures (0.86 s). Privileged renderer alignment supplies per-surface pointer targets; inference does not translate hidden aliases. The red/rojo identity collision is rejected instead of receiving an oracle-metric pass from collapsed targets. Exact-text collision registration is narrower than actor-feature identifiability: the future builder must separately check tokenization/feature collisions. This is a small contract audit, not multilingual acquisition.

S12 parent701 independently reproduces all three DEV/128-TRAIN archived evaluations and 39 calibration records. Its checkpoint bytes, AdamW step16,384, complete sixteen visits per8,192 training constructions, and paired fork bindings verify. The two fork configurations differ only in LR/output path. No parent selection or reserved inference occurred; parent DEV complete recovery remains zero. A07 profile separately reproduces all four64-event cells and source/config hashes, without a competence claim.

A07 development independently reproduces all21 curve/final cells. Globally searchable edge-record attention is not yet acquired at the tested exposure; routing argmax can fail, unlike the earlier correct-path/diffuse-transport diagnosis. Curve-to-final accuracy changes use different populations and must not be presented as a paired learning slope. Earlier strong supplied-address context baselines remain relevant and must not be replaced by this weaker acquisition when assessing unique attention benefits.

C03 static independently reproduces seven historical calibration tensors, the selected N1 state, three checkpoint bytes, and18 evaluated cells. Actual public instruction selection, ordered operands, arithmetic values and downstream decision labels verify separately, including reversed views. Fresh clean/reversed counts are4,060/3,816 of4,096. The augmentation arm remains pending; neither role swaps nor fresh-name no-op controls establish language transfer. A09 exposure-extension preflight requires an exact1000-step parameter replay before continuation and charges the recomputed prefix.

C03 augmentation independently reproduces18 cells, clean-calibration selection at2000, three checkpoint hashes, and all4,000 paired sample/coin steps. Fixed4000 fresh clean accuracy decreases4,060→4,035/4,096 while reversed accuracy increases3,816→4,045. Both registered development thresholds pass, but the clean tradeoff remains explicit. Actual reversed public arithmetic labels verify, with no refusal repair. The strongest future neural baseline should retain this role-robust recipe prospectively; it is still one inspected development initialization rather than confirmation.

S12 constant701 independently reproduces both archived evaluations and26TRAIN-only calibration records, including0/1,024 exact final confirmation graphs under raw and calibrated decoding. Every final target is independently reconstructed from the reserved cache, whose exact hash matches the registered population; no TRAIN semantic overlap occurs. This is the first constant-fork outcome, not the result of the pending paired LR comparison. C04 source preflight clears actual-proposal execution, retained refusals, paired public samples, private overlap audits, and both changed-fact gates. Timing measures full independent paths; its reported peak allocation includes co-resident models and must not be called an isolated arm footprint.

S13 full-cache independent NumPy reconstruction verifies all8,704 constructions and17,408 English/Spanish surface-local target fingerprints, plus34,816 FP32/BF16 feature fingerprints. All8,192 historical TRAIN rows remain byte-field equivalent, and512 new DEV graphs are alpha-key-disjoint from every old TRAIN/DEV/reserved graph and each other. Exact text/features are unique; identity support and shared-pair slot constraints pass. Cache and historical population hashes verify. This establishes cache consistency under the pinned renderer and privileged label contract, not learned multilingual competence or independent linguistic adequacy.

S12 decay701 independently reproduces both evaluations and26TRAIN-only threshold records. Exact reserved graph recovery rises0→66/1,024 under registered calibration and0→2 raw; this seed fails the103-graph competence threshold despite positive gain. Both arms share every final target/event, initial predictions, parent/state/first-batch bindings and fixed exposure. Final checkpoint bytes, AdamW step24,576,24visits per TRAIN construction and complete paired final sampling states verify independently. The previously audited constant-arm reserved target reconstruction applies unchanged. Three-seed replication remains pending; this first pair cannot establish the campaign confirmation gate.

S13 trainer preflight verifies all13 inherited S11 source hashes unchanged and the fixed parent checkpoint binding. Renderer RNG is separate from the restored construction/pair streams; gold-selected training pairs only index loss outputs after public-only hidden-state computation. One English TRAIN128 threshold policy is applied to both languages, with raw outputs retained. Four added visits imply English4 versus mixed2+2, while differing surface lengths require actual token/runtime reporting. Prepared configs reject launch; pure-function pairing/balance checks pass without importing torch. Bounded profile remains contingent on complete S12 and separate release; this is one development parent with Spanish explicitly trained, not held-out-language transfer.

A09 independently reconstructs all25 archived curve/final cells, including supplied versus original targets and changed-answer/terminal conditions. All30 archived SHA entries verify locally or against remote CPU checkpoint reads. The step1000 saved parameters exactly equal the old A07 endpoint; final and intermediate optimizer steps plus RNG-state availability verify. GPU logit equality remains source/receipt verified rather than independently re-inferred. At2000steps final task correctness is437/512 IID,255/512 at64nodes/depth8 and72/512 on the joint condition. Acquisition improved but the declared gate still fails; curve/final populations differ and cannot form a paired slope. Recomputed prefix occupancy remains charged.

A08 independently verifies all72 frozen replacement cells, both oracle agreements, recursive target transitions and successor-edge membership. Replacement preserves graph size and every in/out degree; realized removed/added fractions are12.5%,25%,50%. Every seed/method exactly follows the supplied corrupt oracle at all fractions, while original-answer recovery falls identically across the three strong interfaces. The nine checkpoint bindings match prior independent byte audits and initial/final tensor receipts agree. Shared raw public data establishes paired populations; initial labels and attribute-based successor selection retain generator provenance. This is joint deletion-plus-addition under regular-degree preservation, not pure deletion, missing-address robustness or a unique soft-attention benefit.

A10 preflight independently passes the actual-runner CPU split/uninterrupted comparison for model, AdamW tensors and RNG state. The saved2000 state follows evaluation, so skipping that monitor at resume is required and implemented. The1000additional updates preserve seed/step sampling and restore CPU+CUDA RNG; CUDA numerical parity remains a runtime question. Independent173M monitor reconstruction confirms188→223/256 IID and86→129/256 at64nodes/depth8, with58.69%decline in the two declared100-step loss windows. CPU versus GPU evaluation remains disclosed. This supports the bounded acquisition follow-up with fresh201M final population, not a passed gate or automatic further extension.

Integration snapshot6ccb734 passes674tests and6subtests in15.60CPU seconds, with CUDA hidden and two threads. The immutable export includes imported campaign scripts and succeeds on its first attempt. Archive/tree hashes and complete test output are retained; this covers new C04/S13/A10 source without repeating historical raw audits.

C04 profile review independently verifies111raw files and188primary outcome/timing cells, neural calibration selection, paired sample streams and N1initial states. All source hashes match8a92869; five successful receipts sum21.218454seconds. Profiles have16events/16updates and do not establish acquisition, causal support or main throughput. Freeze c34effb preserves scientific runners; all three config hashes match published bindings and change only status/caps. Independent mocked launcher completion/failure verifies fixed endpoint timing bindings, serial stop-on-failure and durable receipts. The1980-second lineage/5940-second total caps are coherent prospective limits; separate scheduler release remains required.

S14 independently reconstructs1024pinned-generator examples and their historical semantic-key hashes, then verifies stronger root-omitted alpha disjointness against every old split and S13 English population. All15skipped seeds replay as earlier duplicates within1039attempts. The two ordered motifs contain525/499examples, are absent from historical TRAIN, use parent arity3 and retain depth3. Compact copy/finite-value/slot targets and FP32/BF16 full features pass independent NumPy identifiability checks, including excluded English populations, without importing torch or an actor. This supports a fixed smaller-arity structural diagnostic; it is not deeper or language transfer, new training data, model selection or a replacement S12 gate.

S14 all-six evaluator scientific preflight verifies checkpoint/primary/source/audit guards before any model, inherited final English TRAIN thresholds with no fitting, fixed frequency control and public-text-only forward. All128node labels/presence and active typed edges/slots survive compaction; the calibrated archive retains shared attributes and additional slots. Omitted inactive edges are ignored by the unchanged metric. Two independent CPU mechanical tests pass in0.77seconds without model construction. Launcher atomic receipt correction remains requested before freeze; all-six runtime bindings require the completed S12matrix and no inference has occurred.

S14 launcher follow-up830e77a resolves the requested durability gap: start and completion receipts use exclusive temporary files, flush/fsync, atomic replacement and directory fsync; existing start receipts prevent reuse. Independent isolated persistence check passes. Scientific evaluator and running S12 source remain unchanged. Prepared status still requires a separate freeze/release.

C04 aggregate statistical preflight independently verifies exact98%ceiling counts on attempted denominators, persistent refusals/absent supplied labels,≥256changed-event support and undefined/nonpositive normalization failures. The deterministic bootstrap matches an independent formula: separate per-lineage event draws, common across methods/views/delays, equal lineage weights and separate seed dispersion. Fixed/selected outcomes remain distinct, and absent lineages retain pending gates/intervals. FourCPUtests plus a single16event profile raw read pass (28hybrid/copy,8causal,198comparisons,32input hashes); no confirmation outcome or inference is used. Explicit main4096/4000 and neural/hybrid namespace guards have been requested before final analysis signoff.

C04 aggregate guard follow-up4c9adc6 independently passes5CPUtests: hybrid and every neural arm must match exact main seed/replicate, train/calibration/test namespace and16384/2048/4096counts, with fixed4000primary. This closes the requested profile/mixed-lineage guard gap.

S12parent702 independently reproduces all three DEV/128TRAIN evaluations and39TRAIN calibration records. Final checkpoint bytes, AdamW16384,131072presentations and16visits per8192constructions verify; both frozen forks bind the same checkpoint/manifest/evaluation and differ only in LR/output. Parent DEVexactness is zero; no reserved outcome is consumed.

A10 independently reconstructs all9monitor/final cells and verifies every archived SHAentry plus remote2500/3000 optimizer/checkpoint/RNG-state receipts. Restored initial tensors match the A09endpoint; exactly1000added updates/16000presentations are recorded. Final IID489/512 and64node/depth8 361/512 fail the98%/95%acquisition gate; joint96/512 remains limited. Fresh final populations remain separate from monitoring and this is one development seed, not confirmation.

A11 config preflight preserves the independently tested A10runner byte-for-byte and changes only the saved3000→6000endpoint, fixed monitors and fresh211Mfinal namespace. Resume model/state hashes match the independent A10checkpoint audit;3000additional updates imply48000presentations. Independent paired173MCPUmonitor reconstruction yields247/256IID and183/256at64nodes/depth8. This clears a separately released bounded acquisition follow-up; the previous failed gate and one-seed development scope remain unchanged.

A11 independently reproduces all11monitor/final cells and verifies the full artifact SHAinventory plus remote4000/5000/6000model/AdamW/RNG-state files. Initial tensors match A10 and3000added updates/48000presentations verify. FinalIID508/512passes98%, but64node/depth8 467/512fails95%; joint101/512remains limited. The joint acquisition gate therefore still fails. Exact local raw/config bindings are archived for figure provenance.

A12 source preflight verifies four fixed per-head interventions chosen solely by model-score argmax. Oracle successors and edge indices enter only posthoc observations after logits/hidden updates, with no next-step feedback. Independently run4CPUtests pass1.17seconds, including width1024unchanged parity, oracle perturbation independence, no state mutation and hardened own-argmax error zero. Checkpoint bindings match independent A11byte/tensor receipts. Conditional payload error retains numerator/support and selected energy; head disagreement prevents an unsupported single-pointer claim. Only bounded mechanical profile is cleared; supplied hardening cannot retroactively pass learned-soft gates.
