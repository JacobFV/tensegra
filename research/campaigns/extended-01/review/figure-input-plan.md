# Final scientific figure inputs (pending final matrix)

Use repository-relative roots `research/results/campaign-01/` below. Render standalone scientific SVG/PDF and PNG only after major confirmations settle. Freeze final input hashes and retain a machine-readable plotted table. Existing exploratory PNGs are useful references, not authoritative figure inputs.

| Panel | Primary inputs | Display and interpretation |
|---|---|---|
| Semantic learning and intervention | `semantics/s01-*`, `s04-*`, `s11-lr-decay-n8192-196k-dev201/manifest.json.gz`; S11 fixed endpoint audits | Complete-graph counts versus cumulative optimizer presentations, raw/calibrated separate. Highlight the S04→S11 matched-exposure comparison. Supplied S10 schema/bookkeeping must occupy a distinct series/panel. |
| Semantic fresh paired confirmation | `semantics/s12-{constant,decay}-{701,702,703}/manifest.json.gz` and final `evaluation-u24576.json.gz` | Per-seed connected constant/decay exact counts, threshold103/1024 line. Add joint shared-event bootstrap only after all pairs are audited. Show missing pairs as pending, never zero. Seed701 is0→66 calibrated, below competence. |
| Attention acquisition | `attention/a06/{601,602,603}/{none,soft,hard,context}/eval-*.json`; `attention/a07-development`, `attention/a09/results`; pending A08 main | Training curves use curve-population checkpoints only. Final-population results are separate endpoint markers/panels; they must not be connected to a curve as paired change. Label presentations and topology/address access. Keep strong hard/context controls alongside soft and graph-record learning. |
| Attention cross-condition robustness | A06 final `eval-01000.npz/json`, plus A08 and A09 final artifacts | Condition-by-method task counts; separate supplied/original target metrics for changed facts. Small seed dots; avoid treating shared events or conditions as independent replications. Saved attention mass is a diagnostic, not independently reconstructed full tensors. |
| Return mechanism and downstream use | `returns/r04-confirmation/{10,11,12}/predictions.json.gz`, `returns/r05-confirmation/{10,11,12}/predictions.json.gz` and manifests | Per-seed paired readout/use outcomes across delays and interventions. Separate scalar recovery from downstream decision success; preserve margin strata and finite-domain limits. Use R04/R05 uncertainty receipts; do not pool repeated delays as independent events. |
| Composition with strongest neural comparison | `composition/c03-development/{static,roles}/raw/endpoint-*.json.gz`, `paired-analysis.json`; pending C04 confirmation | Show fixed endpoint clean/reversed tradeoff for development. C04 final panel should retain all three lineages, actual emitted proposals/refusals, joint lowering+answer versus answer alone, and both changed-fact gates. Supplied scheduling is part of the task definition. |

Audit anchors under `research/campaigns/extended-01/review/`: `S11-paired-audit.json`, `S12-pair-*-audit.json`, `A06-confirmation-audit.json`, `A09-development-audit.json`, `R04-uncertainty.json`, `R05-confirmation-uncertainty.json`, `C03-paired-audit.json`, and future A08/C04 receipts. Use exact event arrays for new paired intervals, jointly resampling the same event indices across fitted seeds. Seed variation is displayed separately from conditional event uncertainty.

Suggested compact report: three multipanel figures (semantic acquisition/confirmation; attention learning/conditions; return use/composition). Include a small experiment-status table for failures and missing matrices rather than omitting unsuccessful arms. S13 currently contributes only a cache/protocol table; no model outcome exists. Full integration suite and final report/registry reconciliation remain separate checks.
