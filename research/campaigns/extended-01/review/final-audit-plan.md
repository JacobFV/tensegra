# Final campaign verification plan

This is a pending checklist, not final signoff. Reuse completed raw audits and their immutable input hashes; do not repeat historical model inference.

1. Freeze the final integrated Git commit and tree. Verify every one of the 3,953 tracked baseline files at `123299a7c5312e1f75019fa491618943f54d0de2` retains its blob content. Check clean worktrees, diff whitespace, accidental large files and additions outside the authorized campaign layout.
2. Run one final CPU integration suite only if executable source changed after the latest verified snapshot. Export source/tests/configs/scripts/research tools with metadata-only Git support, hide CUDA, use the existing environment and two threads. Record archive/tree hashes, failures, reruns and elapsed time separately from experimental GPU occupancy.
3. Reconcile the frozen experiment registry against actual run matrices. Every declared seed/arm/cell must be present, explicitly failed, blocked or not run. Preserve unselected development recipes, stopped jobs, profile scope and abandoned pre-outcome variants.
4. Check new final artifact, checkpoint, source, config and data hashes against immutable copies. Completed historical receipts suffice when their exact inputs are unchanged. Audit new S12 parent/fork bytes and state equality; historical-baseline hashes are not substitutes for new checkpoint verification.
5. Independently reconstruct remaining confirmation metrics from per-example predictions and targets. Preserve exact denominators, ordered bindings, all required fields, refusals, original versus actually supplied facts, and raw versus TRAIN-calibrated decoding. Verify calibration/selection used only declared partitions.
6. For paired confirmation, retain event identities across arms and seeds. Bootstrap shared event indices together; report seed variability separately. Do not count repeated delays, distractor conditions or identical seed-level test populations as independent samples.
7. Validate S12 final-only reserved inference, three independent parents, identical fork exposure/state, TRAIN-only thresholds and fixed frequency/S10 policies. Keep learned graph recovery, supplied compiler rules, and optional renamed-hash transfer separate. Rename inference requires its own frozen evaluator/source before execution.
8. Reconcile process receipts with the central budget and queue: profile/train/inference/export occupancy, stopped jobs and conservative upper bounds; CPU audit/test costs separately. No overlapping unauthorized GPU owner, hidden retry, or omitted search arm.
9. Review final reports and gate JSON against audited counts. Retain the strongest acquired neural/context/message-passing controls. Historical gates remain unchanged; narrow mixture, finite-domain, typed-record and known-renderer successes must not become general cognition, language or unique-attention claims.
10. Publish a concise independent verdict with unresolved limitations and missing coverage. Composition is a supplied schedule with exact semantics plus learned interfaces; a successful local contract does not retroactively establish autonomous orchestration or a prerequisite for every programmable-attention hypothesis.
