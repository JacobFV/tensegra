# Decisions

- Start from actual clean123299a; prior stages complete, GB10 idle. No reset/interruption.
- Keep root as coordinator. Separate worktrees/branches for returns, semantics, attention and independent review; root alone releases GPU jobs.
- Use three independent initial branches, not another single-stage gate termination. Approximately20% budget reserved for confirmation and intervention.
- Historical composition blocks describe prior evidence. New bounded composition is conditionally authorized by the user, with contracts tied to consumed outputs.
