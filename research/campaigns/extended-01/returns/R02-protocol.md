# R02 development: fresh context diversity at fixed optimization exposure

R01's failed confirmation is retained. Backbone 11 is deliberately selected because its sixteen-step float reconstruction failed, despite perfect finite fitting accuracy. This is failure-directed development, not a fresh confirmation claim.

Use the same immutable Stage 11 wide backbone 11 and original scalar-head warm-start. Do not initialize from a successful R01 refit. Fit three CE consumers on nested fresh event pools of 4,096, 8,192 and 16,384 events. Generate the full 16,384-event batch once, then take prefixes; generating different batch sizes separately would not preserve event identity. Capture all six delays 0/1/2/4/8/16 once per event, with two distractors for fitting. Each arm uses its own train-only mean/std, algebraically equivalent original-head warm-start, AdamW .003, exactly 900 updates and batch256. The optimizer sampling seed is shared. Count actual visited feature rows and events; all arms have230,400 optimizer presentations, but effective epochs differ.

Fresh seed namespaces: fitting22100101, calibration22200101 (1,024 events), validation22300101 (2,048 events). Calibration and validation are common across arms, with two/eight distractors and paired events across delay. No R01 confirmation event is reused. No delay32 is exposed or selected. Calibration curves are diagnostic only: retain fixed update900, without checkpoint selection. Inspect all three arms only after their fixed fits are complete.

Primary comparison: exact scalar reconstruction on the same fresh validation cells, especially delay16. Preserve zero-step and every other covered condition. Report fitting accuracy, minimum/population/per-value/type/primitive support, signed and absolute error, top-one/top-two margin and true-label margin, with complete raw logits. Other fields remain unchanged. Perfect fitting with weak fresh tails supports a generalization problem; poor fitting in the larger pool would motivate an exposure diagnostic instead of attributing failure to diversity.

This three-point comparison selects a further development question, not a main competence claim. Increasing events at fixed updates also changes average revisits and empirical normalization estimates. It does not isolate these incidental consequences from diversity. No new encoder, memory, recurrent weights, head architecture or loss is introduced.

Existing R01 measurements put extraction at approximately1.7s per1,024 events through16. The 16,384-event fitting cache plus common evaluation, three900-update fits and lossless export are estimated at70–110 seconds. Request a conservative300-second process ceiling, using the existing profile rather than another redundant neural profile. All GPU work requires coordinator release. Initial return envelope remains2GPUh, with actual process occupancy charged.

If this improves the failing boundary, freeze the chosen data/exposure recipe and run a new confirmation with fresh populations. If it does not, use train/fresh diagnostics to choose between more optimization exposure and a separately declared readout hypothesis. Do not narrow the failed R01 contract or launch composition based on these development outcomes.
