# R04: fresh confirmation of fitting diversity at fixed optimization exposure

R01's 4,096-event refit failed the declared aggregate scalar contract on historical backbone 11 at delay 16. R02 increased fitting diversity while retaining 900 optimizer updates; R03 did not justify extending those updates. R04 freezes **16,384 fitting events and 900 CE updates** before capturing any new confirmation data. No architecture, representation, loss, backbone or non-value head changes.

Use the existing `topoformer.campaign_returns` runner and `configs/campaign-r04-confirmation.json`. The three Stage 11 wide backbones remain initialization seeds 10/11/12. Seeds 10 and 11 have informed development, so these are fresh event/readout-fit replications, not unseen backbone initializations. Each run receives independent fresh fit/calibration/validation/test populations in the 24-million namespace, checked against each other and archived populations by event hashes. Each evaluation population contains 1,024 events reused across delays and distractor counts.

Fit a categorical readout on normalized scalar workspace features at delays 0/1/2/4/8/16, with 16,384 events × six delays = 98,304 available rows, 900 updates × batch 256 = 230,400 optimizer presentations. Train-only standardization and the original frozen-wide-head warm-start remain unchanged. AdamW learning rate .003 is fixed. Report actual class/type/operator support and fresh context generalization within the same 33 half-unit classes. No numerical-range claim is possible.

Compare unchanged frozen head, fixed ridge alpha .01, and fixed CE endpoint 900 on identical features. Ridge is a contextual alternative with different fitting objective/exposure; no causal attribution to loss alone. Calibration curves are diagnostic only: **no alpha, endpoint, threshold or scope selection** in this confirmation. Test delay 32 is captured only after the readouts are finalized, excluded from fitting and all selection. Delay 16 is trained coverage; 32 is extrapolation.

The prospective narrow scalar contract is at least 98% exact reconstruction in every validation cell (0/1/2/4/8/16 × two/eight distractors) for each of the three runs: at least 1,004 / 1,024. Preserve all failed cells. Report float/integer/Boolean and per-value tails even if aggregate criteria pass; the contract does not assert 98% for each subtype. Report all non-value and joint metrics independently. A scalar pass cannot change the historical six-field gate. No narrowing to 0–8 after outcomes.

If the prospective scalar contract passes, request a separately frozen returned-fact-use experiment with dropped/wrong/swapped return controls. No consumer training or composition is authorized by this protocol alone. If it fails, characterize fitting versus fresh tails and select the next smallest discriminating experiment without reusing this confirmation for another pass claim.

Expected process occupancy 180–260 seconds based on R01/R02 measured capture costs; requested hard cap 300 seconds. Store compact raw predictions and all scalar logits, frozen readout coefficients, feature caches, source/config/checkpoint hashes and external process timing. Independent audit must reconstruct selection-free comparisons and cross-population separation. GPU launch requires coordinator release.
