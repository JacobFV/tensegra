# R05 confirmation: freeze the learned return-use interface

Freeze the development-selected endpoints before new data: learned-access and exact-value comparators at 2,000 updates; query-only at 1,000. All other architecture, input transformations, losses, optimizer settings and causal semantics remain unchanged. Calibration curves are logged but cannot select another endpoint. Existing R04 16k/900 accessors and Stage 11 wide backbones remain immutable.

Run historical backbone seeds 10/11/12 with new consumer seeds 32000010/11/12 and independent fresh data. Backbone 11 informed this task's development; all three backbones have historical training. This is fresh consumer fitting/context confirmation, not a claim of unseen backbone initialization.

Each run fits on 8,192 events across 0/1/2/4/8/16 with two distractors. Calibration has 1,024 events, validation and final test each have 4,096; evaluation uses two/eight distractors. The fixed query-only endpoint consumes 256,000 optimizer presentations, versus 512,000 for the learned/exact-value arms. Their common optimizer-stream prefix and initialization are paired, but final exposure is not equal. Disclose this rather than calling the chosen recipes exposure-matched. All three have 38,914 trainable parameters and a 1,024-wide hidden layer.

Primary advancement requires at least 98% learned decision accuracy in every covered validation cell for each run (at least 4,015 /4,096). The privileged neural comparator must retain its >95% acquisition check. Causal criteria remain: at least 15 percentage points of paired original-answer drop when the return is absent; at least 80% of positive matched exact-value-minus-query-only gain; at least 90% supplied-fact agreement on answer-changing wrong/swap subsets, with at least 256 changed cases per cell. Report paired uncertainty and all absolute effects. No threshold is selected from confirmation.

Causal captures use the first 2,048 events of the already generated 4,096-event validation batch, preserving exact same-event comparisons rather than regenerating a different-size batch. Delays are 0/1/16, eight distractors. Wrong values preserve legal type and exact operand witnesses; swapped returns include their identity-key inventory. Drop removes availability before ingestion; it does not erase an existing latent by fiat. Store both original and supplied labels/types/operations.

After all three consumers for a run are fixed, capture final tests through delay 32. Thirty-two is genuine recurrent-length extrapolation and is excluded from fitting, calibration and selection. The separate 64-per-legal-type/value grid uses fresh nuisance contexts and delays 0/1/16/32, eight distractors. Its prevalence and possible failures remain distinct from the unchanged mixture. Binary decision success cannot establish exact reconstruction of every value.

Keep scalar-accessor score traces, all consumer logits, complete raw targets, event identities, checkpoint hashes and actual fitting exposures. Independently audit all counts, causal subset identities, frozen actor inputs and data separation. A pass supports only teacher return → frozen learned scalar interface → new learned comparator. It does not show autonomous first-operation generation, second symbolic execution, adaptive scheduling or original-workspace orchestration.

Estimated full process occupancy 300–450 seconds from the 68.97-second single-backbone development plus larger final evaluation. Request a 600-second hard cap, with immutable source/config freeze and explicit coordinator release. No outcome-driven recipe changes or retesting for a pass.
