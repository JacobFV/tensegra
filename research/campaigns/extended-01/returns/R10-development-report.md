# R10: more balanced contexts modestly improve tails, without passing the screen

The64kbalanced fitting pool improves the worst fresh value/type stratum relative to16k, but fails the prospectively registered calibration rule. Both fixed consumers reconstruct all6,656fresh balanced ingestion examples correctly. The remaining errors are predominantly late recurrent access, not a replicated ingestion-class blind spot.

| Fixed3,600-update consumer | Worst mixture calibration /1,024 | Worst grid calibration /128 | Worst mixture validation /4,096 | Worst grid validation /128 |
|---|---:|---:|---:|---:|
|16,384balanced contexts|1,019|115|4,076|108|
|65,536balanced contexts|1,021|118|4,070|116|

The fixed advancement rule required all mixture calibration cells≥98%, minimum grid calibration≥122/128, and a minimum gain≥4/128. Only the mixture component passes. No confirmation or endpoint substitution follows. Validation does not override that decision.

The16kcalibration minimum is float−5.5 at16updates; the64kminimum is float−2.5 at16. Fresh validation minima are float−7.5 and float−6, respectively. The changing worst class cautions against treating one numerical label as the entire failure mechanism.

## Paired access rather than a generic retention conclusion

Balanced validation scalar counts at0/1/16updates are6,656/6,655/6,568 for16kand6,656/6,654/6,593 for64k, each over6,656. At16updates the larger pool fixes69scalar errors and introduces44;19remain wrong and6,524remain correct. These are paired outcomes on identical events, not products of marginal accuracies. Joint semantic accuracy changes6,453→6,476; non-value joint remains exactly6,539 because those heads and their backbone are unchanged.

The larger pool does not dominate the original mixture: its worst validation cell is4,070/4,096 versus4,076. Both satisfy the restricted mixture threshold, but the grid improvement is not an unconditional improvement across populations.

## Acquisition and exposure

The16khead's own fitting accuracy after16updates is16,341/16,384; on the49,152additional contexts it did not fit, it scores48,557. The64khead scores65,057/65,536 on its full fitting set. Its last300minibatch losses average.00608 versus.00295 for16k. Thus, quadrupling distinct contexts at fixed921,600presentations also leaves more training error. This does not isolate an immutable representation limit from incomplete optimization.

Both pools have balanced52legal type/value strata,315/316versus1,260/1,261contexts each, with an exact nested event prefix. Actual distinct fitting events sampled are16,384and65,536; distinct delay/event rows98,295/98,304and355,355/393,216. The64karm therefore receives fewer repeat presentations per available row. All rows use unchanged generator witnesses and nuisance identities. Separate train-only normalization reflects each fitting population.

## What was supplied and learned

The supplied interface is a perfect public typed return event, persistent six-facet memory, frozen Stage11wide backbone11 and finite33-value domain. The learned intervention is only the existing33,825-parameter linear scalar consumer; both arms use the same3,600-update AdamW recipe. No phase input, numerical encoding, memory change, backbone training, exact copying or new primitive was introduced. Non-value outputs remain fixed. Fresh nuisance contexts are tested; new numerical ranges are not.

This is one development backbone, selected because of a historical late-tail failure. It cannot establish replicated uniform-value reliability. R04/R05/C04interfaces and historical gates remain unchanged. No delay32was captured or used for selection.

## Cost, integrity and next decision

Full GNU-time driver occupancy was249.14seconds (precision.01s; coordinator conservatively charges249.15). Training capture116.885s, evaluation capture47.764s, two fits7.788s and endpoint decoding5.931s. Remaining child work, predominantly lossless export, was68.975s; it is included in occupancy. Peak CUDA allocation5,126,853,120bytes; process peak RSS10,264,158,208bytes. Allocated GPU memory and CPU RSS are not interchangeable.

All108raw cells, source/config/checkpoint hashes, exact paired predictions, selected event identities and lossless remote features/logits are preserved. Independent audit is pending; no final competence claim relies on an unaudited pass.

The result does not justify simply enlarging the dataset again. A cheap potential next diagnostic would test whether the same fixed-feature linear objective is adequately optimized: the last loss blocks still fluctuate, and extra diversity increases fitting error. An optimizer-only comparison could reuse these immutable features, with a new prospective protocol and endpoint. That is a hypothesis for the coordinator, not an automatic retry or permission to tune on these validation events indefinitely.
