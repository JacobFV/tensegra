# S20 additive runner preparation

Implements the accepted three-lineage protocol with unchanged historical actors, S18 `optimizer_update` and matched `evaluate`, plus S19 field losses, learning-rate schedule, strict record decoder/evaluation and forced160-step timing. No historical source was edited. Original/context restore each lineage's exact S12 decay u24576 model and all98 AdamW states; scratch record resets to the same numeric lineage seed and a new optimizer. All nine training streams independently reset generator15115 and preserve the registered construction/query hashes and exposures.

`train_endpoint` accepts only TRAIN. The sole evaluation-cache loader is called after that arm returns its fixed endpoint; it rejects an incomplete update count, duplicates or changed support. Main loads no DEV; profile has no confirmation input binding. SHA-only verification is permitted before training. Workspace evaluation fits its existing global threshold vector once on actualTRAIN128, retaining raw and calibrated outputs. Record evaluation separately retains teacher-forced diagnostics and public-only greedy predictions, with malformed/codec failure receiving no partial credit. Evaluation state hashes must remain identical.

The top manifest has ordered `results` (seed701 original/context/record, then702 and703). Each arm lives under `seed-{seed}/{arm}` with a final `manifest.json.gz` and `model-u{update}.pt`; update is24576+added for workspace and added for record. Workspace exports retain existing `matched-u{update}/evaluation-u{update}.json.gz` and calibrationNPZ schema (including TRAIN rows). Record exports retain existing `train-u{update}.json.gz` and `confirmation-u{update}.json.gz`; profile uses `development` instead. All timing, inherited/incremental history, parameters, state/checkpoint hashes, sampler/visits and construction/query exposure counts remain explicit. No intermediate evaluation or selected endpoint exists.

Prepared configs remain unallocated: profile20updates for each of three seed701 arms, finalTRAIN128+DEV16/cell, record public160-step stress; main4096updates for all nine arms, finalTRAIN128+confirmation512/cell. The profile180-second proposal needs root allocation/review/freeze; main cap follows measured full setup, optimizer, final calibration/evaluation, checkpoint and export costs. GNUtime must wrap GNUtimeout KILL with exactly the approved cap outside the lifecycle wrapper, which resolves child PYTHONPATH from its own source. No automatic next release, retry, cap extension, or dependency change.

CPU validation:47 new/S19 tests passed plus14subtests, one optional codec corpus test skipped (2.80s);21 historical S18 mechanics/runner tests passed (1.43s). Tests cover all-nine matrix guards, no evaluation reads during TRAIN preparation, endpoint-only access, source/cap/input protection, parent+AdamW identity, scratch reset, exact S19 optimizer replay, stream reset, and durable lifecycle failure/timeout/source isolation. Existing actor tests include actual width1024 FP32/BF16 cached-prefix logits/argmax parity. A separate real-data CPU preflight verified all selected parent and input hashes,4096TRAIN/128panel and82–123 record lengths in5.649s; constructors were replaced by raising sentinels (zero actors/forwards). Confirmation was only hashed, never decompressed or parsed.
