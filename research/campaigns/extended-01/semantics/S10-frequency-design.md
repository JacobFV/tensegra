# S10 complementary frequency-plus-grammar control

Prospectively registered before control outcomes. Reuse the exact archived all-8,192-TRAIN English coordinatewise frequency prediction in `research/results/campaign-01/semantics/s01-frequency.json.gz`, produced by the historical `campaign_semantics_baseline.py`. It stores its complete packed prediction and training-cache SHA256. No refitting, conditioning, threshold choice, or DEV selection is required.

Apply the unchanged four S10 policies (`2085053`) to this constant prediction for the same TRAIN128/DEV512 target populations. The archive's existing DEV baseline metrics must replay exactly before applying the supplied policies. Verify the cached TRAIN hash equals the archived fit hash. Report every arm; frequency prediction has no independent calibrated score policy, so the duplicated raw/calibrated labels in the generic reconstruction format are aliases, not two fitted controls.

Remaining inputs/priors: fixed canonical output coordinates, node/type/value/copy-index/slot frequency modes, edge-majority frequencies, the public output schema, and reference equality of the predicted copy-index labels. This baseline consumes **no per-example text, token features, copy inventory, token length, or renderer information**. Its finite copy-index support was determined by maximum TRAIN token count; that is a fitted position prior, not current-event addressing. No filtering of out-of-range predicted copies or additional per-event validity mask is introduced. Grammar repair may therefore expose how much prior/constant-output structure suffices; it is complementary to the competitive learned actor, not its replacement.

Primary metrics remain exact canonical graphs and typed/ordered F1, with per-event repairs/regressions, active edge errors and prerequisite counts. All learned S10 outcomes and primary actor gates remain unchanged. No GPU; two CPU threads and 300-second process cap, including wrapper archive/export. Source/config frozen and independent preflight before outcomes; independent reconstruction afterward.
