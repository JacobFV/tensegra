# S22 prospective complete diagnostic analysis

`S22-analysis.py` will reject execution until the separate immutable main config is pinned. Its complete matrix requires both unchanged S21 backbones, all three named privileged NODE-boundary policies, seven DEV512 cells, preserved public-only S21 reference artifacts, exact data/source/checkpoint-state/receipt/artifact linkage, and no training/calibration/confirmation input. No acquisition threshold or promotion gate exists.

The loader binds every event's seed, semantic ID, graph digest, public-cell identity and target to the exact cached DEV and unchanged public reference. It reconstructs packed strict components, requires Boolean validity and reason consistency, checks invalid outputs receive zero credit, and links all summaries to the SHA-bound raw artifact. Controller statistics must match the supplied NODE count and policy; forced tags/kinds/full prefix are checked against archived records, replacement counts are bounded, and records after EOS are rejected. Independent strict codec/reference/checkpoint audit remains required.

Output retains per-cell complete/valid/node/value/copy/edge/slot results, strict exact nodes and relations, invalid reasons, all privilege/replacement counters, macro graph F1 with invalid-zero credit, deterministic first failed semantic IDs and complete artifact inventory. Raw/canonical predictions stay in immutable input files. Exact nodes/relations use strict valid-output scoring, so invalid-edge failures cannot be presented as a valid recovered graph. Full-prefix relation recovery is explicitly conditional on supplied gold nodes and never a learned public-only competence claim.

Paired comparisons include every oracle policy versus public-only, kinds versus count, and prefix versus count/kinds. Predeclare10,000 paired bootstrap draws per512-event cell with NumPy default_rng22023, lexicographically sorted semantic IDs, cell order2×4/3×3/3×4/4×3/4×4/5×3/5×4. Share draw indices across backbones and policy comparisons within each cell; cells have separate draws. Percentile95% intervals describe fixed-model event uncertainty only and are neither promotion criteria nor multiplicity-adjusted inference.

Six synthetic analysis fixtures pass: policy counters, privilege direction, forced kind/prefix identity, terminal placement, strict validity flags, paired transitions and refusal of an unpinned main. They read no diagnostic outcomes. Full run begins only after every policy/backbone and whole-process receipt closes. Report how additional supplied information changes recovery, preserve regressions as well as corrections, and keep S19/S20/S21 histories and failed S21 advancement unchanged.

The main pin is now fixed prospectively to `911a70787dfd31bd1225c04cc2d7c4c2a8b9d721396c12a754d47f1cacfb910e`, allocated cap1,160, unchanged scientific source7a4c2923. The197-file read-only main snapshot and GNUtime/KILL1,160 command have separate immutable bindings; CPU source/input/endpoint and unused-prefix guards passed0.7042 seconds. Six analysis fixtures still pass after the pin. No main outcomes have been opened; independent operational clearance and root GPU release remain separate.
