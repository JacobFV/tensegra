# Conditional confirmation of the fixed S21 corpus intervention

Prospectively recorded while S21 development is running, before either main outcome is inspected. **This protocol is eligible only if the unchanged S21 final4,096 joint development gate passes after both arms close and independent audit. If any component fails, this confirmation is not authorized.** This document allocates no compute, launches no job, and does not imply automatic promotion. No sealed confirmation contents are inspected here.

## Fixed conditional comparison

If eligible and separately registered by root, use three new paired scratch initializations **2201/2202/2203**, each original-versus-broad. Retain the exact S21 original and six-motif4096 TRAIN corpora, learner, supplied typed record contract, loss, AdamW/LR schedule, capacity, batch8,4,096updates, schedule generator15115,32,768 presentations and per-corpus token/node/edge/record exposures. Within each seed, both arms must start from identical scratch model tensors and empty AdamW state. Across seeds, initialization is independent. Development seed2101 is not a confirmation replicate. No architecture, renderer, vocabulary, calibration, threshold or exposure change is permitted under this conditional protocol.

Use final-only evaluation: each arm's same frozen TRAIN128 diagnostic panel and all **seven fresh512-example cells**, totalling3,584 confirmation constructions. Sealed cache path `data/s21-six-motif-v1/confirmation.jsonl.gz`, pre-existing audited SHA256 `15491a12b7e508c3997531a4f69a0e68ac97a0c5c4165b977158928bf037824e`. This hash is copied from the pre-model data audit; contents are not opened during this registration. Preserve audited historical/split alpha exclusions. TRAIN panels are in-sample diagnostics, with no calibration. All six endpoints and raw artifacts must close before aggregate interpretation; no interim confirmation-based stopping, model changes, seed dropping or reruns for favorable outcomes.

## Fixed all-seed decisions

For **each** seed separately, using the identical512 events per cell in its paired arms:

- Broad heldout3×4 complete≥52/512 **and** broad-minus-original complete≥26.
- Broad old-known3×3/4×3/4×4 total complete no lower than original total minus76. Loss77 fails the exact integer retention boundary.
- Broad complete≥256/512 in **each** newly trained2×4/5×3/5×4 cell.

A positive replicated claim requires the unchanged joint conjunction in **all three** seeds. Report every cell, seed, arm, component and continuous paired effect regardless of outcome. Do not replace all-seed success with pooled accuracy. These thresholds match the development protocol; they do not establish generalization beyond the particular omitted arity/fact combination. If the all-seed conjunction fails, preserve the failed confirmation without an automatic extension or new threshold.

Report paired2×2 complete-graph transitions, exact component counts, validity reasons, teacher-forced field diagnostics and corpus-specific work. For descriptive95% paired event-bootstrap intervals, use NumPy default_rng22022 with10,000 draws of512 indices within each cell, cells ordered2×4/3×3/3×4/4×3/4×4/5×3/5×4 and semantic IDs sorted lexicographically. Reuse identical event draws across all three seed pairs; draws are independent across cells. Report each seed's broad-minus-original interval and a separately labeled mean over these three fixed pairs. These intervals describe event uncertainty conditional on the trained seeds, not a population-of-seeds confidence claim, and do not change the integer gates.

## Preconditions and boundaries

Eligibility requires closed/audited S21 joint advancement, then a separate root registration, immutable sources/data/configs, seed/stream/initial-state checks, independent prospective review, a final-only timing profile or justified reuse of measured phases, whole-process caps and explicit GPU release. Budget is **unset** here. Forecast all six4,096-update arms, final TRAIN128 and3,584 confirmation graphs per arm,74-token/160-record-step worst-case decoding, full optimizer checkpoints, codec/export work and reserves. No profile may open sealed confirmation; mechanical timing uses the existing inspected DEV population.

Retain S19 and S20 findings/gates unchanged. This conditional protocol tests reproducibility of the S21 **data-distribution intervention** with an unchanged learner; it is neither an isolated architectural experiment nor permission to use S20 confirmation for any choice. If S21 fails, stop this conditional path and diagnose its archived development outputs under a separately authorized task.
