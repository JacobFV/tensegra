# S18 confirmation prerequisites: existing parents and reserved population

CPU-only inventory while S18 runs; no current S18 outcomes inspected, actor constructed, model forward, optimizer step or GPU access. Remote deserialization used CUDA hidden and two CPU threads. All seven listed checkpoint byte hashes match their immutable manifests; model shapes/order match the S11 parent, all have98AdamW state entries and57,853,781model values, with saved sampler/order/position/visits. Full paths, hashes, source/config provenance and optimizer group settings are in `S18-confirmation-inventory.json`. Remote verification took6.911seconds.

**The three S12 decayed endpoints already provide independently initialized, stage-matched parents.** Their schedule matches S11:16,384updates at1e-4, then8,192at1e-5; total24,576AdamW steps and196,608presentations,24visits to each of the same8,192TRAIN graphs. The earlier files named `s12-parent-*` are only at16,384steps/131,072presentations and are not the desired stage. S12 constant-LR fork endpoints have the wrong learning-rate history for this matched recipe and are not additional independent seeds.

| Candidate parent | Stage/update | Checkpoint SHA256 |
|---|---:|---|
| S11 development201 (current baseline) |24576|3799ade595500a083b9a558b6a5bc97c5cbc1b8bdce6e7e4b86d97b937ea373e|
| S12 decay701 |24576|f419fe818626fc23b98f8e0bb7a89491ff5e015e529b5facb68ee5281d2e808a|
| S12 decay702 |24576|64b3b035c386982d3f9563ff7e24bbd459fba8432cadaa60ffc89bf4c5d8261f|
| S12 decay703 |24576|be5c324a24d5a0741b4c66159e46a92f32b8d1691c51df887a17402ccb3267a2|

Remote paths are `results/s12-decay-{701,702,703}/model-u24576.pt` under `/home/brandonin/topoformer-campaign01-semantics`. Every decayed optimizer group retains LR1e-5; steps are exactly24576, and complete AdamW moments are retained. Each seed starts independently from random initialization and owns its optimizer/sampling trajectory. These are independent initializations conditional on shared data/architecture, not unseen historical experiments: S12 results were inspected. Do not filter parents by competence, choose the strongest, or drop seed701's failure. Within a future paired lineage, all three S18 arms must receive the exact same selected parent model+AdamW.

Original S11 used an extra evaluation checkpoint during its LR continuation; S12 used only initial/final fork evaluation and final reserved-population inference. Evaluation does not update parameters/AdamW and training sampling uses saved explicit generators, so this is an evaluation-provenance difference rather than a different optimizer exposure. Model shape/state inventory establishes compatibility, not equality of independently initialized weights. Current S18 code/bindings still pin seed201: use of another parent requires new prospective bindings/configs and differential review, not a silent substitution. The prescribed S18 mixed-corpus sampler resets to4096indices; existing old-corpus sampler states remain archived but are not a schedule to continue over the new corpus.

The suitable untouched construction population is `data/s15-shape-v2/confirmation.jsonl.gz`, SHA **2710e5e1b6fffab6fa33d56105de13e49d46d64c63d81f61c6ea331347229d32**:2,048alpha-distinct constructions,512each3×3/3×4/4×3/4×4, reserved before v2 TRAIN selection in namespaces9171/9172. This inventory rechecked zero alpha overlap with final mixed/control TRAIN and DEV. The prior independent cache audit covers historical S01/S13/S14 exclusions and actual packing/support. Current S15/S17/S18 protocols contain no inference on this confirmation population; hashing/support audit is not model inspection. The unused v1 confirmation cache is byte-identical, so it is not another holdout. S12's1,024original reserved graphs and lexical variants have already been inspected, and S15 DEV is development, not unused confirmation. No new population is generated or selected here.

**Cost correction:** the4000second S18 main limit is a conservative hard cap, not a measured training estimate or lower bound. The prior follow-up note's4600-per-lineage/9200-two-lineage arithmetic must not be used to conclude replication infeasible. The stage-matched parents already exist, so their historical acquisition need not be repeated. Incremental replication still requires all three mixed-shape training arms per additional seed: seed201's reused S15 mixed baseline cannot replace a new seed's original trajectory. Use completed actual S18 main optimizer/evaluation/process timings, original S15 timing and the root's remaining ledger to estimate that work plus confirmation. No seed count, replication protocol, cap or GPU schedule is chosen by this inventory.
