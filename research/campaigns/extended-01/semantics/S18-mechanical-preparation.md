# S18 actor and compute-control mechanics

CPU-only additive preparation; no training runner, threshold/data-policy decision, GPU profile or main authorization. `campaign_semantics_s18_actor.py` dispatches to the unchanged S09 `CopyConditionedActor`: original arm disables its read, contextual arm uses its exact0.5 residual/two-microstep implementation, workspace control disables the read with a prospectively supplied integer recurrence. Historical source/entrypoints are untouched. Public text/features, canonical queries, copy head, bilinear edge head and additive slot head are unchanged. The adapter introduces zero learned parameters and cannot accept gold targets or a parser/normalizer hook.

Eighteen CPU mechanical tests, including the existing S09 tests, passed in1.34s after the final parameter-count assertion (earlier pass1.52s). New coverage checks exact context-off model bytes, outputs, all gradients, inherited AdamW slots and next-update replay; exact context-on S09 outputs/gradients; edge-only gradients through learned copy addressing and contextual activations; padding invariance, public-only rejection, sampled/dense consistency, fixed recurrence replay and recipe rejection. These width16 fixtures are not acquisition evidence. Main1024 parameter shapes/order are checked on the meta device; all three retain57,853,781 parameters. Separate real-parent metadata audit deserializes checkpoint3799ade... onto meta tensors, validates every ordered parameter and all98 AdamW slots against each arm, and makes zero forwards. Numeric optimizer replay is tested on the small CPU fixture, not asserted from metadata alone.

Compute planning reads only public text from the SHA-bound S15 mixed4096 cache. It replays all4096 batches of8 under schedule seed15115 and the inherited `next_indices` rule. Every construction occurs eight times. Construction sequence SHA25685054bf25e3a3e2a5a9a932b441d6413fd0d7ad827df79e1583268aaddf5acd9 exactly matches the existing completed S15 mixed-v3 manifest. Public lengths46/54/64 occur2048/1024/1024 times; padded batch maxima46/54/64 occur20/418/3658 times. No labels or outcomes enter this calculation.

For width w, workspace rows s and public length t, a block's counted MACs are `(10s+2(t+1))w²+2s²w+2s(t+1)w`, including the existing cross-memory sentinel. Four blocks make a workspace microstep. Context arm retains two workspace microsteps plus eight token-row blocks and reused read projections `(t+128)w²+128tw`. Counts exactly reproduce the archived S09 values at lengths54 and64. Select one global integer≥2 minimizing absolute total training-schedule MAC difference, lower integer on ties. **The resulting control uses10 microsteps for every training and inference call.** It does not adapt to inference batch lengths; thus compute matching does not introduce batch-dependent predictions.

Counted schedule MACs: original57,423,784,050,688; contextual276,544,914,718,720; control287,118,920,253,440. Control/context=1.038236 overall, with per-batch ratios1.032709–1.150870. These exclude unchanged feature projection, node decoder and graph heads, elementwise/masking/normalization/softmax work, backward and AdamW; they are not measured FLOPs or time. Equal parameter count is not equal cost. Actual three-arm CUDA profiles and a registered lifecycle budget remain necessary before training. No thresholds/calibration population were selected or changed in this preparation.
