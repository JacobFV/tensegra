# S03: selected current-actor exposure continuation

Selected from the completed, prospectively matched-diversity S01 development ladder. N8192 achieved DEV copy.95768 and calibrated ordered-edgeF1.85424 at65,536presentations, improving.13013/.13880 over its preceding checkpoint. These exceed the registered.02component-improvement extension rule. Completegraph remains0/512; this selection is development-driven and must not be relabeled confirmation.

Continue the exact N8192 seed201 optimizer trajectory from8,192 to16,384updates, totaling131,072presentations (16visits per TRAIN construction). Preserve all57,853,781parameters, actual width1024, eight workspace rows, data, public actor inputs, ontology/copy vocabulary, losses, AdamW moments/step/lr, pair-sampling RNG, shuffle RNG, current order/position and visits. No architecture, loss, calibration policy or data extension. The existing stochastic operations use the two saved explicit generators; the actor has no nonzero dropout. Global RNG is not present in the historical checkpoint, so no claim of preserved unspecified randomness is made.

The parent checkpoint and final evaluation SHA256 are pinned in config and checked before loading. Re-evaluate the resumed state at update8,192 and require exact agreement on every TRAIN128/DEV512 raw graph, calibrated edge set, targets and thresholds before any optimization. Stop on mismatch rather than silently changing precision. Save immutable continuation checkpoints and source/config/data provenance at8,192/12,288/16,384updates; report cumulative presentations, unique constructions, tokens and new GPU occupancy separately. TRAIN calibration remains first128 records and never accesses DEV. Reserved1,024confirmation remains unseen.

No early stopping or checkpoint selection: final16,384update result is the declared endpoint, midpoint is a curve only. Extension to262,144total presentations is a separate future queue decision and requires the same registered gain rule on the last interval (DEVexact gain≥.02 or copy/orderedF1 gain≥.02). Absence of improvement motivates a focused diagnostic, not an automatic capacity claim. Completegraph remains primary; report raw/calibrated component and frequency controls unchanged.

Measured baseN8192 training cost496.955s for8,192updates; external665.953s including startup/data/evaluation/export. The same additional8,192updates with three evaluations projects~11minutes. Request900s full-occupancy cap; reuse the measured representative profile rather than spending another GPU profile on an unchanged training step. Root release required. This is one development trajectory, not a paired-seed confirmation.

Before deleting the loaded checkpoint, independently replay the next batch from cloned saved shuffle/pair generators and save its TRAIN indices, public instance seeds and sampled-pair hashes. The first actual continuation batch must match that prediction exactly. Curriculum weights use cumulative update count, not a restarted schedule. Initial evaluation is deterministic and does not consume the two explicit generators. A mechanical CPU fixture verifies continuation across a shuffle boundary.

## Second tranche, selected after131,072presentations

The final first-tranche interval improves DEVorderedF1 by.03685, meeting the predeclared.02extension rule. Freeze `campaign-s03-exposure-262k.json` with exactly16,384additional updates, endpoints16,384/24,576/32,768updates and cumulative262,144presentations. Same source d743464, no altered learning rate/objective/actor/inputs; new parent checkpoint/evaluation hashes pinned. Replay and firstbatch checks repeat. No early stopping/selection; no extension beyond262,144is automatic. Measured495.141s per8,192updates projects~990soptimizer and~20minfull occupancy; request1,500s cap. Root release and reviewer acknowledgement required before launch. Reserved confirmation remains untouched.
