# Semantic campaign handoff

Root alone owns GPU queue. Worktree campaign-semantics; S01 frozen runner2c7e987 and external launcherfef09e9. Remote ~/topoformer-campaign01-semantics; CUDApython ~/topoformer-stage8-cuda/bin/python.

S01 matched TRAIN128/1024/8192, seed201,65,536presentations each; common512DEV and first128TRAIN calibration. Reserved1,024 confirmation untouched. N128 complete603.831s; N1024 complete646.974s; N8192 pending root release. Same initial state SHA confirmed. Both completed arms0complete graphs, but DEVcopy improves46.97%→72.44%, calibrated orderedF1 .50009→.66262 under matched presentations. Do not interpret as no learning; current actor diversity/exposure ladder continues. Full artifacts/report committedceac237/d84db7e.

S02 existing edge-head optimization diagnostic registered8970eae and guard fixes7844351, reviewer preflight approved. Captures public learned-node vectors, freezes all nonedge components, preserves BF16 dense/sampled formulas and inherited edgeAdamW/RNG/schedule; exact cache/public-logit parity required. Profile32updates8/8 needs queue release, then measured main cap before separate release. Main128TRAIN/512DEV,0/1200/4800updates, primary exactedge128/128; frozen slots impose126/128wholegraph ceiling. No new architecture or gold-node inference. CPU parity fixture passed.

Root scheduled A03 thenR03 before S02profile and S8192. No semantic job currently running. Use separate campaign_semantics_edge_launch.py for S02; do not change frozenS01launcher. Check GPUidle before queue-authorized launches. External receipts count full process occupancy, not only optimizer/inner runner. ProfileS01 was14s conservative external versus12.048inner; do not rerun it.
