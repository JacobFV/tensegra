# Prospective reuse of frozen S10 public-schema policies

Registered before new S11/S09 final outcomes. Decoder source remains `2085053`; no parameter, threshold, type admissibility, scope prerequisite, reference matching, or slot policy may change. The same baseline/schema/bookkeeping/combined policies apply to every TRAIN128/DEV512 raw and calibrated cell. These are secondary supplied-grammar diagnostics and never replace the actor's primary raw/calibrated metrics or historical gates.

Matched comparisons:

* **S11 final 196,608 presentations (u24576) versus original S04 intermediate 196,608 (u24576):** same S03 parent, training constructions and total exposure; S11 is the separately registered LR-only continuation. This analysis does not select continuation checkpoints.
* **S09 final 65,536 presentations (u8192) versus S01 final 65,536 (u8192):** same supplied graph targets and public input population; S09 has its separately declared node-query access intervention. Its primary learning protocol is unchanged.

Report every policy, checkpoint, split, and raw/calibrated mode, including exact canonical graphs, typed/ordered F1, paired per-event correctness transitions, introduced/removed active edge errors, scope abstentions, copy-reference prerequisites, and output tensor hashes. Match events by stored seed and verify exact target equality; repeated events do not count as independent new samples. No threshold selection from these outputs. Node predictions remain learned; grammar and equality bookkeeping remain supplied. An improvement in this secondary interface is not autonomous semantic inference or structural-attention evidence.

Old comparator outputs may be computed immediately. New outputs are evaluated only after their immutable producer archives exist; record their SHA256 on use. S01 historical reuse must reproduce prior S10 outcomes exactly. No cherry-picked checkpoints, failed runs, or policies may disappear.

Each invocation is CPU-only, CUDA hidden, two threads, timeout 300 seconds; process wall/RSS receipts retained. No GPU/training. Independent reconstruction audits remain required. The previously audited S10 decoder is reused without changing historical results. Future experiments motivated by these observations require their own registration and fresh confirmation.
