# Independent S21 data acceptance audit

**Passed for the frozen dataset contract; this is not model competence.** The independent checker prepared before generation (`6349e13c`) regenerated all11,264 prescribed rows from their seeds, existing generator settings and compiler. It never consumed model outputs. The reserved confirmation construction metadata was used only for independent data correctness and exclusion checks, not recipe selection or scoring.

- TRAIN4,096: exact prescribed six-motif quotas,2,048 exact reused S15 rows and2,048 fresh rows. The shared subset equals the prospective global-seven-cell-index seeded selection.
- DEV3,584: all seven cells512;2,048 exact reused S15 DEV rows and1,536 fresh rows.
- Reserved confirmation3,584: all seven cells512, all fresh.
- All11,264 are alpha-distinct across these three splits. Every fresh row avoids the41,638-key independently reconstructed prior inventory. Historical reuse exceptions are exact source records, not mislabeled fresh data.
- Full generated nodes, edges, canonical semantic digest, full graph digest and English public text match regeneration for every row. Independent matching of ordered pattern arguments against facts recovers exactly one correct answer/option for every row.
- Public copies, unchanged finite-value vocabulary and strict record codec pass for every row; maximum42nodes/143records fits128/160. No graph is truncated or inferred from gold node count at runtime.
- An additional independent scheduling check verifies exact broad corpus shuffle, old TRAIN subset ordering, broad128 diagnostic panel, and original S17 TRAIN128 panel binding.

## Independent public-feature argument

Across baseline plus all new split rows,15,360 presentations contain13,312 unique lexical feature sequences and28 distinct public tokens, with maximum74 tokens. No two different public tokens collide in the actual64-bit lexical feature encoding; no identical token-feature sequence maps to incompatible canonical targets. The lexical coordinates are0/1, exactly representable in bothFP32 andBF16. Consequently, rounding positional channels cannot collapse distinct lexical sequences. This independently establishes the finite-dataset feature identifiability property rather than merely trusting the builder's numerical feature hash audit. It does not establish learnability or transfer.

## Provenance, cost and limits

Raw input/cache hashes are retained in `s21-independent-data-check.json`; second audit binds the builder audit SHA256 `8cb6a99f717d82c64864a92ac2934effb0ba00e05575e31c9f299e62ddf46469` and panels SHA256 `c1abc21807d07bbb336b87278926d5a3f052e0470373ba265edf336b3dd02736`.

First frozen checker:12.294177s CPU wall. Independent panels/features checker:2.107397s. Total14.401574s; zero failures/retries, GPU occupancy0. Generator-only, no neural modules imported. The previous39.903115s exclusion reconstruction is a separate already reported cost, not charged again here.

The prior inventory still discloses unknown undocumented historical constructions as a coverage gap and conservative overexclusion in tiny configured ranges. This audit does not remove that caveat. These seven motifs remain shallow; holding out3×4 is one omitted combination, not general arbitrary graph induction. Confirmation model inference remains sealed behind its prospective experiment protocol.
