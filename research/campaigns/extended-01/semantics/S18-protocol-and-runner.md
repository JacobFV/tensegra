# S18 matched contextual-node-read development protocol

This is an S15/S17-informed development intervention, not independent discovery. Original2pass is the strong equal-information baseline. The contextual arm is the exact existing S09 predicted-pointer read with0.5 residual; the other new arm is the same original actor with the audited fixed10pass recurrence. All share full English public text, canonical queries and57,853,781 parameters. The contextual residual feeds presence/kind/value/edge/slot heads; copy logits are computed from the pre-read base. Therefore it is a contextual **node-read** intervention giving edge losses access to pointer/context, not an edge-only numerical intervention. No supplied parse, graph, equality, schema mask, normalizer or changed copy objective is added.

## Reuse and training equivalence

Reuse the immutable completed S15 mixed original training trajectory and all four checkpoints, with S17's matched-policy endpoint. The runner's stdlib preflight verifies their exact bytes, historical source hashes/config, same independently selected S11parent3799ade..., inherited model and AdamW, mixed4096cache8ddbd15...,4096updates/batch8/LR1e-5, seed15115 order and150150000+presentation pair RNG,32,768presentations/eight visits, and all saved curve bindings. Training update helper preserves the historical order: zero gradients, BF16 public forward, inherited curriculum-weighted corrected losses, finite check, backward, norm1 clipping and AdamW step. New arms must finish with exactly the original construction/query hashes and token/node/edge exposures. Width16 differential tests reproduce the inherited update and optimizer state byte-for-byte through three updates; actual checkpoint shapes/order and off-path gradient/AdamW replay were independently audited previously.

Evaluation is no-grad with an actor having no dropout, restores train mode, and does not consume the explicit training generators or mutate parameters/optimizer. Changed calibration evaluation therefore does not require equivalent original training again. Reused baseline historical full training/evaluation occupancy is484.141407571seconds, retained as inherited work; it is not a fresh independent run or free newly acquired capability. Reference bindings are in `S18-bindings.json`; independent semantic audit01085d1d provides the separate equivalence assessment.

S17 already exports final matched TRAIN128 panel and full DEV predictions, so **no final baseline forward is requested**. A separate `reference` job evaluates only frozen original checkpoints at added0/1024/2048 on the same matched panel/DEV population, verifying raw graphs and targets exactly against historical S15 exports. It has no optimizer or training. Existing historical-policy curves remain labeled historical; missing matched early curves are not relabeled. The reference job remains uncapped/unlaunchable pending a measured budget and separate release.

## Fixed evaluations and decisions

Use the exact S17 mixed selection:64 actual TRAIN3×3,32 TRAIN4×3 and32 TRAIN4×4 examples in original cache-index order. Same128indices for both new arms and reused baseline. At every checkpoint fit the inherited global relation-threshold rule on this panel, applied unchanged to all four DEV512cells; no per-cell fit/DEV selection. This matched policy is primary. Run the historical TRAIN128 calibration policy as a separately labeled diagnostic at the same checkpoint, retaining raw predictions and asserting raw/target equality across policies. Main curves are added0/1024/2048/4096;4096 alone determines gates. Store full calibration NPZ scores/targets/pairs, packed predictions/targets, thresholds, metrics, model and optimizer checkpoints and stream/exposure hashes.

The fixed mixed128panel is both a fitted calibration panel and an acquisition diagnostic; its calibrated exactness is optimistically in-sample. Report its raw and calibrated results separately from fresh DEV and split trained3×3 versus known-arity cells. It is not an independent generalization set. No extra panel is selected from outcomes, and its repeated use does not alter training exposure. Its results diagnose acquisition but do not replace the registered DEV gates.

Retain the planning-note endpoint gates: contextual trained3×3 calibrated exact≥52/512 and≥26/512 above both original and10pass control; known-arity pooled exact≥103/1024 and loss≤51/1024 relative to either control. Recombination requires separately held-out3×4 exact≥52/512 and≥26/512 above both controls. Preserve per-cell components, relation FP/FN and paired uncertainty; do not promote from component F1 or a favorable intermediate endpoint. Checkpoint0 and full curves must remain visible:10pass warmstarts from a2pass-trained parent, creating its own phase/depth shift. It tests this declared compute allocation, not an optimized compute baseline. Beating it does not establish unique efficiency. Additional paired seeds and untouched confirmation remain separately frozen/released; no automatic continuation or exposure extension follows a miss.

## Profile, accounting and CPU checks

The proposed frozen profile is two serial new arms,20updates each, full two TRAIN128 calibrations at added0/20, first16DEV per cell (64total) per policy/checkpoint, and full checkpoint serialization. Proposed whole-wrapper cap180seconds; this freeze is not GPU authorization. Profile outputs exist solely for mechanical replay/timing and must not select models, policies, counts or future exposure. Calibration/TRAIN-panel, DEV, export, model setup, optimizer and checkpoint-export times are separated. Main prediction/evaluation costs scale DEV count64→2048 and checkpoints2→4 while calibration remains128, rather than multiplying fixed setup by those ratios. Main4096training updates, all curves, both policies, baseline-reference inference and source/process/serialization tails must all enter the later cost proposal. Main/reference prepared configs have no cap and reject launch.

All23 CPU tests pass in the final CPU check (the earlier21-test pass took1.52seconds). They include the18 audited actor tests plus exact inherited-update differential, recipe/source/cap rejection, selected-population validation and atomic failed/timeout receipts with no retry. Full actual artifact/reference/data preflight passes2.599480seconds with actor creation replaced by a raising sentinel:0 constructions,0 forwards,4096TRAIN,128matched/128historical panel,64profile DEV. Main model width remains1024. CPU staging is isolated `/tmp/s18-runner`, CUDA hidden; historical sources and S17 files are unchanged.

Use a new immutable S18 source snapshot and output namespace. Lifecycle wrapper includes its source/reference checks and complete child in the fixed cap, atomically fsyncs start/final receipt, checks GPU idle, rejects preexisting outputs, stops on failure/timeout and never extends. GNUtime must wrap the entire launcher through exit with conservative centisecond accounting. Root remains sole GPU scheduler. Development aims≤1.5hours with approximately2.4hours campaign confirmation reserve; no numeric main cap is frozen until these profiles and independent review complete.
