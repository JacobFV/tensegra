# S05: programmed identifier/entity relation diagnostic

CPU-only diagnostic selected after S03 residual errors concentrate in `refers_to`. This is a programmed schema/equality decoder applied to frozen learned attributes, not learned coreference, a new neural architecture, or a change to S04 training.

Compiler inspection: `semantic_graph.compile_term` maintains one `entities[value]` table per construction. Every `ident` occurrence refers to the unique `entity` with the same exact string. Both receive the same visible identity-copy target; the existing public decoder normalizes repeated-token positions to their first occurrence. Audit all8,192TRAIN and512development constructions for this equivalence and visible-identity injectivity. Stop if any counterexample occurs. Do not inspect reserved confirmation predictions or create new targets.

Predeclare one primary rule on archived **predicted** attributes: edge(i,j,refers_to) iff predicted presence(i),presence(j),kind(i)=ident,kind(j)=entity and predicted copy(i)=copy(j). Multiple predicted matching entities produce multiple edges; no gold tie-breaker. Missing entities yield none. All other predicted edges, attributes and slots remain unchanged. Preserve raw and original TRAIN-calibrated policies separately; no new threshold fitting. Compare baseline, programmed refs, and privileged gold refs replacement. Gold only scores/audits; never enters the programmed function. Record references with zero/multiple matching predicted entities.

Report completegraph, refers-to precision/recall/F1 and exact relation sets, full typed/ordered graph metrics and every example. Exact graph still includes original copy/type/slot errors. Primary population is the already-inspected S03development archive512 plus TRAIN128diagnostic, one initialization; no fresh generalization or confirmation claim. Programmed and oracle arms are labeled separately. This scoped compiler contract says nothing about lexical shadowing, separate runtime scopes, aliases with different visible names, ambiguous natural-language pronouns or unseen languages.

Root authorized this CPU diagnostic; no GPU compute, training, or S04/source changes. Implementation/tests/source/data hashes freeze before applying the programmed rule to archived predictions. Known gold-refers-to ceiling27/512 is disclosed selection context, not a newly independent test.
