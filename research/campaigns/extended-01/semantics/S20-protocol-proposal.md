# S20: three-lineage fresh confirmation of known-motif acquisition

Prospective CPU proposal following S19 development, before any confirmation model inference. Root registration, independent source/data review, measured caps and explicit GPU releases remain required. This file does not allocate or launch work.

**Question:** does the fixed scratch typed-record learner replicate its known-motif acquisition advantage on untouched constructions across three independent initializations, compared with both inherited original and contextual workspace learners? Heldout3×4 is a separate structural generalization question. S19's zero heldout result remains immutable; no recipe is adjusted to improve it.

## Fixed arms and data

Use all seeds701/702/703. Within each lineage, original and context restore the same corresponding S12 decay u24576 model **and AdamW**, then run the unchanged S18 mixed-corpus continuation. Scratch record initialization uses the same numeric seed, with the unchanged S19 actor/loss/learning-rate schedule. Numeric seed pairing does not make the different learners' initial states equal. Original/context restore independently trained parents across lineages; seed201's old baseline is never reused as three replications. Keep701 despite its historical competence failure. Exact parent paths, checkpoint/manifest hashes and stages are bound in the companion JSON and previously independently audited S18 confirmation inventory.

All nine arms receive the same mixed4096 TRAIN population, schedule generator15115,4,096updates/batch8,32,768 incremental presentations/eight visits, and expected construction-stream hash. Original/context retain S18's per-presentation negative query seed150150000+n,128 negatives, LR1e-5, restored moments and clipping; pair query streams exactly. Scratch retains AdamW3e-4,128-update warmup, cosine3e-5, eight field means and the160-record public greedy contract. No new recipe, width, loss, threshold algorithm or endpoint choice. Corpus streams are paired, not arithmetic, parameters, inherited history or objective.

The existing untouched confirmation population is2,048 graphs,512 each3×3/3×4/4×3/4×4, SHA2710e5e1b6fffab6fa33d56105de13e49d46d64c63d81f61c6ea331347229d32. Its audited alpha exclusion from TRAIN and DEV is retained. No new generation, subset selection or confirmation inspection precedes freeze. Original/context have196,608 inherited presentations; record has none. Report both total and incremental histories and measured parameter/runtime scopes.

## Final-only inference and calibration

Retain training loss logs and a final model+optimizer+sampler checkpoint. There are no repeated DEV evaluations or best checkpoints. At fixed4,096, evaluate the exact S17 mixed TRAIN128 panel (64/32/32 across3×3/4×3/4×4) and all confirmation2,048 for every arm. For each workspace endpoint, fit precisely the existing threshold algorithm on this actual TRAIN128 once; apply its one global relation-threshold vector unchanged to every confirmation cell. Retain raw and matched-calibrated outcomes and calibration prevalence/absence information. No historical calibration sweep or per-cell threshold choice. Record categorical decoding has no calibration. TRAIN scores are training-exposed; baseline matched TRAIN scores additionally overlap threshold fitting.

All nine endpoints must be completed and audited before aggregate decisions. No early stopping on confirmation, seed removal, adaptive rerun, recipe changes or exposure extension. Preserve failed/timed-out conditions as incomplete and request separately reviewed mechanical remedies without looking for a favorable endpoint.

## Registered decisions and descriptive comparisons

Primary known-motif accuracy is the unweighted macro of fresh3×3,4×3,4×4. A positive replicated acquisition claim requires **every** seed's record arm to achieve at least90% known macro (at least1,383 complete of1,536) and at least80% in each known cell (at least410/512). For every seed it must also exceed **both** original and contextual matched-calibrated macro scores, with a positive lower95% paired event-bootstrap bound against each. This is equivalently superiority to the strongest of those two fixed baselines without discarding either. Retain both raw comparisons and all continuous results if any gate fails. These stricter confirmation competence gates do not retroactively replace S19's development gate.

Bootstrap10,000 stratified draws of512 event IDs with replacement within each known cell, random seed20020; share identical draws across lineages and comparators. Compute per-lineage paired macro deltas and their2.5/97.5 percentiles, and a separately labeled average over the three fixed lineages. This interval reflects event uncertainty conditional on those trained models, not a population-of-initializations confidence claim. Report every lineage and all-three direction agreement; three seeds alone do not warrant a conventional seed-level significance claim. Cell transitions and exact components accompany macro results. Do not choose a comparator or policy based on which makes the difference largest.

Heldout3×4 remains secondary: a positive absolute competence statement requires at least52/512 complete in **all three** record seeds. Report raw/calibrated baseline comparisons and exact outputs even if this fails. This threshold by itself establishes neither broad unseen-shape transfer nor isolated architectural causation. Known-motif and heldout decisions remain separate.

## Profile, audit and compute

Before allocation, mechanically profile all three arms using lineage701,20 updates each, full TRAIN128 and the fixed first16 DEV examples per cell, final evaluation only. No confirmation path is consumed by profile. Reset all main jobs to their original parents or scratch seed; profile endpoints are not warm starts. Keep the full4,096-update LR schedule during the20-update profile. Separate optimizer, TRAIN/calibration, variable evaluation, codec/packing/export and setup/checkpoint timings. Include one separately labeled public160-step batch32 stress for record decoding; actual evaluated outputs stop naturally.

The root planning envelope is at most5,000 seconds for the full nine-arm confirmation plus profiles, with actual caps to follow measured phases. Existing S19 main321.10 seconds included four evaluation curves; original S15 main484.14 and S18 contextual optimizer335.17 also provide conservative context. A coarse three-lineage estimate around3,000–4,500 seconds is plausible, not an allocation or bound. Final-only confirmation avoids unnecessary DEV curves; caps must include whole process occupancy and export. If the meaningful complete condition cannot fit, revise budget or defer before confirmation inference rather than shrink support silently.

Independent preflight must establish parent/model/AdamW identity, seed and stream resets, unchanged learners, fixed actualTRAIN selection, no confirmation leakage into fitting, strict record codec failures, and immutable source/config/launch/cap bindings. Independent final audits reconstruct all scores and calibration policies, verify checkpoint states/exposures, and reproduce gates. No inference is authorized by this prospective document.
