# S18 first reference attempt: verification-order failure and prospective correction

The separately released240-second reference attempt stopped after its first original checkpoint, exit1 at the raw/target replay assertion. Full GNUtime63.92seconds; conservative charge63.93. GPU was free before CPU diagnosis. Full failed logs/receipts/calibration/prediction partials are retained under `research/results/campaign-01/semantics/s18-reference-failed-v1/`; remote `results/s18-reference-v1` is read-only. No second checkpoint or main arm ran.

The new reference evaluator orders DEV by four cells while the original S15 export uses cache order. The verifier incorrectly zipped the two lists. CPU diagnosis proves both contain2048unique semantic identities, exactly the same complete population, different orders, and2048/2048 exact raw graphs and2048/2048 exact targets under semantic-ID alignment. Thus the failure is solely row-order verification, with no observed model or target mismatch. The original failed assertion and cost remain reported rather than converting the failed launch into a complete reference run.

Minimal correction replaces the zip comparison with a bijective semantic-ID population check, rejecting missing/duplicate/extra identities before demanding raw and target equality for every identity. Regression tests cover reorder success and missing/duplicate/raw-mutated/target-mutated failures. All24CPU tests pass1.52seconds. No actor, training operation, evaluation population, calibration rule, threshold, checkpoint, source/target information or main decision gate changes. Main source guard changes because the shared runner contains the corrected reference-only function; main numerical code is unchanged.

At root's instruction, reference-v2 will rerun all three early original checkpoints under the same240-second cap after its separate freeze/review/release. The first checkpoint's2048DEV forwards and128TRAIN calibration are deliberately repeated rather than introducing recovery/assembled-manifest machinery. This duplicate work and failed63.93seconds remain charged; the valid first partial is retained as diagnostic evidence only. Final original checkpoint still reuses S17 and is not forwarded. Main-v2 remains4000seconds, fixed two-arm4096-update protocol; unused v1 freezes remain archived. No automatic rerun or main release is authorized by this correction.
