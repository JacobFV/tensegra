# S22 prospective full diagnostic cost formula

Planning only; profile cap180 is separately frozen. Main has no training/optimizer/calibration/checkpoint export, but loads and rehashes both frozen S21 checkpoints and preserves six complete diagnostic outputs. Total branch envelope is1,200 seconds including actual profile charge. Main allocation follows independent profile/timing review.

For each backbone, measure fixed load/setup, source/data/checkpoint hashing, public target/boundary preparation, and all three policy-specific decode/export/residual timers. Profile is112DEV examples per policy; main3,584, exactly32× population. There are112 full batch32 calls per policy,672 total across six policy/backbone combinations. Scale per-policy decode by32 as a measured natural-termination estimate; additionally budget672× the larger observed74-token/160-step public stress batch time, with an explicit allowance for controller forcing/counter work if this exceeds or is insufficiently represented by ordinary profile timing. The profile's padded final16-example batch makes simple32× scaling conservative relative to exact full-batch counts; state this rather than mislabeling it a measurement.

Use the larger supported decoding estimate; do not choose a policy based on profile efficacy or shorten its160-record cap. The stress helper itself is public-only and ignores termination for timing, while real diagnostic policies stop naturally under their frozen rules. Controller overhead includes forced-kind/value/copy bookkeeping and must not be assumed free. Scale all raw-record/packed-target/export and remaining evaluator CPU work by population and add a transparent payload/variance reserve, particularly if profile outputs are invalid or short. Keep setup/checkpoint loads fixed per backbone, but account for increased DEV target preparation. Hashing/import/idle-query and full outer timeout occupancy belong to the total.

Report the measured subtotal, engineering reserves, proposed hard cap and remaining1,200-second envelope separately. If a complete six-policy/seven-cell main does not fit, report the constraint before launch; no silent cell, checkpoint, policy, or record-limit reduction. Sealed confirmation and model-quality selection are excluded from all cost decisions.
