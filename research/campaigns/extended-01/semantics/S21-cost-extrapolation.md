# S21 prospective full-main cost formula

Use the completed paired profile's timing fields only. No efficacy-based recipe choice, main cap, or launch is authorized here. Development envelope is1,800 seconds including actual profile charge and both complete main arms.

For each arm, extrapolate20-update optimizer time by204.8. Keep actual different broad/original token/record work separate; identical presentation count does not imply equal training cost. Main has four full evaluations versus two profile evaluations. TRAIN teacher-forcing/packing/export sums scale2× (128 each curve). DEV corresponding sums scale64×: four3,584 populations versus two112 populations. These are timing forecasts, not deterministic bounds.

Use separate public worst-case160-step decoding at the longest supported74-token inputs, including the original arm's enlarged shared DEV population. Each main arm has `4 × (128 + 3584) / 32 = 464` full batches; paired main has928. Multiply each arm's464 by its mechanical stress batch timing, allowing an explicit safety factor or the larger mechanically comparable observed stress if needed. Do not extrapolate greedy cost from early short or invalid outputs. Ordinary inference still stops naturally at predicted EOS and never receives gold length/count.

Record teacher forcing separately. Scale export and unclassified evaluation packing/codec residuals with their respective TRAIN/DEV populations, then add an explicit valid-graph payload allowance. Report that allowance as engineering reserve rather than measured main cost. Main saves one initial empty-AdamW checkpoint and three full-state checkpoints per arm: use `C0 + 3*C20`, not an unqualified doubling of the profile checkpoint sum. Setup is once per arm. Preflight includes complete TRAIN encoding plus a larger DEV target allocation in main; reserve additional CPU preparation time rather than assume profile preflight fixed. Retain outer launcher/import/hash/idle-query time and unclassified residual in the whole-process forecast.

Separate scientific-main estimate, fixed/variable measured phases, chosen engineering reserve, proposed hard cap and remaining envelope. Root chooses actual main allocation after profile and independent review. If full4,096updates/allfourcurves/sevenDEV512 cells cannot fit meaningfully, report the constraint and defer or separately revise budget/protocol before main. No silent width, step, population or curve reduction. Neither sealed S21 confirmation nor unfinished S20 confirmation enters timing or recipe selection.
