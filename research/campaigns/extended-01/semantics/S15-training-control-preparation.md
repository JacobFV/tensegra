# S15 v2 training source prepared, not released

`campaign_semantics_shape_train.py` and four prepared configs refuse execution until separately frozen. No S15 model was constructed, loaded, or run. The independent full cache/feature audit and trainer review remain prerequisites; a source-bound freezer/durable wrapper and timing allocation still need preparation before any profile release.

Both arms inherit the exact independently selected S11 model and AdamW state at24576updates/196608presentations; override only LR to1e-5. Unlike S13, this is a new4096-construction corpus, so the old8192-index parent sampler cannot be continued. Reset both arms to the same explicitly seeded schedule15115 over the already identically arranged common/replacement slots. Both have4096updates×8=32768presentations, exactly8visits/construction. Preserve all canonical actor/loss/curriculum/precision contracts. Save checkpoints0/1024/2048/4096 addedupdates.

Each presentation gets its own generator seeded150150000+zero-based presentation number, independent of arm. Different graphs may draw different pairs and consume different numbers of draws, but cannot disturb subsequent common-example sampling. The CPU-only fixture (CUDA hidden) verifies120common presentations have exactly equal pairs, alongside136different-shape presentations; all4096updates yield8visits/graph. Prepared configs reject execution. No model or checkpoint was loaded. The raw result is `s15-shape-cache-v2/pairing-check.json`.

Calibrate only on the original historical English TRAIN128, not new DEV or new shape training examples. Require initial TRAIN predictions and thresholds to replay the fixed parent exactly. Apply each checkpoint's one calibration policy to every development cell. The20-update profile evaluates first16 percell (64total), with full128calibration, using the identical schedule/loss/data/parent path as the main. Profile outputs are mechanical and never main parents. Main evaluates all2048DEV examples each curve; no confirmation inference. Main records full raw/calibrated graphs, calibration tensors, cell outcomes, inherited state/steps, common-pair and construction sequence hashes, node/edge/token exposures and timings. The prepared implementation intentionally does not accept an8192-update extension; eligibility would require a separately reviewed continuation config/source with both4096 endpoints and preserved schedule states, never an untracked config edit.

The separate stdlib freezer/launcher are now prepared as `campaign_semantics_shape_freeze.py` and `campaign_semantics_shape_launch.py`. Freezing checks every prepared source, all cache/audit bytes, parent checkpoint/evaluation and original calibration/vocabulary files, binds helper/launcher hashes and allocated cap, and creates a new file exclusively. Launch requires actual GPU idle, unused prefix/output, starts its durable receipt/whole-process clock before repeating CPU guards, and gives the model child only the remaining allocation. Receipt writes use temp+flush+fsync+replace+directory fsync. No S15 frozen config has been created or GPU release sought. `S15-freeze-check.py` passes immutable creation, cap/source guards and the real wrapper's durable failure path with a fake idle query and nonexistent child; no real GPU query or model child occurs in that test. Cache/trainer/launcher independent review and explicit root cap/release remain required.
