# S20 prospective aggregate analysis

The standalone `S20-analysis.py` aggregate core consumes audited per-event complete-graph Boolean flags. It has no model or confirmation loader and cannot yet run against campaign artifacts. The additive loader will be source/config-pinned after the S20 runner freezes. Current work reads no confirmation contents.

Exactly three seeds701/702/703 and three arms original/context/record are mandatory. Workspace raw and matched policies are both required; record has its fixed categorical policy. All four512-event cells must have identical semantic IDs across every seed/arm/policy. **Sort semantic_sha256 lexicographically within each cell before array construction.** Cells must be disjoint, totalling2,048. Pair identical sorted indices for every comparison.

Generate10,000 bootstrap draws per known cell in fixed3×3,4×3,4×4 order with NumPy default_rng(20020). Reuse those identical draw arrays across all lineages, comparators and policies. Average the three cell accuracies equally. Preserve each lineage's paired delta,95% percentile interval, and all per-cell2×2 transition counts. Separately show the interval for the average of the three fixed lineages using the same draws. These are event-conditional intervals, not seed-population confidence intervals.

Integer count gates are exactly1,383/1,536 known macro and410/512 each known cell, required for every record seed. Each seed must have positive known macro difference and positive lower95% bound against **both** matched workspace baselines. Heldout competence independently requires52/512 in every seed. Raw comparisons, all continuous outcomes and failing seeds remain visible; no automatic extension.

The future loader must validate the ordered nine-arm top manifest/config; each seed's independent frozen parent hash/model+AdamW or scratch initialization;4,096updates/eight visits/32,768 exposures and exact construction stream; raw/matched TRAIN128 global calibration identities and NPZ bindings; immutable final artifact/receipt/source/state inventory; and identical targets and event identities across arms. Record inputs retain raw/canonical generated records and strict independent codec replay. TRAIN summaries and per-field training loss curves remain separate from fresh confirmation.

Report schema: source/config/receipt/artifact inventory; history/parameter/exposure/timing scopes; per-seed/arm TRAIN fit and training loss logs; per-cell raw/matched/categorical valid/exact/component counts; all paired transitions and conditional intervals; fixed known and heldout decisions. No per-cell threshold fitting, checkpoint choice, seed filtering or absent-arm aggregate is allowed. This is fresh confirmation of a learner comparison, not an isolated architectural intervention.

Six synthetic tests currently check exact integer boundaries, all-seed/both-baseline conjunction, interval-zero rejection, paired transitions, constant-delta bootstrap, ID/support/type guards. They read no real outputs. The runner worker's planned schema is ordered top-level results with per-arm `seed-{seed}/{arm}/manifest.json.gz`; workspace retains S18 matched final schema and record retains S19 final TRAIN/confirmation schema. Loader implementation awaits final source and freeze pins.
