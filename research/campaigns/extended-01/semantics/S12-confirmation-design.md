# S12: paired confirmation of a single late learning-rate reduction

Freeze before any reserved-confirmation model forward. S11's fixed development endpoint justifies testing its LR intervention across independent initializations. S09 remains unrun; this comparison changes no architecture or representation.

Three independent parents use seeds701/702/703 (no existing campaign config uses these seeds), original width1,024,8 workspace rows and original objectives. Each trains16,384 updates×8=131,072 presentations from random initialization on the same8,192 TRAIN graphs. Fork each immutable parent into constant1e-4 and reduced1e-5 LR continuations to24,576 updates=196,608 presentations. Both forks restore exactly the same model, AdamW moments/steps, sampler generators, order/position and visits. Neither fork is initialized from another seed's parent. Only restored group LR differs; its effect on AdamW decay is included. No early selection, other LR, extra architecture, occurrence target, or exposure extension.

Parent evaluation uses the already-inspected512 development examples only to verify outputs and retain learning diagnostics, never to select a parent/checkpoint/recipe. Fork initial outputs must exactly replay each parent's TRAIN128/DEV512 outputs; first sampled indices/pairs must agree between forks. The fixed branch endpoint evaluates all1,024 reserved constructions. There is no confirmation-derived vocabulary, calibration or checkpoint selection. The first128 TRAIN examples fit the unchanged per-relation threshold rule separately per final model, with raw decoding retained. Prediction-presence masks are learned; no gold node/edge inference input. All final outcomes remain archived even if a seed fails.

Frozen caches: TRAIN SHA9c86e311b7f58587918cb314ae9ba7fbc749c062c71829023135c86e568c0710; DEV SHA c15bb600e5e02f8e4fd9e4e9a86ca3f1fe8328d071535c817f733a8caa9f8b62; reserved confirmation SHA fc85da89fe4e1a522d1e0256fc1e58f8afb279494cb1af7ee4ac7235403a43f8. All splits are alpha-disjoint by the existing independent dataset audit. These are new equality/reference patterns within two known ordered tree motifs at depth3 and known English spelling/renderer, not new tree-shape/language generalization.

Primary outcome: learned graph exact canonical recovery with TRAIN-calibrated decoding. Report every seed/arm and paired LR differences, including counts of both-correct, constant-only, decay-only, neither. Use paired example bootstrap95% intervals within each seed (10,000 resamples, fixed CPU seed12012); report the three seed differences individually, their mean and range. Example-level intervals do not establish seed-population certainty. Raw complete/typed/ordered/copy metrics remain parallel, never selected after outcomes. Competence is at least103/1,024 complete graphs in every decayed-LR seed. Replicated directional advantage requires a strictly positive paired complete-graph difference in every seed AND a positive lower bound of the95% shared-event bootstrap interval for the mean paired gain. Resample the same test-graph indices jointly across the three fixed seeds (10,000 resamples, seed12012), preserving cross-seed event correlation; this is conditional on these seeds, not a seed-population confidence interval. Report each seed interval regardless of significance. If any difference is zero/negative, do not claim a replicated advantage. This comparison does not require a forced winner. Count all failures. A positive effect in one seed is insufficient for replicated advantage.

The unchanged full-TRAIN frequency baseline is evaluated on the same confirmation support. The already-frozen S10 public-schema/reference rule is a separate engineered-decoder secondary, applied identically to raw/calibrated outputs of both arms without fitting any confirmation policy. It is supplied bookkeeping plus learned attributes/remaining relations, not fully learned graph recovery. No composition or broad language claim follows.

Budget: each parent approximately1,200–1,350 full-process seconds; each continuation approximately650–700 including1,024 final examples. Total approximately7,500–8,250 seconds. Per-parent external cap1,400; per-fork800; sum hard maxima9,000 seconds requires coordinator allocation; expected occupancy is lower. Root authorizes serial launches only, not a blanket reservation. Parent source/data/config freeze precedes training; immutable checkpoint hashes are recorded immediately after each parent and verified before forks. Same checkpoint values are used by both forks. Global CPU/CUDA RNG is not archived; dropout is zero and sampling is controlled by the two saved explicit generators.

Optional lexical rename feasibility is CPU-only at this stage: consistently replace visible identifier spellings and corresponding copy targets while preserving public tokenization/copyability and semantic equivalence. This would change lexical exposure and requires a separately frozen diagnostic partition/policy before prediction. It is not included silently in this confirmation or used for selection. No new renderer training or corpus extension is authorized here.

Implementation: parent jobs dispatch to the unchanged S01 trainer and never load the reserved cache. A versioned paired-fork runner changes only the LR override and final evaluation set; its initial evaluation remains the DEV replay. Parent byte hashes are bound mechanically after training by a utility that does not load prediction correctness. Templates and all hyperparameters are frozen beforehand. Two CPU tests verify prescribed seed/budget constraints and identical hash-binding for both arms, including rejection of corrupt parent artifacts. Source freeze covers the dispatcher, fork runner, wrapper and unchanged dependencies. A confirmation checkpoint cannot be selected from a curve because only the fixed final fork endpoint evaluates the reserved set.
