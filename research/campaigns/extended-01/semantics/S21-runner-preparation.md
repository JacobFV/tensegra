# S21 additive runner preparation

CPU-only draft; no GPU or sealed confirmation access. `campaign_semantics_s21.py` reuses the unchanged S19 actor, field loss, learning-rate schedule, evaluator, codec and forced160-step public timing helper. Both arms reset scratch seed2101 and schedule15115. Original and broad train4096 constructions for4096updates/batch8, with curves0/1024/2048/4096 and TRAIN128 plus all seven DEV512 cells. Exact initial-state equality is checked. No old source file is edited.

The data worker owns generation/audit. Runner expects existing S15 original TRAIN bytes, new `data/s21-six-motif-v1/train_broad.jsonl.gz`, shared development, panels.json, audit.json and the existing vocabulary audit. `panels.json` preserves the exact S17 original selection and deterministic broad selection, with index/seed/semantic/alpha identities. Broad epoch totals are independently compared with builder audit summaries. Both arm totals are recomputed from actual public tokens/codec records and frozen into prepared configs, then multiplied by eight and checked against actual update exposures. Construction-index stream retains the fixed15115 hash; different corpus content and work are reported explicitly.

Prepared files deliberately contain unbound data hashes/exposures, and `DATA_BINDINGS=None` blocks any freeze/launch until the independently audited cache is available. No confirmation input key is accepted. The runner neither loads nor hashes the sealed confirmation cache. The audited data manifest may describe its existence without opening it for model work.

Profile proposal is paired20updates, curves0/20, fullTRAIN128 and first16DEV/cell (112 total), plus separate160-step batch32 stress for each arm using the longest visible public inputs in its TRAIN/profile-DEV pool. Ordinary evaluation stops naturally. Main caps remain unset; the branch planning envelope is1800 seconds including profile. Full seven-cell evaluation cost, public lengths and exports must be measured before main allocation.

All generated records, canonical predictions, targets, invalid reasons and eight field diagnostics are preserved by the unchanged evaluator. Training logs preserve each active field numerator/denominator. Full model+AdamW+sampler checkpoints are written at every curve. The additive launcher retains durable started/final receipts, source/config guards and own-source PYTHONPATH; outer timeout/time binding remains an operator responsibility after allocation.

Six stdlib source/config guards pass without importing torch. Two prospective development gate fixtures enforce heldout52/+26, each newly trained cell256, and exact old-known loss<=76; loss77 fails. Compilation passes. Actual-cache preparation and remote CPU actor/integration tests remain pending audited data; unchanged S19 actor/loss tests and prior audits do not substitute for those new boundary checks. S20 closed-matrix analysis retains priority.

## Exact cache bindings prepared

Cache b338db54/root5d5b8593 now supplies exact TRAIN/DEV/panel/audit hashes in both prepared configs and the additive freezer. Independent population/provenance review is still pending; no frozen launch config or GPU allocation exists. The preparation placeholders described above have been replaced, while prepared status and null caps remain unlaunchable.

CPU data preflight (`S21-runner-data-preflight.json`) passed in4.6995 seconds with CUDA hidden and the actor constructor replaced by a raising sentinel. It verified all six input hashes, both4096-construction corpora, exact128 panels/proportions,112 profile DEV examples across seven cells, codec records and all actual epoch exposure totals, and independently replayed both full eight-visit15115 construction schedules. No confirmation cache was opened. A first temporary CPU staging attempt lacked the unchanged vendor package; copying that package into the temporary source tree fixed import resolution without altering scientific source or data.

Broad eight-epoch exposure is1,835,008 public tokens,1,074,344 nodes,2,444,456 edges and3,551,568 records; original remains1,720,320/988,008/2,208,616/3,229,392. Broad maximum TRAIN public length is74 tokens and maximum target length143 records, versus64/123 original. Profiling must therefore cover74-token inputs even for the original arm's shared enlarged DEV set. The stress pool includes profile DEV public inputs as well as each arm's TRAIN. Actual differential CPU model/loop tests remain required before final clearance; this data preflight deliberately made no model call.

## Review and CPU differential mechanics completed

Independent exhaustive population/features audit ef7b825e/rootdf5d1340 and provenance/chronology audit9565aadf/rootf1db2a25 cleared the exact caches; roote5aeff61 declared data verified. The remaining preflight is runner/freeze/launch review, not data availability.

Two remote CPU differential tests passed in1.283 seconds (CUDA hidden, two threads, width16 explicitly for mechanics). The actual new `train_arm` loop's one update produces bit-identical model and every AdamW state tensor to a directly executed unchanged S19 teacher-forced loss/update, with identical LR, gradient clipping, seed reset, saved sampler/order/position/visits, exposure counters and checkpoint cadence. Repeating as the broad arm on the same synthetic fixture gives identical initial/final states. A second test calls the unchanged actual evaluator with a public-only greedy spy, verifies only ActorInput reaches greedy inference, and checks invalid output yields no complete/valid credit. This supplements the earlier full-width actor audits; it is not a new efficacy experiment.

The initial one-update test passed in1.010 seconds before extending coverage to paired resets and the inference boundary. The temporary missing-vendor attempt failed before any actor construction during import resolution; its exact elapsed time was not separately captured, so it is disclosed rather than assigned an invented duration. All tests and cache preflights are CPU-only. Main remains unallocated; the proposed profile cap is180 seconds pending root's freeze/release process.
