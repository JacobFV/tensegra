# Prospective S18 paired analysis

`S18-analysis.py` is a CPU-only helper for runner schema c8c20c0c; launcher isolation35e94fce does not alter this schema. It imports no model, never fits thresholds, and does not inspect profile or main outcomes during preparation. Only new S18-analysis files are owned here; runner/config/actor ownership stays with the composition worker.

Inputs are explicit local main/reference archive directories, frozen main/reference config files and occupancy receipts, bindings JSON, corresponding source directory, historical archive root and output filename. CLI flags: `--main --reference --main-config --reference-config --main-receipt --reference-receipt --bindings --source --archive-root --output`. This avoids silently treating remote absolute paths as locally available. Bindings are pinned to4364e758, all baseline S15/S17 input hashes are verified, source/config/receipt and full event/curve/policy coverage are guarded. Frozen numeric caps remain runner-owner inputs; they do not change analysis formulas. Remote saved checkpoint/AdamW bytes require independent state audit; this helper checks their immutable bindings and manifests without deserializing model tensors.

Original baseline: all raw/historical curves from S15; first three matched curves from the reference job; matched4096 from S17. Context and10pass use all four new matched/historical curves. Each curve must contain2048distinct events,512in every cell. Every policy/arm/curve must agree on targets; the same arm/checkpoint must replay raw predictions and raw metrics across policies. TRAIN selection must match S17's exact128indices/seeds. Both new training streams must match original order/query/exposures and4096eight-visit counts; initial tensor hashes and inherited AdamW steps must match.

The output keeps raw, historical and matched outcomes separate, complete/component counts, macro graph metrics, typed/ordered micro-counts/F1, role FP/FN/slot errors, learning curves, contextual-minus-each-control transitions and signed component/error changes. TRAIN summaries split3x3/4x3/4x4 for matched128; historical128 is separately labeled. All are calibration-overlap fit panels, not independent generalization.

Only4096matched DEV decides the fixed52/26 acquisition,103/-51 retention and separately52/26 recombination rules, against both original and10pass. No intermediate checkpoint or raw/historical metric substitutes. No automatic extension or confirmation. Event-bootstrap95% intervals use10,000draws with RNG18018, shared event draws across policies and comparators within each cell. These descriptive, unadjusted intervals condition on one inspected training lineage; they are not seed uncertainty or independent discovery.

Six synthetic tests passed in0.012seconds: exact gate edges for both controls, missing arms/cells, missing/duplicate/foreign events, stream length/count/hash/optimizer-step guards, paired transitions/shared bootstrap, and presence masking/edge-slot conjunction. No campaign results are opened by the tests. Main execution and full numerical audit remain pending actual completed artifacts.
