# Conditional S09 reassessment after S15

CPU planning only; S13 and S15 remain the current acquisition comparisons. S09 is profiled and frozen but its main has never run. If S15 learns its new training motif yet fails the held-out combination, S09 offers a specific alternative information path, not evidence that another architecture will solve recombination.

S06 replaced the original eight-row workspace with token states; at65536 presentations it achieved zero complete DEV graphs and lower component scores than the original actor. S07 retained that actor but changed identifier copy supervision to occurrence targets; it learned occurrence positions (S08:88.94%) without sufficient ordered-edge acquisition, again zero complete graphs. S09 instead retains the workspace and canonical queries, builds a parallel contextual token stream with shared existing blocks, and uses the model's own copy softmax to read contextual tokens into each node with fixed residual scale0.5. Graph-head losses can therefore shape the same pointer used for copying. First-identity copy supervision stays unchanged. These are distinct interventions; S06/S07 failure does not constitute an S09 result.

The rename result exposes a relevant limitation: S09 still consumes the same lexical hash features and predicts the same first-identity pointer. Context may help structural disambiguation, but neither the read nor weight sharing guarantees invariance to unseen lexical codes. A badly displaced pointer also retrieves the wrong context. Thus any later claim about lexical robustness requires the same fixed renamed diagnostic on an independently frozen endpoint; do not add spelling augmentation or change copy supervision inside an S09 comparison without a separate version and matched control.

Existing measured20-update profile:2.0033 optimizer seconds,11.3356 full seconds,1.279GB peak allocated CUDA memory; unchanged57,853,781 active parameters. The old8192-update main forecast is roughly821 optimizer seconds plus170–250 evaluation/serialization seconds, cap1500 proposed in its design. Parameter equality is not compute equality: parallel contextual processing raises estimated backbone/read arithmetic about4–5×. Profile timing from the old fixed-shape corpus cannot directly budget a new S15 dataset. A promising contextual path also requires the predeclared extra-workspace-pass compute control before an attribution/confirmation claim.

Do not silently execute the old S09 config as an S15 follow-up: it starts from scratch atLR1e-4 on8192old-shape graphs, whereas S15 continues the independently selected S11parent atLR1e-5. A future comparison must prospectively choose either the original bounded S09 question against its historical matched control, or a separately versioned matched S11-parent continuation on identical S15 data/exposure with context_read off versus on. The latter more directly addresses S15's residual failure but changes the old registered question and needs new source/protocol audit, paired mechanical profiling, allocation, and a frozen development/confirmation rule. No tiny memorization rerun is needed; inspect training-panel acquisition during real-diversity training, reserve confirmation budget, and preserve S14/S15 heldout definitions.
