# S19 runner and lifecycle preparation

CPU-only implementation of root S19 design234fb98d/eb7cc4ae with corpus schedule15115 and actor output-domain correction e3d8a1be. Main/profile prepared configs are unlaunchable until separate source/cap freeze, independent review and root GPU release. No model acquisition, profile, confirmation or new data selection has run.

The runner starts seed1901 from scratch with62,677,315parameters,2encoder/2decoder layers,width1024,8heads,FFN4096,dropout0. Train batch8; AdamW(.9,.999),eps1e-8,weight decay.01,gradient cap1. Update1 LR3e-4/128, update128 LR3e-4, cosine reaches3e-5at4096. Training averages eight CE field means over their applicable records across each batch, with differentiable zero for absent families and fixed denominator8. NODE value/copy targets are direct indices; slotNONE remains class0 via slot+1. No nonexistent required-field missing class is learned. Per-field loss sums/support counts are logged.

The exact original mixed4096construction sequence is retained: torch generator15115 and inherited next_indices. Main requires eight visits/32768presentations, sequence85054bf25e3a3e2a5a9a932b441d6413fd0d7ad827df79e1583268aaddf5acd9,1720320public tokens,988008nodes,2208616edges and3229392target records includingEOS. There are no sampled negative queries in S19, so query-stream equality is not claimed. Encoder/decoder-token computation, parameters and lack of inherited196608presentations differ from S18.

Evaluation batch32, fixed checkpoints0/1024/2048/4096. TRAIN is the exact preselected S17mixed128(64/32/32cells); DEV is512in each of fourcells. No calibration or confirmation input is loaded. Teacher-forced field losses/accuracy are reported separately from public-only greedy records. Greedy receives only ActorInput text/copy inventory, stops naturally onEOS/invalid/overflow, and never receives gold record lengths/counts/phases. Full raw and public-copy-canonicalized records are retained. Codec errors—including target-adapter unsupported pair-slot conflicts—force exact failure with no partial graph credit. Distinct edge emission permutations are semantically allowed; sortedness is a separate diagnostic. Node order is not gold-realigned.

Profile recipe:20updates, evaluations0/20 on fullTRAIN128 and16DEV/cell. In addition, one32-example stress selects the first32longest public TRAIN token sequences (ties original index), runs exactly160cached own-prediction steps, and exports the generated stress records. It mirrors optimized cached decode and one stacked argmax host transfer per step, but deliberately ignoresEOS/invalid stopping for timing only; no stress predictions are quality results. Actual evaluations still stop naturally. Teacher-forced, free-running, export, setup/preflight, optimizer and checkpoint-write phases are separate. Stress timing alone is not a full-main cost bound; failed/short initial predictions cannot justify extrapolating natural-stop latency to acquired long outputs.

Prepared outputs: `results/s19-profile-v1`, `results/s19-main-v1`. CLI child: `python -m topoformer.campaign_semantics_s19 CONFIG`. Freezer CLI: `python src/topoformer/campaign_semantics_s19_freeze.py PREPARED FROZEN --source src/topoformer --cap CAP`. Wrapper CLI: `python src/topoformer/campaign_semantics_s19_launch.py FROZEN --cap CAP --python PYTHON --prefix PREFIX`. Wrapper resolves its own isolated source tree, includes idle/source/data checks in its internal cap, atomically fsyncs receipts, rejects used output/prefix, and never retries. Outer GNUtime and GNUtimeout must additionally cover imports and entire process-group lifetime. Profile180seconds remains proposed; main cap is unset.

Validation:36CPU tests plus14subtests passed in2.73seconds; one optional full-cache codec test skipped because its independent prior audit already passed. Runner fixtures verify exact field support/direct target offsets/fixed divisor, padding/absent-family gradients, warmup/cosine boundaries, exact historical construction stream, public-only free-running call boundary, invalid-output failures,160stepstress/model immutability, recipe/source/input/cap/immutable-output guards and durable synthetic failure/timeout receipts. Actual data preflight passed1.785seconds with actor construction replaced by a raising sentinel:0models/forwards,4096TRAIN records82–123,128exactpanel,64profileDEV and exact eight-epoch exposure totals. Gold dense tensors are built only for evaluated TRAIN panel/DEV, not all training examples. Source hashes are in `S19-runner-cpu-preflight.json`; independent runner review remains pending.
