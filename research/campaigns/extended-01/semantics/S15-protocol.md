# S15 v1: controlled shape exposure and held-out combination

CPU preparation authorized after S14, before fixed S13 outcomes. This is an S14-informed development hypothesis, not an uninspected shape discovery. No GPU allocation exists. Parent is the fixed S11 development seed201 endpoint (196608 presentations), selected independently before S12, checkpoint3799ade595500a083b9a558b6a5bc97c5cbc1b8bdce6e7e4b86d97b937ea373e; inherit model and AdamW. Actor and English n-ary renderer unchanged; proposed LR1e-5, batch8,4096updates, with final recipe/source frozen and independently reviewed before any profile/main.

The bounded support audit ran 10000 seeds per global arity after alpha-equivalence exclusion of historical/S13/S14 constructions. It found 3385 distinct arity3/three-fact cases,4902 arity3/four-fact,4350 arity4/three-fact,4980 arity4/four-fact. This establishes availability, not finite-space exhaustion. It does not support assuming4096 fresh arity3/three-fact training examples plus heldouts. Freeze the smaller plan:4096 unique constructions per arm.

Reserve fresh DEV256 and confirmation512 per each of four arity×fact-count cells BEFORE final TRAIN selection, with namespaces9161/9162 and9171/9172 respectively. Exclude alpha-equivalent constructions across every split and all prior corpora, including all inspected S14. Then select TRAIN from audited9151/9152 candidates: common2048arity4 (1024each fact count); control adds2048arity4 (1024each fact count); diversity adds2048arity3/three-fact. Shuffle common and replacement slots once with RNG15015, sharing common-example order and total exposures across arms. Both arms have4096unique constructions; their ordered training motif counts are2 and3. Held-out arity3/four-fact is one untrained combination, although both factors occur in diversity training. Depth remains3 everywhere. Training arity3/three-fact is acquisition, never described as transfer.

Selection is deterministic first qualifying seed within the preregistered namespaces; no model outcomes affect selection. Each DEV/confirmation arity namespace has a10000-attempt bound; TRAIN uses existing10000-per-arity audit candidates. If any quota is not reached, report actual saturation and stop before freezing caches. A separately recorded CPU-only extension to the next disjoint10000-seed block may be considered; no silent duplication, generator change, or outcome-based selection. If support still cannot fill quotas, revise matched counts prospectively and rerun independent audit. No confirmation-model inspection until a separately frozen confirmation release.

Public inputs contain only English text-derived features. Preserve original value vocabulary, visible identity/copy inventory, ordered roles/slots, and capacity128; audit full labels, lexical support, equality patterns and text→target consistency. Token/node/edge exposures differ with shape and must be reported even when construction counts and presentations match. All lexical names and rendering syntax are inherited; novel lexical codes are not part of this intervention. Independent audits must check exact FP32/BF16 feature and target packing, not just graph hashes.

Keep original English TRAIN128 calibration and fixed policy; no per-cell calibration. Evaluate known-arity retention, trained-new-shape acquisition, and held-out-combination separately. Profile both arms at the same20 mechanical updates and same fixed evaluation plan, with timing-only budget decisions. Source must isolate negative-sampling RNG per presentation (or explicitly disclose the pairing limitation) because differing graph sizes consume different draw counts; unlike graphs cannot share identical negative pairs. No architectural changes. Main curve schedule and any development extension/stopping rule must be frozen before profile/main; initial intended comparison4096updates cannot be extended selectively by arm. The fixed S13 pair retains priority and is unaffected.
