# S10 frozen grammar decoder on S12 confirmation

Prepared before any S12 final confirmation outcome. This secondary CPU analysis reuses the **exact decoder from `2085053`**, SHA256 `0db15a241b6ef591b5e5f0d97651f09415463349e6b88157f34776adbbb0a7c7`. The four fixed policies are baseline, schema, bookkeeping, combined. No new rule, threshold, type restriction, reference tie break, schema ambiguity behavior, or checkpoint choice is permitted.

Six fixed endpoints: campaign S12 initialization seeds701/702/703, constant/decay continuation, u24576 (196,608 presentations). Analyze their TRAIN128 and reserved confirmation1024 archived predictions after producers finish. Actor primary learned metrics remain separate and must reproduce exactly under the baseline policy before secondary reporting. Verify endpoint update, declared reserved split, semantic identities, target tensors, and same shared-event order across all six archives. Training/calibration data remain unchanged; no reserved data is read during this preparation.

Reuse the immutable full-TRAIN frequency prediction SHA256 `600bfeeaf6d4f11a9129a7f6cf96b94ee8a2966e4395a99a8617c757bdad845e`, with the same four policies. It consumes no current-event text, length, or copy inventory. Its fixed canonical coordinate/copy-index modes and public compiler schema remain supplied. Use one completed endpoint's target archive only for scoring; the frequency predictor and decoder never receive those targets. Frequency raw/calibrated labels are identical aliases, not two fitted policies.

For every endpoint/policy report exact canonical accuracy, micro typed/ordered F1, primary-to-secondary paired repairs/regressions, active-edge errors introduced/removed, scope abstentions, reference prerequisites, and exact edge/slot output hashes. Raw tensor change/conflict counts may include padded nodes and must remain separately labeled. No supplied-rule result replaces the primary learned gate.

Across the three seed pairs, report all constant→decay four-cell outcomes for each raw/calibrated policy and all four decoder variants. Use the S12-prespecified shared-event bootstrap:10,000 resamples, CPU seed12012, identical event index draw across all three seeds. Report individual seed differences/intervals and their mean with a shared-event interval. This measures event uncertainty conditional on these three fixed seeds, not seed-population uncertainty. Do not select a secondary policy or claim a new secondary promotion gate after outcomes. Keep primary S12 competence and advantage gates entirely separate.

Execution: CPU-only, two threads, CUDA hidden, at most300seconds per endpoint/frequency invocation including export. Final summary has a separate300second CPU cap. Retain archive/source/config hashes and process wall/RSS receipts. Independent reconstruction required. No model inference or reserved target inspection is needed now. Producer archives must exist and be immutable before use; missing endpoints leave summary blocked, never silently omitted.
