# S21 prospective complete-pair analysis

`S21-analysis.py` is pinned to final main config5a13908ec22e9ad47350d7d85433b46291dada4552a474300fcef57871f30db3 and the immutable helpers. It rejects anything except the complete closed original/broad pair, all four curves, full TRAIN panels and7×512 DEV, exact data/source/receipt hashes, paired scratch initialization, and eight-visit corpus-specific exposures. No sealed confirmation path is accepted or opened.

Each artifact's example/cell/valid/complete/invalid-reason/teacher-forced summaries are linked to its manifest entry. Every row's semantic ID, seed, graph digest and cell must match its actual public cache row, with strict Boolean validity flags and reason consistency. Targets must agree across curves and shared events. Packed predicted components are reconstructed; malformed outputs receive zero credit. Strict raw-record codec replay and checkpoint model/AdamW validation remain the independent reviewer's responsibility. Raw/canonical generated records stay in their immutable artifacts rather than being duplicated.

The output retains every curve, per-cell valid/complete/component counts and failures, all eight teacher-forced metrics, field-loss training curves, exact construction/token/node/edge/record exposure, checkpoint/state inventory, timings and paired complete-graph/component differences. Baseline and broad TRAIN128 panels differ by declared selection, and are separately labeled in-sample diagnostics.

Fixed gates call the prospectively frozen `S21-decisions.py`: final heldout3×4 broad≥52 and broad-minus-original≥26; old-known complete-count loss≤76 summed across3×3/4×3/4×4; broad≥256 in each newly trained2×4/5×3/5×4. All conjunction components and counts remain reported on failure. No best checkpoint, calibration, altered thresholds or automatic extension.

Descriptive uncertainty is predeclared before main outcome access: NumPy default_rng21021,10,000 paired event resamples within each512-example cell, cells in2×4/3×3/3×4/4×3/4×4/5×3/5×4 order, semantic IDs lexicographically sorted before draws. Report final broad-minus-original percentage-point percentile95% intervals; draw arrays are independent across cells. These are conditional on the single paired initialization and inspected development set, not seed-level or fresh-confirmation uncertainty. No interval controls the fixed count gates.

Five synthetic loader guard tests and two frozen gate tests pass. They check exact exposure/state/curve completeness, cache identity mutations, strict validity semantics, helper hashes, heldout52/+26, new-cell256, and retention loss76-versus77. They read no main outcomes. Execution is authorized only when both main arms and whole-process receipt are closed. Localization remains separately owned and cannot change these gates.
