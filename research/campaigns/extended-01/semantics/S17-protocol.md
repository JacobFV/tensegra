# S17: frozen endpoint calibration-population diagnostic

S15-informed exploratory follow-up, authorized for CPU preparation only. S15 primary raw/historical-TRAIN results and failed criteria remain immutable. No architecture, checkpoint, model parameter, training exposure, public input, target, or threshold algorithm changes. This is a decoder calibration diagnostic, not retrospective S15 promotion or newly learned graph construction.

Use both fixed S15 final4096-update endpoints. For each arm, select128examples deterministically from its own actual4096-row TRAIN cache, before any new model scores. Control: first64arity4/three-fact and first64arity4/four-fact rows. Mixed: first64arity3/three-fact, first32arity4/three-fact and first32arity4/four-fact rows. Concatenate the selected rows in increasing original TRAIN index order. Freeze indices, seeds, semantic/alpha hashes and cache bytes; no replacement, outcome filtering, DEV/confirmation examples, or per-cell policy. All selected graphs were presented eight times during the corresponding training run.

Reuse the exact inherited `train_relation_thresholds`: one scalar threshold per relation minimizing unweighted errors over predicted-present TRAIN pairs, lowest-threshold tie break; no-positive relations select the largest observed score. Keep the128count, presence masking and all inference/decoding details unchanged. Fit once per frozen endpoint on its selected TRAIN population and apply that one policy unchanged to all2048existing DEV examples (512in each of fourcells). Threshold differences between endpoints are fitted on their respective TRAIN mixtures, not selected by DEV outcomes. Preserve raw outputs and original historical-TRAIN thresholds/outputs as explicit references; assert raw and target replay exactly. Save calibration scores, labels, active pairs, population prevalence/absent relations and new thresholds for independent recomputation.

Report every cell's raw, historical-policy and matched-TRAIN-policy complete graphs and components, paired transitions, and relation-level signed changes in false positives/negatives. Report gold calibration prevalence separately from predicted-present calibration support. Explain no-positive role behavior without imposing a supplied schema mask or unseen-role prohibition. Do not search threshold grids on DEV, fit one policy percell, choose a winning checkpoint, or select between multiple candidate populations. No confirmation inference and no new pass/fail gate; all results are descriptive exploratory evidence. A successful diagnostic would motivate a separately frozen prospective calibration policy, never overwrite S15.

Profile both endpoints serially in one separately released90-second whole-process job: full128calibration each and first16DEV examples percell (64each), with raw replay and all main guards. These are mechanical checks/costs only; no population/recipe selection. Propose main cap only after measured pair. Main evaluates all2048DEV per endpoint and all outputs retained. Profile calibration checkpoints are the same immutable S15 endpoints; no optimizer or training is run. Source, helper, configs, selection, all source/cache/checkpoint/evaluation hashes, durable receipts and explicit root GPU release must precede any profile/model call. No main or successor is automatically authorized.
