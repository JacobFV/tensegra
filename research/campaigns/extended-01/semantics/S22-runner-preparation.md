# S22 frozen NODE-boundary diagnostic preparation

The additive runner uses both final4,096 S21 checkpoint/model-state hashes, unchanged public DEV inputs and the strict S19 codec/evaluator. Both public-only reference artifacts are separately SHA-bound. No optimizer is constructed, no calibration fitted, no training performed and no confirmation input is accepted. Every decode must preserve the frozen model state; checkpoint bytes are rehashed afterward.

Controller sourcebf0b400c is separately owned/reviewed. The runner passes only ActorInput plus the exact frozen dataclass appropriate to each policy: NodeCount, NodeKinds or NodePrefix containing NODE records only. Evaluator graph targets and old public outputs are held separately and never become controller arguments. Metadata construction uses audited canonical serialization, extracts the leading NODE prefix and discards all edge records before creating a controller boundary. That is privileged gold node information, explicitly disclosed, not learned input.

All three policies run on both backbones. Main retains all seven DEV512 cells; proposed profile uses first16/cell (112) per policy/backbone and one separate longest-public160-step batch32 stress per backbone. The stress reuses the audited public-only S19 timing helper; it does not add a failure-ignoring scientific controller mode. Controller forcing/counting overhead, packing/export and fixed hashes/setup must remain in the measured cost projection. Main has six×112 full inference batches (672 total), plus policy-specific forcing and all raw exports. Profile/main caps are unset in prepared configs; suggest180 seconds for profile review/allocation, main cap only after measured timings within the1,200-second branch envelope.

Artifacts preserve raw/canonical records, packed predictions and targets, all historical strict components/validity reasons, exact nodes/relations, public-reference outcomes and per-event supplied/forced/replacement counts. `exact_nodes` and `exact_relations` follow strict complete-valid-output scoring: invalid output receives no partial credit. In full-prefix policy the node records are supplied; reported edge recovery is conditional on that privilege. No acquisition gate or historical promotion criterion is added.

CPU validation: six stdlib config/source/input/endpoint guard tests pass. Fifteen controller+runner CPU tests pass0.94 seconds, including unchanged greedy no-op parity, forcing/channel/mask direction, sequential prefix cache semantics, narrow type/public boundary, preserved model state and invalid-output zero credit. The three runner fixtures use width16 synthetic records explicitly for mechanics; they are not efficacy evaluations. Actual-cache CPU preparation passed5.837 seconds with actor construction replaced by a raising sentinel, all endpoint/reference/cache hashes checked,3,584DEV rows and10,752 policy boundaries prepared, max42nodes/74publictokens. No controller decode/model forward or sealed confirmation read occurred in that data preflight.

The immutable source inventory includes unchanged S19 helpers, the controller and additive runner/freezer/launcher. Whole-process launch must bind GNUtime outside GNUtimeout KILL outside the own-source wrapper, with durable started/final receipts. Prepared files cannot launch. Root retains sole GPU scheduling authority; independent review and explicit allocation/release remain required.
