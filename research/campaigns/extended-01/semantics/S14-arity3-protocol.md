# S14 frozen CPU population: unseen arity3 motifs

Before any model forward, construct up to1,024 unique alpha-normalized unification graphs with the unchanged pinned generator/renderer/compiler, English only, difficulty1/3 (arity3), seeds914100001 upward, first10,000attempts maximum. Stop at1,024accepted or exhaustion of that bounded attempt budget. Report the actual unique support, duplicates and attempt curve; do not infer finite-space exhaustion merely from a bounded sample. If fewer512unique, explicitly call this a restricted available population. Any further CPU attempt tranche requires a recorded coordinator decision. No generated example enters training or calibration.

Compare every fixed S12seed701/702/703 × constant/decay endpoint on exactly this same population, after the full S12matrix and a separate GPUrelease. Primary diagnostic decoding reuses each endpoint's original English TRAIN128 thresholds unchanged; raw outcomes remain parallel. Report every seed/arm, complete-graph/copy/typed/ordered metrics, motif and graph-size strata, and paired LR differences. No model, threshold, subset or recipe is selected from these outcomes. S12competence and replicated-advantage gates remain exclusively on their original confirmation population; this separate structural diagnostic has no substituted success gate.

Graph equality/identity patterns are alpha-normalized exactly as historical `semantic_scaling.semantic_key`. Exclude every historical TRAIN/DEV/confirmation construction and all prepared S13 TRAIN/DEV constructions using their semantic identities. Both ordered arity3 motifs must be absent from historical TRAIN under the unchanged diversity-audit definition; all examples retain depth3. Validate≤128nodes, unchanged node/edge classes, fixed value vocabulary, unique visible English first-copy targets and single-slot edge labels<32. Record source/config/cache hashes, graph/motif/identity hashes and public-target consistency; no actor imports or model inference in generation. No inherited source mutation is permitted.

This is transfer across arity to smaller unseen motifs with the same n-ary renderer and trained slot support. It is neither deeper compositional transfer nor second-language acquisition. S13's paired renderer acquisition protocol/cache remain unchanged. The generated population is an exploratory fixed diagnostic, not a new training corpus or a replacement confirmation set.

## Prospective all-six evaluator and mechanical profile

Versioned `campaign_semantics_motif_inference.py` validates all six endpoint manifests/checkpoint/primary-output bytes and inherited source/audit before constructing any model. The fixed inherited frequency prediction is applied unchanged to this population. Prepared configs reject running until separately frozen; profile and main each require their own root release. The mechanical profile uses the first8cache examples for each of all six endpoints (48forwards); main uses all1,024for each (6,144forwards). Neither profiles a selected winner nor changes thresholds. No GPU work precedes the complete S12matrix.

Archive metric-sufficient predictions as all128 node labels/presence plus sparse active typed edges and their slot labels. Calibration stores only its different edge set/additional pair slots alongside shared raw node/slot values. Targets are stored once per population, hashed and referenced across endpoints; no score/logit tensors. This intentionally drops edges incident to predicted-absent nodes because the existing metric masks them. `expand_policies` reconstructs tensors for independent metric replay; runtime assertions require exact equality of every metric before/after compaction. Preserve row-level complete/component metrics, motif and node-count labels, per-size complete counts, original thresholds and checkpoint hashes. The same compaction verification applies to targets and the frozen frequency control.

Two CPU tests pass in0.82s with CUDA hidden: random masks/typed edges/slots and exact success preserve all metrics for raw/calibration archives, and both prepared configs reject execution/all-six omissions. These tests instantiate no model. Source/compact-format review remains required before profiling. The separate durable occupancy wrapper follows the existing serial capped launch contract and changes only the dispatched module. No launch is authorized by this implementation.
