# S17 CPU mechanical preparation

No S17 checkpoint was loaded or forwarded. The deterministic actualTRAIN selection is frozen independently before scores, and prepared configs reject execution. Propose one90-second whole-process paired profile (serial control and mixed,128calibration+64DEV each), pending independent review and root cap/release. Main remains prepared/unallocated until measured profile phases.

The evaluator reuses the inherited calibration algorithm and evaluation function unchanged. Observer-only instrumentation marks first DEV access and first NPZ export, restoring the original save function in `finally`. It reports calibration forward+fit+TRAIN metric/packing, DEV forward+metric/packing, export+summary, setup and verification separately; the durable outer wrapper records its preflight and full occupancy. Calibration and DEV costs will therefore be measured separately instead of extrapolating a combined average. No model values, calls, thresholds, target packing or file content except timing metadata are altered by these observers.

`S17-public-spy-check.py` ran with CUDA hidden and two CPU threads using a synthetic callable, not a model/checkpoint. All five forward calls received only `ActorInput`; instrumented versus uninstrumented inherited evaluation exactly matched raw/target/threshold/calibration/metrics and all NPZ arrays. Timing phases add up and the save hook is restored. `S17-guard-check.py` passes deterministic selection replay, prepared/recipe rejection, immutable freeze, cap/source/selection/checkpoint/cache guards, durable preflight-failure receipt and whole-clock timeout receipt. The failure/timeout tests use a fake idle query and a sleeping dummy process, not a GPU job or model inference.

All source/config changes in this preparation only add these timing observations and tests before freeze. Source, selected population, public-only computation and primary S15 references remain independently reviewable. Raw/target replay and immutable model-state checks run for both endpoints in any future profile/main. Old S15 raw/historical calibration outcomes and failed gates stay primary and unchanged.
