# Extended campaign 01

Baseline `123299a`; began 2026-09-23 19:50:55 UTC. Coordinator `/root` owns integration and the sole GB10 GPU queue. Actual baseline/main and every existing worktree were clean; no experiment process was running. Historical worktrees remain intact.

## Authority and goal

Pursue reliable scalar consumption, public-text semantic generalization, direct programmable attention comparisons, and conditionally competent bounded composition. The new campaign authorizes adaptive follow-ups; historical gates remain immutable. Width1024 is standard. No paid cloud, large pretrained model, new general-cognition platform, or external runtime side effects.

12 GPU-hours of experiment-process occupancy, including failed runs and exports; 24 elapsed hours. Reserve20% for confirmation/interventions/verification. CPU audits are recorded separately. Profiles precede major allocations. Failures select a new experiment rather than automatically terminating the campaign.

## Initial hypotheses

- R01: broader-backbone training and stronger scalar readouts may combine. Freeze Stage11 wide backbones; compare unchanged/ridge/CE on fresh paired multi-delay features through16, with32 unseen during fitting/selection. Follow weak fitting, phase mismatch, or retrieval evidence separately.
- S01: eight-construction memorization says little about diversity. Audit128/1024/8192 alpha-distinct unification constructions, then test current actor with meaningful exposure and fresh semantic splits. If it remains uncompetitive, test a direct contextual-text/node-query path as a separate mechanism.
- A01: supplied topology in attention must compete with equivalent graph context and strong message passing on typed ordered traversal. Explicit node grounding first; exact pointer execution is a programmed reference.

Belief research stays frozen. Each branch may advance independently. A narrow value interface can authorize a narrow composition only under a newly declared >=98% three-seed fresh-validation contract; it cannot pass historical six-field reconstruction.

## Current state — 2026-09-24 10:27 UTC

**Returns:** R04/R05 confirm restricted original-mixture scalar access and causal finite-domain decision use across three lineages. Balanced value/type tails remain. R10 larger balanced pools modestly help but miss their screen. R11's exact-replay optimizer comparison improves with lower LR and worsens with unchanged LR; both fixed endpoints miss the declared worst-stratum calibration criterion. That local search is closed, with final raw/state audit passed. No numerical-range or uniform-grid reliability claim follows.

**Semantics:** S12's three paired LR-decay improvements are independently audited: calibrated0→66/131/139 of1024, mean+10.94pp. Directional gain passes; all-seed competence fails seed701. The8192 training constructions have distinct equality/reference patterns but only two ordered tree motifs. All six fixed models recover zero complete graphs after consistent identifier renaming and after an unseen-arity shift; independent audits validate those populations and predictions. S13 fixed English/bilingual arms completed: partial Spanish component acquisition but zero complete Spanish graphs, with English complete recovery18/512 versus135/512 in the matched control. Its extension rule fails and final paired audit passed. S15 shape exposure also fails its acquisition/retention criteria: mixed1/512 complete on the new trained shape,0 on the held-out combination, and90/1024 known shapes versus276control. Archived localization finds455/512 new-shape examples fail only edges; the held-out combination has broader failures. S17 actual-TRAIN calibration improves the trained new shape1→22/512 but not the held-out combination. S16 guarantees the tested public spelling invariance but reduces original calibrated complete graphs66/131/139→48/103/120; both diagnostics are independently audited. S18 is now running a fixed contextual node-read versus10-pass recurrence comparison at width1024, reusing the exact original training trajectory. The reference verifier initially failed on row ordering; all2,048 predictions/targets were identical by event identity, a regression fix was audited, and the corrected three-checkpoint reference evaluation completed. The failed cost remains charged. Main results are pending.

**Attention:** Strong structural attention, keyed context and message passing tie in A06 and follow supplied corrupted graphs in A08. A14 confirms that all value heads using one common model-selected destination repairs deep payload transport: hardest-condition original176/187/177→shared-hard1019/1007/1015 of1024 across three seeds. Paired mean gain81.41pp (shared-event95%CI79.49–83.24), conditional correct-route task3038/3038. This is an engineered read-policy result, not learned planning or unique structural-attention superiority. All nine strong supplied-neighborhood engineering references score100% task and suffix on the same graphs. The complete independent aggregate audit passed; explicit-address localization is closed.

**Composition:** All three C04 lineages and aggregate independently audited. All72 workspace joint cells and24 causal checks pass; the strong role-trained MLP and supplied-copy alternatives also pass. Workspace delay16 is roughly47–50times slower in the measured batch1 comparison. The result establishes a restricted functioning interface, not necessity, efficiency, new arithmetic or autonomous scheduling. Exact primitive semantics, schedule and domain validity remain supplied; copying uses the sole supplied return, not learned selection among multiple returns. This branch is closed.

Root owns the serial GB10 queue. Exact accounting and active releases are in `budget.json` and `queue.json`; roughly6.81 of12 GPU-hours are charged before the running S18 main. Deadline remains2026-09-24T19:50:55Z. Snapshotc129b674 passed755 tests plus26 subtests with a test-only Git context; later reference-verifier regression tests and audits also pass. Tests are mechanical checks, not model competence. All3953 baseline blobs/modes remain unchanged. New restricted gates never overwrite historical failures.
