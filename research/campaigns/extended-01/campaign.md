# Extended campaign 01

Baseline `123299a`; began 2026-09-23 19:50:55 UTC. Coordinator `/root` owns integration and the sole GB10 GPU queue. Actual baseline/main and every existing worktree were clean; no experiment process was running. Historical worktrees remain intact.

## Authority and goal

Pursue reliable scalar consumption, public-text semantic generalization, direct programmable attention comparisons, and conditionally competent bounded composition. The new campaign authorizes adaptive follow-ups; historical gates remain immutable. Width1024 is standard. No paid cloud, large pretrained model, new general-cognition platform, or external runtime side effects.

12 GPU-hours of experiment-process occupancy, including failed runs and exports; 24 elapsed hours. Reserve20% for confirmation/interventions/verification. CPU audits are recorded separately. Profiles precede major allocations. Failures select a new experiment rather than automatically terminating the campaign.

## Initial hypotheses

- R01: broader-backbone training and stronger scalar readouts may combine. Freeze Stage11 wide backbones; compare unchanged/ridge/CE on fresh paired multi-delay features through16, with32 unseen during fitting/selection. Follow weak fitting, phase mismatch, or retrieval evidence separately.
- S01: eight-construction memorization says little about diversity. Audit128/1024/8192 alpha-distinct unification constructions, then test current actor with meaningful exposure and fresh semantic splits. If it remains uncompetitive, test a direct contextual-text/node-query path as a separate mechanism.
- A01: supplied topology in attention must compete with equivalent graph context and strong message passing on typed ordered traversal. Explicit node grounding first; exact pointer execution is a programmed reference.

Belief research stays frozen. Each branch may advance independently. A narrow value interface can authorize a narrow composition only under a newly declared >=98% three-seed fresh-validation contract; it cannot pass historical six-field reconstruction.

## Current state — 2026-09-23 23:17 UTC

**Returns:** R04's fresh three-backbone confirmation passes the declared original-distribution scalar contract through16 updates. R05 independently confirms causal use of that scalar for finite-domain threshold decisions. Neither passes every balanced value/type stratum: mixture averages conceal tails. R06 balancing improves the worst development strata but does not establish uniform reliability. R07's wider nonlinear consumer worsens acquisition at the inherited learning rate; R08 prospectively screens only that optimization parameter. Historical failures remain unchanged.

**Attention:** A03 established a functional routing mechanism but no unique advantage over strong graph context/message passing. Its soft4 model can follow the correct argmax path yet dilute the value. A05's noncoalescing content-selection task separates methods; frozen sharpening helps both structural attention and the strongest context baseline. A06 confirms that fixed concentration repairs deep value transport for all three strong interfaces, including structured context. All405 cells and12 checkpoints are independently audited; this branch is complete.

**Semantics:** Increasing real unification diversity and exposure improves components, but complete fresh graph recovery remains zero. S06 replacing the workspace with token states and S07 occurrence supervision both underperform the matched original actor. S08 shows partial occurrence acquisition is insufficient for correct ordered edges; exact node-renumbering does not rescue graphs. S10 supplied compiler-schema decoding recovers100/512 complete S03 DEV graphs (original0), using predicted attributes and learned associations; this is an audited exploratory engineered-interface result. S11 lower-LR continuation is running; S09 predicted-pointer/contextual read is profiled and frozen. No semantic confirmation data have been consumed.

**Composition:** P01 confirms learned complete typed-record dispatch across three seeds; R05 confirms the consumed returned-value decision contract independently. C01 therefore advances under a new narrow contract: learned proposal, existing exact runtime, frozen learned scalar access/decision. C01 development hybrid passes all12 jointcells (minimum4076/4096); N1 is strong at4056/4096, while N2 fits training but generalizes poorly at2940/4096. All initialphases are completed. A rekeying diagnostic will test the N2 shortcut hypothesis before confirmation. The schedule and primitive vocabulary are supplied; no autonomous planning, calibrated readiness, or broad language claim is authorized by these results.

Root owns the serial GPU queue. About2.3 GPU-hours are charged out of12; queue/budget JSON are authoritative. Width1024 remains standard. All historical source/result bytes and gates remain unchanged. Independent raw/source audits accompany each promoted result. Failed exploratory alternatives are retained alongside successful ones.
