# Extended campaign 01

Baseline `123299a`; began 2026-09-23 19:50:55 UTC. Coordinator `/root` owns integration and the sole GB10 GPU queue. Actual baseline/main and every existing worktree were clean; no experiment process was running. Historical worktrees remain intact.

## Authority and goal

Pursue reliable scalar consumption, public-text semantic generalization, direct programmable attention comparisons, and conditionally competent bounded composition. The new campaign authorizes adaptive follow-ups; historical gates remain immutable. Width1024 is standard. No paid cloud, large pretrained model, new general-cognition platform, or external runtime side effects.

12 GPU-hours of experiment-process occupancy, including failed runs and exports; 24 elapsed hours. Reserve20% for confirmation/interventions/verification. CPU audits are recorded separately. Profiles precede major allocations. Failures select a new experiment rather than automatically terminating the campaign.

## Initial hypotheses

- R01: broader-backbone training and stronger scalar readouts may combine. Freeze Stage11 wide backbones; compare unchanged/ridge/CE on fresh paired multi-delay features through16, with32 unseen during fitting/selection. Follow weak fitting, phase mismatch, or retrieval evidence separately.
- S01: eight-construction memorization says little about diversity. Audit128/1024/8192 alpha-distinct unification constructions, then test current actor with meaningful exposure and fresh semantic splits. If it remains uncompetitive, test a direct contextual-text/node-query path as a separate mechanism.
- A01: supplied topology in attention must compete with equivalent graph context and strong message passing on typed ordered traversal. Explicit node grounding first; exact pointer execution is a programmed reference.

Belief research stays frozen. Each branch may advance independently. A narrow value interface can authorize a narrow composition only under a newly declared >=98% three-seed fresh-validation contract; it cannot pass historical six-field reconstruction.

## Current state — 2026-09-24 07:20 UTC

**Returns:** R04/R05 confirm restricted original-mixture scalar access and causal finite-domain decision use across three lineages. Balanced value/type tails remain. These results authorize the named composition contract; historical reconstruction gates and numerical-range claims remain unchanged.

**Semantics:** Two audited S12 pairs give calibrated constant→decay exact counts 0→66 and 0→131 of 1,024 (raw 0→2 and 0→6). Seed701 misses the 103 competence threshold. Seed703's parent has completed; its constant fork is running, with decay still required. The 8,192 alpha-distinct equality/reference patterns span only two ordered tree motifs at depth3. All-six lexical-renaming and unseen-arity diagnostics, followed by controlled English/Spanish acquisition, are prepared but not yet run.

**Attention:** Strong structural attention, keyed context and message passing tie in A06 and follow supplied corrupted graphs in A08; no unique soft-attention advantage. In A13, one frozen all-record model improves joint-depth/size task accuracy from 86/512 to 499/512 when all heads use the common destination selected from their mean probability. Its predicted route does not change. Shared soft probabilities alone give 89/512. This identifies a read-policy failure in this checkpoint, not learned planning or a replicated result. A14 will test the intervention across three fresh initialization seeds after profiling.

**Composition:** All three C04 lineages completed. All 72 workspace joint cells and 24 causal checks pass; the strong role-augmented MLP also passes every primary answer condition. Supplied-copy slightly exceeds workspace accuracy and is much faster. Workspace delay16 is roughly 47–50 times slower than the role-augmented MLP at batch1. The result supports a restricted functioning interface, not necessity or efficiency. Final third-lineage and aggregate independent audit is pending. Copy means the sole actual runtime return supplied to a learned consumer; scheduling, primitive semantics and domain validity remain programmed.

Root owns the serial GB10 queue. Charged occupancy is 5.27 hours of12, including disclosed conservative upper bounds where complete outer receipts were unavailable. Current fork reserves800 seconds. Deadline remains2026-09-24T19:50:55Z. The expanded suite passed686 tests+10 subtests at32fe72f; later contracts have independent tests. All3,953 baseline blobs/modes were unchanged at that integration check. No historical gate is overwritten.
