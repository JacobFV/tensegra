# Extended campaign 01

Baseline `123299a`; began 2026-09-23 19:50:55 UTC. Coordinator `/root` owns integration and the sole GB10 GPU queue. Actual baseline/main and every existing worktree were clean; no experiment process was running. Historical worktrees remain intact.

## Authority and goal

Pursue reliable scalar consumption, public-text semantic generalization, direct programmable attention comparisons, and conditionally competent bounded composition. The new campaign authorizes adaptive follow-ups; historical gates remain immutable. Width1024 is standard. No paid cloud, large pretrained model, new general-cognition platform, or external runtime side effects.

12 GPU-hours of experiment-process occupancy, including failed runs and exports; 24 elapsed hours. Reserve20% for confirmation/interventions/verification. CPU audits are recorded separately. Profiles precede major allocations. Failures select a new experiment rather than automatically terminating the campaign.

## Initial hypotheses

- R01: broader-backbone training and stronger scalar readouts may combine. Freeze Stage11 wide backbones; compare unchanged/ridge/CE on fresh paired multi-delay features through16, with32 unseen during fitting/selection. Follow weak fitting, phase mismatch, or retrieval evidence separately.
- S01: eight-construction memorization says little about diversity. Audit128/1024/8192 alpha-distinct unification constructions, then test current actor with meaningful exposure and fresh semantic splits. If it remains uncompetitive, test a direct contextual-text/node-query path as a separate mechanism.
- A01: supplied topology in attention must compete with equivalent graph context and strong message passing on typed ordered traversal. Explicit node grounding first; exact pointer execution is a programmed reference.

Belief research stays frozen. Each branch may advance independently. A narrow value interface can authorize a narrow composition only under a newly declared >=98% three-seed fresh-validation contract; it cannot pass historical six-field reconstruction.

## Current state — 2026-09-24 06:45 UTC

**Returns:** R04/R05 confirm restricted original-mixture scalar access and causal finite-domain decision use across three lineages. Balanced value/type tails remain. These results authorize the named composition contract; they do not rewrite historical reconstruction gates or establish numerical-range generalization.

**Semantics:** Two audited S12 pairs give calibrated constant→decay exact counts0→66 and0→131 of1,024 (raw0→2 and0→6). Seed701 misses the103 competence threshold; seed702 passes. Seed703 is training from scratch, with both fixed forks still required. All recipes remain unchanged. The data contain8,192 alpha-distinct equality/reference patterns but only two ordered tree motifs at depth3. Separate all-six lexical-renaming and unseen-arity evaluations, then controlled English/Spanish acquisition, remain queued. No broad semantic-transfer claim follows from the current pairs.

**Attention:** Strong structural attention, keyed context and message-passing references tie in A06 and exactly follow supplied corrupted graphs in A08; no unique soft-attention advantage. A11's broader all-record baseline acquired IID but failed moderate/joint competence after6,000 updates. A12 frozen per-head hardening raises deep task79→124/512 at best but does not repair deep trajectories. Its mean-path and per-head diagnostic supports differ. A13 separately tests a shared model-selected destination, with matched query-trajectory instrumentation and an explicitly privileged routing ceiling. Source/profile audits passed; main is queued. This is value-transport diagnosis, not new learned planning.

**Composition:** C04 lineages0/1 completed and independently passed runtime, raw-metric, causal and provenance audits. Both pass all24 workspace joint cells and all8 causal checks. The strong role-augmented MLP also passes both primary answer conditions in each lineage; static and rekeyed comparators miss at least one. Lineage2 remains mandatory. Exact-copy is supplied copying of the sole actual runtime return into a learned consumer, not learned multi-return addressing. Finite computation overlap is high; neither arbitrary arithmetic nor autonomous scheduling is established.

Root owns the serial GB10 queue. Completed GPU-process occupancy is about4.65hours of12; the running third parent is charged when it finishes. Exact receipts/reservations live in budget.json. The24-hour deadline remains2026-09-24T19:50:55Z. The expanded CPU suite passed686tests+10subtests at32fe72f; newer A13 contracts have separate independent tests. All3,953 baseline blobs/modes remained unchanged at that integration check. Source/protocol milestones, compact raw artifacts, audits and interim figures are pushed to campaign/extended-01. No historical gate is overwritten.
