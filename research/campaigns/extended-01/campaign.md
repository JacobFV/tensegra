# Extended campaign 01

Baseline `123299a`; began 2026-09-23 19:50:55 UTC. Coordinator `/root` owns integration and the sole GB10 GPU queue. Actual baseline/main and every existing worktree were clean; no experiment process was running. Historical worktrees remain intact.

## Authority and goal

Pursue reliable scalar consumption, public-text semantic generalization, direct programmable attention comparisons, and conditionally competent bounded composition. The new campaign authorizes adaptive follow-ups; historical gates remain immutable. Width1024 is standard. No paid cloud, large pretrained model, new general-cognition platform, or external runtime side effects.

12 GPU-hours of experiment-process occupancy, including failed runs and exports; 24 elapsed hours. Reserve20% for confirmation/interventions/verification. CPU audits are recorded separately. Profiles precede major allocations. Failures select a new experiment rather than automatically terminating the campaign.

## Initial hypotheses

- R01: broader-backbone training and stronger scalar readouts may combine. Freeze Stage11 wide backbones; compare unchanged/ridge/CE on fresh paired multi-delay features through16, with32 unseen during fitting/selection. Follow weak fitting, phase mismatch, or retrieval evidence separately.
- S01: eight-construction memorization says little about diversity. Audit128/1024/8192 alpha-distinct unification constructions, then test current actor with meaningful exposure and fresh semantic splits. If it remains uncompetitive, test a direct contextual-text/node-query path as a separate mechanism.
- A01: supplied topology in attention must compete with equivalent graph context and strong message passing on typed ordered traversal. Explicit node grounding first; exact pointer execution is a programmed reference.

Belief research stays frozen. Each branch may advance independently. A narrow value interface can authorize a narrow composition only under a newly declared >=98% three-seed fresh-validation contract; it cannot pass historical six-field reconstruction.

## Current state — scientific runs closed, final verification

The campaign stops at the convergence described in [closeout-decision.md](closeout-decision.md), with no further GPU jobs scheduled. See the [final report](campaign-report.md), [claim map](claim-map.md), and [evidence index](evidence-index.md) for audited scope and references.

**Returns and composition:** R04/R05 establish restricted original-mixture scalar access and causal finite-domain use. C04's two-operation interface passes across three lineages, but the strong role-trained neural and supplied-copy alternatives also pass and are substantially faster. Balanced value/type tails survive R10/R11; neither numerical-range reliability nor necessity of the workspace follows.

**Semantics:** S20 independently confirms fresh known-motif graph recovery by the sequential learner at95.898/94.727/94.857% across three lineages, far above both workspace baselines. Histories, objectives, parameters and compute differ. Every arm remains0/512 on the omitted combination. S21 broader exposure acquires the additional motifs (504/504/437 of512) and retains old motifs within its criterion, yet omitted-combination recovery stays0; confirmation is ineligible. Strict labeled graph-isomorphism checks show that node renumbering cannot repair any valid omitted-combination output. S22 then supplies node count, types or complete node prefixes to frozen models: exact omitted-combination relations and graphs still remain0/512 under every intervention. These privileged diagnostics localize failure beyond early node construction; they do not establish learned repair.

**Attention:** Correct-topology soft bias, competitive keyed context and message passing tie in A06. A14's common hard destination read improves the tested deep-path model by81.41percentage points, but all nine strong supplied-neighborhood references are perfect. The policy is programmed; no unique metric-programming, learned-planning, sparse-compute or efficiency advantage is established.

**Resources and preservation:**119 charged ledger jobs total31,061.728823924484seconds (8.628258GPU-hours), including profiles, exports and the failed job. The12-hour ceiling is not exhausted; additional speculative modules are not scheduled merely to consume it. CPU audits are separate. The GPU queue is empty. All3,953 baseline123299a blobs/modes remain unchanged; the integrated test receipt records843tests,40subtests and49supplementary fixtures passed with one explicit cache-dependent skip. Mechanical tests are not model-competence evidence. Historical gate failures are preserved.
