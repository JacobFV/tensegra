# C02: prospective N2 public-code invariance intervention

Status: CPU implementation/preflight only; profile and every main launch require root GPU release. Main process caps will be frozen from the mechanical profile. This is one exploratory development comparison, not three-seed confirmation. C01 N2 remains a failed fresh acquisition result; its optional extension trigger was fulfilled but root declined the extension. No C01 recipe or outcome is replaced.

C01 selected N2 fits 255/256 answers and 256/256 values on the original fitting-pool prefix but obtains only 2940/4096 answers and 580/4096 values on fresh data despite 4056/4096 correct auxiliary ordered proposals. C02 tests whether immutable random identity codes support memorization. It does not presume that explanation is correct.

## Paired frozen contract

Use the unchanged ContextualBaseline hidden1024/key32 architecture, original seed501 initialization, AdamW(.0003, weight_decay=.01), original sample generator510000100, batch256, and4000 updates. One LR per arm, 1,024,000 optimizer presentations per arm; 2,048,000 total. The same original16384 train examples(seed510000001),2048 calibration(seed510000002), and4096 observed development validation(seed510000003) are preserved. There is no new semantic training pool. New untouched4096-example validation530000003 is expressly exploratory/development, not confirmation. Root checked and reserved530000003/100/200/300.

Static arm deterministically replays the original .0003 path. Rekey arm replaces every non-null32d identity code with fresh iid Gaussian codes independently per sampled row/presentation, consistently across candidate table, all instruction destination/operand references, and requested destination. Exact zero null remains zero. Public equality establishes the bijection; no targets, gold selected-record index, return event/value, exact operation or decision enters rekeying or neural forward. Duplicate/unknown identities refuse. Numeric operands/types, role membership, operation cues, threshold/polarity, table/record order and all labels stay unchanged. Rekey RNG530000100 is independent of initialization and sample selection. This is supplied nuisance augmentation, not newly learned semantic supervision.

Check common initial state, identical sample-index stream and base-data/label hashes. Count optimizer presentations and distinct visited base events separately; rekeyed presentations are nuisance views, not unique semantic graphs. Code-view stream hashes are recorded; unique-view cardinality is not measured or claimed. Null codes consume a random draw before being reset, without changing any model/sample RNG.

Primary endpoint is fixed step4000. Secondary checkpoint is selected only on unchanged original calibration answer count at0/250/500/1000/2000/3000/4000; ties select earliest. Neither calibration nor validation is rekeyed during selection. Original C01 final4000 model was not saved; this replay supplies that endpoint without relabeling it as a recovered historical checkpoint. Compare historical .0003 calibration logits/labels at every saved step and original selected-state tensors; report exact parity or all deviations. Save final model/optimizer and RNG states, selected model, all calibration logits/labels, visitation and hashes. No recipe adaptation after observing outcomes without a new prospective branch.

## Outcomes and controls

Report answer-only and auxiliary33-value accuracy, full ordered proposal/primitive/destination/required-operand accuracy, primitive/type/query-gap strata and paired arm disagreements. Keep endpoint-primary and calibration-selected-secondary results separate. Evaluate both original and fresh development populations with static keys, then identical post-selection rekey controls(eval RNG530000200). Retain all C01 public controls on fresh validation: record order, inventory order, fresh names(actor-input no-op smoke), reverse roles, unrelated instructions(control seed530000300+i). Reverse-role labels derive the actual changed public record; report original and supplied answers and changed-answer supports. No threshold for superiority or confirmation is inferred from this single pair. Preserve failures/refusals and do not extend automatically.

Mechanical profile uses16 updates,32 training and16 evaluation rows, width1024, original batch256 replacement sampling. Profile namespaces539000001–4 are disjoint from main. Both static and rekey phases capped60s each, pending root release/reviewer; only timing/memory used to freeze main caps. The tiny profile is not an acquisition test. Original full two-LR N2 process466.858s includes data creation, evaluation and export; it is a rough guide, not a per-kernel timing estimate.

CPU mechanics: exact identity links/null/nonkey evidence preserved; malformed identities refuse; dedicated generator deterministic and global RNG unchanged. A one-update paired width1024 smoke verifies same initialization/index stream/data with changed code views and complete endpoint/control exports. GPU profile/main remain unrun.
