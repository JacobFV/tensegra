# C01 hybrid development: scheduled composition acquired on one fresh population

The frozen P01→exact primitive→R04→R05 pipeline meets its prospective one-population development contract. All4096 complete ordered lowerings are correct, with no runtime refusals. At both2/8 distractors, joint correct-lowering-and-answer counts are4081/4096 for delays0/1/2/4/8 and4076/4096 at16; every cell exceeds4015. Answer-only counts happen to coincide because lowering is perfect on this population. This is one development population with the declared P01303/backbone11 pairing, not three-seed composition confirmation.

Oracle lowering and oracle return produce exactly the same exported logits and predictions as the actual hybrid on these clean examples. The engineering exact-copy consumer answers4081/4096, separately from the learned scalar-access path. These controls delimit the remaining error; they do not replace hybrid inputs. Frozen model/buffer/normalization hashes match before and after execution. No new hybrid parameter was fitted.

## Actual fact use and public controls

On the fixed2048-example causal subset, clean original-answer accuracy is2043/2048 at delays0/1 and2042/2048 at16. Dropping the return reduces original-answer accuracy by23.05/22.22/22.07percentage points. Normalized gain over the frozen query-only comparator relative to the exact-copy comparator is1.000/1.000/0.9979. All declared criteria pass. Query-only accuracy is1574/2048; its strong prior is expected under this generator.

Wrong-return changes the answer on901 examples: supplied-answer agreement is899/901 at0/1 and898/901 at16. Swap changes628 answers, with627/628 supplied-answer agreement at every covered intervention delay. Both exceed the>=90% criterion with>=256 support. Original, requested and actually supplied labels stay separate. Drop has no supplied label; raw exports preserve that absence. Reported confidence intervals are paired normal approximations for the clean-minus-drop difference, not independent-delay replication.

Public record/inventory order and unrelated-instruction controls retain4081/4096 at0/1 and4076/4096 at16. Reversing binary operand roles yields4080/4096 requested-answer correctness at0/1 and4078/4096 at16, with original-vs-requested changed subsets retained. Fresh-name renaming is an actor-input no-op smoke test, not lexical transfer.

## Remaining limits

Near-threshold failures remain prominent. At16 steps, equality queries are123/136 correct and absolute half-unit gaps246/252; integer/float/Boolean groups are1632/1634,1610/1628 and834/834. Neither this aggregate pass nor perfect lowering establishes uniform-value or boundary-uniform competence. The registered finite mixture, existing primitives, complete typed evidence and fixed schedule are supplied. No learned language grounding, planning, halting or numerical-range generalization is established.

N1/N2 equal-public-evidence neural baselines are still pending. Do not compare hybrid joint lowering-plus-answer accuracy against a baseline answer-only score; final comparisons use answer-only on both sides, with the hybrid semantic gate separate. Inherited P01/backbone/R04/R05 acquisition differs from baseline training and must be disclosed; current-phase timing is not matched total training compute. No superiority claim or confirmation recipe follows from this phase alone.

Full experiment-process occupancy117.989399920s, exit0 under600s cap; internal116.890662014s, peak447417856CUDAbytes. GPU released before CPU analysis. [Raw compact outputs, source/checkpoint provenance and receipts](../../../results/campaign-01/composition/c01-development/hybrid/raw/manifest.json) are committed; full tensors remain at `~/topoformer-campaign-01/composition/c01-development-hybrid/`. Independent audit is requested before interpreting this as verified development evidence.
