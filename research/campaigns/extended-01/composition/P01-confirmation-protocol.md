# P01 fresh confirmation v1

Frozen after successful one-seed development and before any confirmation launch. Keep the reviewed acquisition runner/sourcef0d0005 byte-identical, full width1024/key_dim32 and registered2000updates, AdamW.001/weight_decay.01, batch256,8192 fitting/1024 calibration/4096 validation, checkpoints0/250/500/1000/1500/2000. Each initialization independently selects by maximum calibration full semantic count, ties earlier. Validation and five public controls follow selection only. Do not shorten training to the successful development250 checkpoint or select a later checkpoint from validation outcomes.

| Initialization | Fitting seed | Calibration seed | Validation seed | Controls seed |
|---|---|---|---|---|
|302|481000001|481000002|481000003|481000100|
|303|482000001|482000002|482000003|482000100|
|304|483000001|483000002|483000003|483000100|

These procedural populations are distinct from development48-million and profile47.9-million namespaces and from each other. The population generation recipe retains exactly the original retention-event mixture under correct lowering. Each config is committed before launch. Coordinator controls staging/release. Proposed hard cap120s each (development measured22.114s); combined estimate70s and ceiling360s. No automatic launch or C01 execution.

Primary promotion: all three4096-example clean validation cells require >=4015 exact full ordered semantic proposals (>=98%). The five supplied-control validation cells must also meet this count in every initialization for the declared control-robust scope. No pooled averaging, dropped refusals, numerically coincident substitutions, or control retargeting can repair a failed cell. Separately report primitive, destination, first required operand, binary second operand and noncommutative order with denominators. Raw unary null-pointer diagnostics remain separate from canonical predicted-arity absence. Fresh-name renaming is an actor no-op smoke test and does not widen the claim.

Persist raw logits/public tensors and original/supplied control labels, all calibration snapshots, selected checkpoint hashes, config/source provenance and actual training visitation. Independent CPU replay must reproduce full-semantic/component counts, canonicalization from predicted arity, selected checkpoint logits and earliest-tie selection. Reconstruct source public examples from seeds/hashes; verify no reference events or gold bindings reach forward. For clean correct predictions, independently execute actual predicted proposals and compare all returned event fields to original witnesses. Report any wrong/refused proposals without gold repair. Audit public role/order controls and distinct populations. The runner's per-run fixed generic claim string still says single-development-seed; confirmation identity comes from the explicit config version and this frozen registration, and no per-run summary alone promotes the pooled gate.

P01 confirmation permits only restricted typed dispatch/address matching and exact event-fidelity claims. C01 entry additionally requires independently audited three-backbone R05/R04 contracts on the consumed mixture. Whole-class float-zero limitations, numerical-range restrictions and supplied scheduling remain. Even a pass does not establish language/planning/learned halting or end-to-end composition success.
