# C01 development v1: frozen scheduled composition, equal-evidence neural controls

P01 independent audit e01a820 and R05 full raw/cache/population audit passed the restricted consumed-interface entry contracts. Coordinator now authorizes CPU implementation and protocol review; no profile/main GPU release is implied. This is a fixed supplied complete-evidence schedule: learned primitive/destination/ordered operands → existing exact arithmetic → typed return → frozen scalar access → frozen learned threshold/polarity decision. It is not learned halting/planning or two independently acquired symbolic lowerings.

## Frozen populations and checkpoints

Development pairs frozen P01 initialization303 selected250 with frozen Stage11 backbone11, R04 CE16384/900, and R05 confirmation11 learned consumer. This mapping is fixed and informed by prior component development; it is not a held-out initialization claim. Exact SHA256 values and paths for all six loaded artifacts (P01, backbone, scalar accessor, learned consumer, oracle consumer, query-only consumer) are in `campaign-c01-development.json`, checked before use. Every hybrid weight remains frozen/eval; baselines are separate model instances. Fresh procedural seeds510000001/2/3 supply16384 fitting,2048 calibration,4096 validation events. Swap seed511000003. Profile uses509000001/2/3 with512/64/64 events and swap509000004. Global configuration search found these namespaces unused before registration.

The lowering public record/table generator and original event prevalence remain unchanged. Public numeric literal types determine exact Python scalar output types. Runtime accepts only actual predicted actions and public rows. A refusal creates no event; refused rows stay prediction−1 and count as failures. Runtime ranges are never clipped or repaired from original results. C01 evaluates both target correctness and agreement with actually supplied facts. Incorrect lowering may shift the event distribution; correct-mixture R04 evidence does not guarantee those shifted events.

## Hybrid development contract and controls

One fresh4096-example validation population is evaluated at delays0/1/2/4/8/16 with2/8 memory distractors, paired across every arm and delay. Primary clean acquisition target is at least4015/4096 examples with BOTH full ordered semantic lowering and original-answer correctness in each of12cells. Also report answer-only counts, full lowering counts, actual refusals and supplied-answer agreement. Every error remains in the denominator even if its binary answer happens to coincide. This is one development population; promotion requires a later pre-outcome frozen three-population confirmation and all consumed-interface scopes.

Matched controls: oracle lowering executes the publicly requested primitive/operands, oracle return supplies the unchanged original event, and exact-copy engineering passes the ACTUALLY executed scalar one-hot to the frozen R05 oracle-trained comparator. The latter is privileged engineering, not learned scalar discovery. Query-only uses the frozen R05 prior comparator. No oracle control replaces primary hybrid events. Original and actual labels are retained separately; absent/refused returns have no supplied label.

For the first2048 validation examples with8 distractors, evaluate correct/drop/wrong/swap at delays0/1/16. Wrong-value reflection and witness rebuilding reuse the audited R05 intervention function on the ACTUAL returned event, never the original gold result. Swap uses a separately sampled valid event and its key inventory, retaining original query/context. Drop suppresses all initial/persistent return reads. Pre-existing lowering refusals remain absent under every intervention. Intended-use criteria are >=15percentage-point drop in original-answer accuracy under removal, >=80% of exact-copy-minus-query-only clean gain, and >=90% supplied-answer agreement on answer-changing wrong/swap subsets, each support>=256. A nonpositive normalization denominator cannot pass. Report effects and supports without clipping, and include original-vs-supplied paired counts. This is development evidence, not an autonomous policy claim.

Public controls on the full4096 population at0/1/16,8 distractors: record order, inventory permutation, fresh-name no-op, reversed binary instruction roles and replacement of unrelated records. Primary requested-answer control targets are derived in a separate privileged evaluator from changed PUBLIC records; hybrid executes its own actual predictions. Retain original, requested and actually supplied answers separately. Reversed-role examples can have changed requested answers; report those subsets. Do not condition evaluation on correct proposals. Fresh-name control is explicitly actor-input no-op. Numerical task/control counts and per-type/value/operator/query-gap strata—including float-zero—must remain visible. No balanced-grid reliability claim follows.

## Neural controls and fixed acquisition recipe

Implement the strong N1/N2 design in `C01-neural-baseline-design.md`. N1 initializes its learned soft operand retriever from the same P01 checkpoint and fine-tunes end-to-end; N1-frozen is a separate optional interface diagnostic. N2 observes all16 tokens through two1024-wide ordinary attention blocks,8heads,FF1024,dropout0, no arbitrary inventory/record position encoding. Ordered operand slots remain explicit. N2 has direct answer/value and operation/pointer auxiliary heads; no exact address gather or primitive occurs in forward. N1's answer head is direct, not forced through its auxiliary scalar classes. Both receive every public typed numeric witness and query; query-only is not the neural comparison arm.

All learned hidden widths1024, key_dim32, batch256, ordinary FP32 training, AdamW weight_decay.01. Each arm gets the same train pool and sampled-index stream (seed510000100),4000updates×256presentations per registered learning rate .001 and .0003. Loss is unweighted sum of answer CE,33-value auxiliary CE, primitive CE and existing destination/required-operand CE losses; unary null pointer remains masked. Evaluate calibration at0/250/500/1000/2000/3000/4000. The added3000 checkpoint makes the proposed last1000-update continuation trigger observable. Select highest calibration answer count, then earlier step, then first registered LR. Validation/controls occur only after selection. Save every calibration logit snapshot and visits, selected state/hash, validation/control logits/public inputs and separate labels. No target enters forward. A >=0.5-point calibration improvement from3000→4000 makes an equal-exposure extension to8000 eligible by separate coordinator authorization; it is not implemented/automatic in this initial runner. A baseline still improving cannot support a claim that the hybrid is intrinsically superior.

Neural-only clean predictions are delay-independent because original evidence stays available. Their original-input predictions also remain unchanged under return-only interventions; this is correct behavior, not failed causal use. Compare clean answers fairly; treat return interventions as hybrid mechanism tests. For public-record controls every arm sees identical changed inputs. Report trainable/total parameters and all inherited training plus actual current optimization cost. Different historical pretraining cannot be called compute matched. A baseline tie/win is valid.

## Profiling and release

Use separate phases `hybrid`, `n1`, `n1_frozen`, `n2` in `campaign_composition_study`, each under the external capped C01 launcher. Profile16updates at batch256 and width1024;64-example validation preserves all declared delay/control kinds. Proposed profile caps60/60/60/120seconds respectively. Main provisional caps600/900/900/3600seconds are planning ceilings only, to be replaced by measured estimates and explicit queue release. No silent batch/width reduction. Record full process occupancy, peak allocation, parameters and provenance. Independent reviewer must preflight source/CPU tests before any GPU phase. Hybrid profile has no acquisition training; neural profiles do not establish task competence or select the development recipe.
