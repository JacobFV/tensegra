# C03: prospective matched N1 operand-role augmentation

CPU source/protocol freeze; no GPU release. Root approved this narrower replay contract in place of the earlier unfrozen C01-N1-role-augmentation draft. C01 N1's original clean success and reversed-role failure remain unchanged. This is one new development intervention, not confirmation. Independent C02 rekeying targets a different baseline and is not combined here.

## Unchanged comparator and exposure

Both arms use original NeuralOperandBaseline hidden1024/key32, P01303 initial lowerer (hash in config), model seed501, sample seed510000100, original16384 training510000001 and2048 clean calibration510000002, LR.0003, AdamW weight_decay.01, batch256 and4000updates. Each receives1,024,000 optimizer presentations; pair2,048,000. No LR search or extension. Static arm is the original N1 .0003 replay. Original4096 validation510000003 is retained; new paired clean/reversed exploratory validation540000003 has4096 base events. Root checked/reserved namespaces540000003/100/300 and profile549000001–4.

Primary is fixed4000. Secondary selection uses only original CLEAN calibration answer count at0/250/500/1000/2000/3000/4000, earliest tie. Reversed calibration is saved as diagnostic and NEVER selects either arm. Validation does not select. Static replay compares all historical clean calibration labels/logits and selected model tensors. Missing files or unequal labels abort; numerical deviations require coordinator review before comparison claims. Store final and selected models, final optimizer/RNG, sample-index hash and visits. No recipe adaptation after outcome inspection without a new prospective branch.

## Sole intervention

At each augmented optimizer presentation independently sample Bernoulli(.5), generator540000100 separate from sample/model RNG. When selected, swap the two operand references in EVERY public binary instruction record, including distractors, using public operation cues; keep unary records/null unchanged. Public candidate keys, values/types, instructions' destination and operation cues, requested destination, threshold/polarity and row order stay fixed. No gold selected instruction is used to choose which records swap. Static arm uses no swaps. Do not combine key re-randomization, extra operands, operations, hidden layers or stronger exact arithmetic into forward.

Private supervision is precomputed by exact execution of the ACTUAL swapped public requested record. Targets include actual ordered pointers, primitive, value class, answer and runtime-derived result type. Add/mul/neg values and answers are invariant; sub and compare are direction sensitive. Exact execution remains outside model.forward. Every swapped view must satisfy the supplied numeric range/type/domain contract. Any refusal aborts; there is NO rejection/resampling, clipping, substitution or gold repair. This changes the joint operand-witness distribution for the neural baseline; it is not claimed to remain the original R04 return-event mixture.

Log total optimizer presentations, swapped/clean counts, base-event visitation, and visited(base-event,view-slot) counts separately. Two slots can sometimes have identical public evidence, so these are not claimed to be distinct semantic graphs or distinct tensor values. Preserve exact same sampled base indices across arms. Swap masks have their own stream hash; target/view selection shares that mask. Numeric/task labels remain supervision only.

## Evidence and gates

Report answer-only clean AND reversed accuracy for fixed4000 and selected endpoint separately, with paired arm differences and original/supplied changed-answer counts. Predeclared acquisition target: at least98% in BOTH clean and reversed fresh development views (4015/4096 each); failure remains visible. Ordered lowering, primitive/destination/required operands, auxiliary scalar accuracy, correct-lowering conditional accuracy and each primitive are diagnostics. Include unchanged-answer errors, multiplication symmetry, comparison direction, and public record/inventory/unrelated-instruction controls. Fresh names is an actor-input no-op smoke. No claim of unrestricted arithmetic, learned planning, or hybrid superiority follows.

If this becomes the strongest acquired baseline, a later C01 three-seed confirmation must prospectively include that recipe with fresh matched populations, answer-only comparison and the separate hybrid joint lowering+answer gate, plus inherited compute disclosure. This development study cannot itself supply three-seed confirmation.

## Mechanical readiness

Two CPU tests verify public view equivalence to the exact evaluator, involution, unchanged keys/query/unary slots, explicit sub/compare changes, labels selected with the same independent coin and untouched global RNG. One-update paired width1024 CPU smoke verifies matching initialization/sample/data hashes and endpoint/calibration/control exports. Existing CPU environment only, no installs.

Profile16updates with32train/16eval rows and batch256, width1024; distinct549000001–4 data namespaces. Proposed60s cap each static/roles; release only at root queue boundary. Main caps will be frozen from profile; historical N1 two-LR75.14s is a starting estimate, not a guaranteed bound with new paired evaluations and private-view preparation. No profile accuracy will select the recipe.
