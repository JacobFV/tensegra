# P01 standalone acquisition launch contract v1

Prospective source/config review, before any profile or development training. Parent coordinator alone releases GB10. Profile cap60s; provisional development cap360s including data preparation, training, evaluation and exports. No launch follows merely from committing this document. Reestimate from measured profile throughput and separate data/evaluation costs; queue through coordinator with the exact source hash. No C01 code or runtime consumer is included.

Commands after release: `python -m topoformer.campaign_composition_acquire --config configs/campaign-p01-profile.json --output <new-profile-directory> --device cuda`, then separately authorized development config with a fresh output directory. Existing output paths are rejected. The outer coordinator watchdog enforces process occupancy cap; JSON cap is declarative. No package installs. Full width1024/key_dim32 is enforced; only isolated tests monkeypatch width16 for mechanical checking.

The frozen proposed recipe in `campaign-p01-development.json` uses seed301, fresh fitting8192/calibration1024/validation4096 at seeds48000001/2/3, AdamW lr.001 and weight_decay.01, batch256,2000 updates (512000 sampled presentations), no schedule or gradient clipping. Existing `proposal_loss` trains primitive, destination, first operand and binary second operand; absent unary pointer is excluded. Return data are generated only to construct public records and private acquisition labels; the precomputed reference is discarded before training. Forward receives only `model_inputs(public_rows)`; numeric witnesses are typed public values. No return-event value, selected binding, gold op, target pointer or gold result is fed to the actor. Labels enter loss/metrics only.

Checkpoints0/250/500/1000/1500/2000 report calibration and a fixed fitting-prefix1024 curve. Maximum full semantic calibration count selects the checkpoint, ties earlier. Validation and controls are evaluated only after this selection. Save all calibration logits, selected state, validation/control full-precision logits and public tensors, labels separately, dataset seeds/counts/public hashes, config/source hashes, actual elapsed/fit time and peak allocation. A profile uses disjoint seed399 and47900001/2/3 populations,512/128/128 examples and64updates. It is never confirmation evidence or used to change the development validation population.

Report raw learned primitive/destination/first-operand accuracy and second-operand accuracy only on truly binary examples. The raw unary null-pointer diagnostic has its own unary denominator and is never an acquisition gate. Canonical unary correctness is an explicitly programmed arity rule based on the predicted primitive. Full semantic proposal requires correct primitive and all canonical ordered pointers, including destination; any wrong destination/op fails regardless of numerical coincidence. Report predicted public output-schema validity separately. The runner does not execute a learned return-consumer pipeline; exact CPU execution remains a mechanical witness-fidelity test. Full semantic errors and schema-invalid predictions remain in primary denominators.

Fixed controls: reordered records, permuted candidate inventory, fresh names, reversed binary instruction roles and replaced unrelated instructions. Changed controls are constructed entirely from public rows. Privileged evaluation labels are independently rederived by exact public address lookup after the intervention and never forwarded. Reverse-role results retain supplied/original labels and summary accuracy plus changed-proposal support; inventory-permutation original pointer labels are explicitly noncomparable until remapped. Missing/ambiguous public schemas are CPU refusal tests rather than fabricated actor observations. No confidence product or probabilistic-readiness claim. Default argmax predictions use only public schema checks; uncertainty-policy experiments remain separate.

This single development result decides whether a fresh three-initialization confirmation is worthwhile. It cannot promote P01 or C01. Prospective continuation requires development full semantic count at least4015/4096, then a separate frozen confirmation protocol using fresh seeds302/303/304 and populations declared before launch. P01 full-semantic validation must meet >=98% independently in all three fresh populations; C01 also requires audited R05 confirmation. Failure stays recorded and selects a new versioned intervention.

Before profile/main, provenance logging also includes selected-checkpoint byte SHA256, total/trainable parameter counts, actual optimizer presentations, unique fitting-event visitation at every checkpoint, and the complete per-event visitation-count tensor with SHA256. Fresh display-name renaming is an actor-input no-op invariance smoke test because the model never consumes display names; it is not learned lexical transfer.
