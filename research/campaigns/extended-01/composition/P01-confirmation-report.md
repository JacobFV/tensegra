# P01 confirmation: reliable restricted typed lowering, one retained error

All three fresh initializations meet the prospective full semantic proposal contract, with no checkpoint chosen from validation. Numerical results await independent raw/state/population/event-fidelity audit before interface promotion or C01 implementation.

| Initialization | Selected step | Calibration | Fresh validation | Each of five supplied controls |
|---|---:|---:|---:|---:|
|302|250|1024/1024|4096/4096|4096/4096|
|303|250|1024/1024|4096/4096|4096/4096|
|304|250|1023/1024|4095/4096|4095/4096|

The registered cutoff is4015/4096 in every cell; none is replaced by a pooled mean. All three runs retain2000updates and all six calibration snapshots. Every post-zero saved calibration count ties the250-update count in its initialization, so the earliest-tie selection rule determines all three checkpoints. Each run receives512000 presentations and visits all8192 fitting examples. Width1024/key_dim32 and the142341-parameter ProposalModel are unchanged.

Every primitive, destination and first operand is correct in clean validation. Required second-operand counts are3282/3282,3263/3263 and3278/3279; ordered noncommutative pairs1630/1630,1679/1679 and1652/1652. Raw null-pointer diagnostics are0/814,0/833 and0/817 while canonical absence is perfect for those same unary examples. Unary absence remains a programmed predicted-arity guarantee, not a learned pointer success.

The one seed304 error is validation row27, multiplication: predicted second-operand pointer3 rather than6. The actual public operands are(-5,-4), whereas the requested operation uses(-5,1). Exact CPU mechanical replay refuses the actual proposed result because it exceeds the runtime bound; no returned scalar is supplied and no original -5 result is restored. The error remains in every relevant full-proposal denominator even though the output-role schema is valid. No validation-driven checkpoint change or confidence threshold is introduced.

Reversing ordered binary roles changes3282/3263/3279 proposals. Agreement with originals falls to814/833/817 of4096—the unchanged unary cases—while supplied-label performance remains as in the table. Record/inventory permutations and unrelated-record replacements preserve these accuracies. Display-name changes are actor-input no-op smoke tests, not lexical transfer. These are public typed dispatch/address-matching results with explicit operation codes; no language, planning or learned scheduling claim follows.

Experiment-process occupancies are23.458178592/24.243418547/22.722677094seconds, sum70.424274233seconds; serial batch wall70.586268995seconds. All exit0, no cap exceeded, GPU released immediately after export. Full tensors/checkpoints remain in `~/topoformer-campaign-01/composition/p01-confirmation-{302,303,304}/`. [Committed compact predictions, hashes and receipts](../../../results/campaign-01/composition/p01-confirmation/gates.json) link each checkpoint and include all calibration/validation/control per-example outputs. Original artifact bytes remain preserved.

C01 remains conditional until this gate and the consumed R04/R05 contracts have independent audit approval. A lowering pass does not by itself establish end-to-end composition, uniform scalar reliability or numerical-range generalization.
