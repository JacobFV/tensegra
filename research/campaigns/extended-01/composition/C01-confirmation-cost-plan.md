# Prospective C01 confirmation: consumed interfaces, strong controls and inference cost

Design contract recorded before C03 outcomes. No confirmation populations, training allocation or GPU benchmark are launched by this document. Final recipe/checkpoint mapping, fresh three-seed namespaces and process caps require a separate prospective config freeze after development gates and independent review. Preserve every C01/C02 failure; three-seed confirmation is not pooled development performance.

## Comparison contract

Compare the actual finite-domain task on identical complete public typed records, public numeric operands and threshold/polarity queries. No language, learned planning/timing, arbitrary numerical representation or multiple exact-execution claim. The composed path performs ONE actual predicted exact primitive execution, followed by an acquired return interface and decision. Wrong lowerings execute their actual public operands; refusals persist and count as incorrect. No gold reset or second exact execution supplies the operational answer.

Retain the strongest competent neural development recipe for fresh confirmation, including C03 if its augmentation closes the operand-role weakness or dominates the hybrid. Recipe choice occurs before new confirmation data are examined and is disclosed as development selection. Both clean and reversed finite-domain behavior matter. Preserve an answer-only comparison for all arms, plus a separate hybrid joint ordered-proposal-and-answer gate. Do not infer necessity or efficiency from a small accuracy difference. C02's augmented contextual baseline remains a disclosed competitor/result even if another neural baseline is stronger.

At minimum distinguish these three actual inference paths:

1. Workspace composition: learned P01 proposal → actual exact runtime → typed return event → frozen R04 historical workspace reconstruction/accessor → frozen learned R05 return consumer.
2. Supplied-copy engineering control: the SAME learned P01 proposal → SAME actual exact runtime → public returned scalar encoded as the supplied33-class one-hot value → frozen trained R05 oracle-return consumer. No workspace reconstruction/backbone executes. This is an explicit engineering copy interface, not a successful reconstruction or accessor pass. It has the same actual lowering errors/refusals; gold/oracle lowering never replaces them. The copied result comes from actual runtime output, not private labels.
3. Strong neural comparator: all its supplied public typed evidence through its frozen learned inference path, with no exact runtime/value oracle. Include the acquired N1 role-augmentation recipe if it is strongest, even if it wins. Report its inherited P01 initialization/training too.

Existing C01 exact_copy already measures path2's answer from actual built returns; future latency must time its FULL path including P01 and runtime, rather than just timing the final consumer or reusing a preconstructed return. R05's oracle-return consumer is independently trained on supplied exact33-class values; its privileged representation is disclosed. Do not label this arm the same consumer or acquisition as the reconstructed-score path.

## Timing and resource measurements

Measure end-to-end inference wall time on the same hardware, dtype, thread count and event order after loading and fixed warmup, with explicit accelerator synchronization before/after timed intervals. Include input transfer, proposal, device-to-host action transfer, actual CPU runtime/public checks, return construction/encoding, recurrent reconstruction where applicable, readout/decision and output transfer. Exclude dataset generation, gold-label computation, optimizer work and checkpoint disk loading from inference timing; report setup/cold-load separately if measured. No gold values may be cached in operational inputs.

Predeclare batch sizes1 and64, and a fixed first256-event subset from each new confirmation validation population, selected by index before any predictions. Use the same subset in every arm. Primary workspace latency is the registered delay16 schedule; delay0 is a separate endpoint. Run each delay as its actual independent inference path, rather than dividing a shared multi-delay capture by the number of reported cells. Exact-copy/neural paths have no reconstruction delay and are timed independently. Fixed warmup8 examples and three timed passes per cell; report each pass, median and range. This is a future benchmark allocation, not current GPU authorization; profile/cap the implementation before scheduling it. If resource limits require changes, revise/freeze the benchmark before any timing outcomes are inspected.

For each timed cell report total events, correct answers, refusals, total wall seconds, seconds per attempted example, throughput, and seconds per correct example = full timed wall / correctly answered examples. Report per-pass and aggregate accounting without discarding errors or timing only successes. Also disclose task accuracy on all registered validation events; the timing subset is not a replacement acquisition gate. Include paired correctness counts and uncertainty; do not collapse higher cost and lower accuracy into an unsupported efficiency claim.

Report executed parameter counts by component, total resident model parameters, peak accelerator memory and registered recurrent microsteps. Count backbone/workspace reconstruction explicitly; the nominal width1024 does not imply equal architecture size or compute. The historical backbone may be far larger than N1 and repeated reconstruction can dominate cost. Exact-copy can legitimately dominate latency; preserve that result as an engineering alternative. Do not subtract work merely because components are frozen.

## Training and public-input diversity

Publish a component ledger for P01, inherited Stage9/Stage11 backbone, R04 accessor and R05 learned/oracle consumers, with selected-checkpoint presentations, distinct underlying events, delay/view reuse, full development search exposure and measured occupancy scope. N1 inherits P01; N2 starts from scratch. Refer to C01-inherited-compute.md and source manifests; distinguish pure fit timing where available from inclusive capture/control/evaluation runner time. Do not charge every historical diagnostic as selected-head training, or omit inherited pretraining and call equal fine-tuning exposure equal total compute. Count unsuccessful development searches separately and retain their cost.

C02 static and rekey arms share16,384 underlying semantic examples and1,024,000 optimizer presentations each. They do NOT have identical public-input diversity: static reuses fixed identity-code views; rekey generates a new Gaussian nuisance-code view per presentation. That intervention supports a key-shortcut/generalization diagnosis without uniquely proving a stored-key lookup mechanism. C03 similarly adds sampled role views with actual recalculated supervision while retaining the same base-event pool. Report views, base examples and presentations separately, not as fabricated additional semantic graphs.

All accuracy and cost conclusions remain scoped to the actual finite-domain interfaces, supplied schema/schedule and acquired checkpoints. Fresh confirmation and independent raw audits are required before promotion; report both supplied-copy and strongest-neural alternatives even if they weaken a hybrid advantage claim.
