# Campaign stopping decision

Scientific runs close at a meaningful convergence point, not because a single gate failed or the resource ceiling was exhausted. No further GPU job is scheduled. Final report/audit/integration work continues.

- Returns: the original-mixture scalar interface and downstream decision use were confirmed, and the bounded pipeline functions. Balanced value/type tails survive several independently scoped pool/optimizer diagnostics. Strong role-trained neural and supplied-copy alternatives also pass and are substantially cheaper; another generic recurrence change is not supported by these results.
- Programmable attention: aligned topology and shared hard reading can help the tested models, but competitive supplied-neighborhood/context references already solve the same tasks. No unique metric-programming, sparse-compute, or learned-planning advantage was established. This explicit-address branch has reached its comparison boundary.
- Semantics: switching the complete learner acquires fresh known-motif graphs at95.16% mean across three confirmed lineages, a major improvement over both workspace baselines. Broader motif exposure learns the newly supplied shapes and preserves old ones, yet the omitted combination remains0/512. Strict identity-preserving permutation checks rule out node renumbering as a repair. Frozen count/type/full-node-prefix interventions still leave heldout exact relations and complete graphs at0/512. The problem is not only early node construction; no node-only repair is evidenced.

These are independently bounded findings, not a single model demonstrating general orchestration. Further semantic progress would require a new justified relation/layout learning or decoding intervention, with its own acquisition and transfer study; the current diagnostics do not select one. We will not fill unused budget with another speculative module or repeat a saturated recipe. The sealed S21 confirmation remains unused because its development gate failed.

The ledger contains119 charged processes (profiles, exploratory runs, confirmations and one failed process), totaling31,061.728823924484 seconds,8.628258 GPU-hours. The12-hour ceiling leaves12,138.271176075516 seconds unused. Confirmation matrices retained all prescribed seeds and outcomes. Source/config/data/checkpoint hashes, raw metrics, learning curves, interventions and independent receipts are preserved. Final accounting distinguishes cumulative experiment-process occupancy from CPU auditing and elapsed campaign time.

Next research decisions: develop a semantic relation/layout interface only after a concrete supervised fixed-set and fresh-structure contract; prefer learned addressing plus exact copying when exact scalar regeneration is unnecessary; retain strong context/message-passing and neural-only baselines for any renewed programmable-attention or composition claim. These are recommendations, not queued jobs or retroactive gate changes.
