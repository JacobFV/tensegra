# A11: final bounded acquisition exposure check

Prospectively authorized by root after the preserved A10 failure. Continue the unchanged record interface from saved3000 to6000 cumulative updates, exactly3000 additional updates. No architectural, initialization, optimizer, learning-rate, weight-decay, batch, width or training-data change. The purpose is to discriminate continuing acquisition from an exposure limit; an underacquired global-record comparator does not establish attention superiority.

Load saved A10 model/AdamW/CPU+CUDA RNG state with file SHA256 `e310472135468fb2de086d29684ee9993091cc9cc09470fc84079005b13d78b6` and model tensor SHA256 `bbf755776f1164ff892f7c19d482c9bb342fdb9c9c3b2174a225fb7a8dadb4f7`. Skip reevaluation at3000 because the stored RNG follows its final evaluation. Keep the exact saved-state runner validated for A10 by CPU split/uninterrupted equality of model, AdamW and RNG; no runner changes are required.

Fixed monitoring checkpoints4000/5000 use173M populations,256events each. Fixed final6000 development uses fresh211M population,512events in each of the same seven conditions, including the predeclared joint record-logit multiplier2/address16 diagnostic. Save model/optimizer/RNG at4000/5000/6000. No early endpoint selection, no architecture search, no adjusted thresholds and no additional exposure extension based on this result. CPU-only additional3000 monitoring may contextualize the curve and is explicitly labeled development inference with separately counted cost.

Additional3000 CPU inference completed before A11 launch:96.4844%IID and71.4844%N64/D8 on173M, compared with2500 monitoring94.1406%/60.5469%. CPU inference cost10.442924625seconds; CPU/GPU numerical differences are possible. This is labeled additional development inference, not recovered historical monitoring. The same monitoring shows further moderate-depth acquisition while remaining below the gate.

Gate remains≥98%IID and≥95%N64/D8 under the original policy. Passing would justify consideration of separately registered three-seed strong-record confirmation; it would not itself be confirmation. Failure requires acquisition-boundary inspection rather than another automatic exposure increase. Preserve all1000/2000/3000 failed endpoints and distinguish monitoring from fresh final populations.

A10 cost46.974923630seconds for1000additional updates including≈32seconds of expensive final evaluation; A09's full2000-update replay cost60.396638588seconds. The unchanged3000-additional-update run should fit roughly70–100seconds including final export; reserve180seconds with durable atomic full-process receipt and explicit root GPU release. No new profiling GPU run is needed. Frozen configuration `configs/campaign-a11-record-continuation.json`; independent preflight and source/config freeze precede release.
