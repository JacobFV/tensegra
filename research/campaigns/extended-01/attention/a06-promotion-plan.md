# Prospective A06 promotion decision (no run authorized)

This records decisions before A05-development-v2 outcomes. It is a plan, not a frozen confirmation protocol and not permission to use the GPU.

Promote an informative comparison only after the v2 generator guarantee and leakage/mechanical audits pass and at least one equal-information graph arm reaches95% fresh IID task accuracy in the registered development budget. Retain all strong competitors in confirmation even if a preferred arm fails. If no graph arm acquires IID behavior, stop OOD confirmation and inspect learning/labels/conditioning instead. The95% screen is an acquisition signal, not the98% interface gate for learned composition.

If long-path task accuracy fails while diagnostic routes remain correct, use the already registered frozen content-scale16 intervention to distinguish confidence/sharpness effects from route selection. A repair on this development set may motivate prospective confirmation of that intervention across all arms; it cannot retroactively pass the unchanged learned-scale model. No additional scale search is silently introduced.

Confirmation uses a new A06 namespace, proposed initialization seeds601/602/603, training141,000,000+seed×100,000+step, intermediate monitoring142,000,000, final-only confirmation151,000,000+group×100,000+offset. These are distinct from all A04/v2 development draws and the unrun superseded A05 draft. Fresh confirmation has1024 events per required cell and preserves each seed. Shared events across arms/seeds stay paired and are not counted as independent replicates.

Retain soft, exact-gather/neighbor-attention, keyed neighborhood selector and the no-graph information ablation if profiled cost permits. Gather and masked neighbor attention are algebraically the same comparator. Keyed context carries supplied neighbor addresses and a known-key retrieval prior; it is not a graph-text-only transformer. Primary analysis compares soft with these strong equal-information baselines, not with the no-graph arm.

Separate N, depth, K and joint stress; preserve the v2 group-bijection contract under degree-preserving wrong-topology/correspondence controls. Do not reuse missing/spurious corruption through fixed-K top-k padding. Retain same-graph instruction changes, changed-terminal/answer conditional accuracy, original versus supplied-target agreement, content-null, consistent permutation and mean-head route diagnostics. Report actual hardware time/memory, not allocated-parameter equality alone.

Freeze the final protocol/source after development and a largest-shape resource profile, before confirmation outcomes. Root releases each paired seed. No promotion authorizes autonomous composition or learned execution calibration.
