# A10: bounded saved-state acquisition continuation

Registered before new training outcomes. A09 remains a failed2000-update development endpoint. Root requested the smallest informative exposure follow-up if the same-population trend remains unsaturated. Additional CPU-only development inference of the frozen2000 checkpoint on the173M monitoring population yields87.1094% IID and50.3906% N64/D8, versus73.4375% and33.5938% at1500. These are+13.67/+16.80points; CPU numerical differences are possible. Inference cost8.634655571CPU seconds. Mean training loss fell from.330986865 at1401–1500 to.136746169 at1901–2000 (58.69%). These monitoring/loss signals justify bounded continued acquisition; final191M outcomes are not used for selection.

Continue exactly1000additional updates,2000→3000, from saved A09 model/AdamW/CPU+CUDA RNG state, preserving width1024, architecture, learning rate, weight decay, initialization, training seed and step-dependent example sequence. SHA256 of training-state-02000.pt is `efe14ec128c52124222cc346eb94f5467394d82bc3c7f91d70075a01007c92c8`; loaded model tensor SHA256 must equal `1ec3253d97ebc4b88af6787efd3918c8bcd71d99fdd346efcdd572caacd2bea0`. The runner rejects mismatched files, model tensors, steps or training contracts. Do not reevaluate step2000 after restoration, since the stored RNG state already follows its final evaluation. Mechanical CPU testing compares resumed model, AdamW tensors and RNG against uninterrupted execution.

Monitor2500 using the unchanged173M populations. Final3000 development uses fresh201M population,512events, all seven prior conditions and unchanged diagnostics. Save model/optimizer/RNG at2500/3000; preserve original A09 artifacts. This is one exploratory seed and no architecture search. Gate remains≥98%IID and≥95%N64/D8 under original policy. If acquired, propose separately registered matched-seed comparison; if underacquired, report that outcome and inspect acquisition boundaries before proposing anything further. No automatic further exposure increase or attention-superiority claim.

Estimate45–70seconds including expensive joint final cells; reserve cap120seconds, explicit root release required. Full process receipt must persist atomically remotely. CPU tests/inference counted separately. Frozen config is `configs/campaign-a10-record-continuation.json`; final source/config freeze and independent preflight precede launch. No GPU launch authorized by this registration itself.
