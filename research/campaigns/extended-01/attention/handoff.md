# Attention track handoff

Source frozen for first profile:53aa9f6. Five mechanical tests pass on GB10 CPUenv in0.58s (width32 fixtures only). Primary width1024.

Remote immutable snapshot `/tmp/campaign-a01-source-53aa9f6`; CUDAenv `~/topoformer-stage8-cuda/bin/python`. Root is sole GPU queue owner. No attention GPU process launched yet. Profile pending R01release; config `configs/campaign-a01-profile.json`,12updates,64examples×2cells at0/12. Profile output must be new immutable dir.

Next: obtain profile slot, profile softarm, report walltime/memory and conservative five-arm500update estimate. Development source remains same; mode/config variation only. Root/reviewer approved direction. Keep strong context identityprior and MP. Hard/MPcleanidentical bydesign. No future/goldtargetsforward.

A01 readout value correctness may maskwrongnode; mean-head argmaxroute diagnostic stored alongside exact clean pointer targets. This diagnostic is not causal proof. Context multisuccessor corruption retainsindividualneighboraddresses (53aa9f6fix). Alloldfilesunchanged.
