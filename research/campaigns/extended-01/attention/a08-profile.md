# A08 recovered profile

Source e789147 ran before the credit interruption. On resume, completed public/prediction archives and manifest were recovered from `/tmp/campaign-a08-profile`; no attention process was active. Frozen checkpoint tensor hashes match exactly and optimizer updates are zero.

64 largest-shape events × four corruption fractions took 2.573 seconds inside the experiment. The outer process receipt was not recovered. Charge the full reserved 60-second cap conservatively; do not present this upper bound as measured occupancy. CUDA peak 434,488,832 bytes; process peak RSS 1,653,048 KiB.

Clean task and supplied-corrupt-pointer agreement are 100%. Clean-answer accuracy under 12.5%, 25%, and 50% replacement is 7.81%, 14.06%, and 12.5%; every prediction still agrees with exact traversal of the supplied corrupt graph. These 64 profile examples are not the main population or a general robustness claim.

Main estimate 150–220 seconds; retain cap300 including exports. It remains inference-only over all nine frozen checkpoints and1024 fresh events at each registered shape. Await explicit root scheduling release. No checkpoint or scale selection follows these outcomes.
