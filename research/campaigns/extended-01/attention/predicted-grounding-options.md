# Optional next branch: common predicted correspondence

Planning only. Do not promote before A03 evidence and a new registered protocol.

The narrow extension would remove explicit token/runtime row correspondence while retaining immutable public identity observations. Independently permute runtime entities and token rows. Runtime keys are E; token identity observations are U=E·R+noise, where R is a fixed cross-view transform shared across the population. Keys and graphs are fresh. The hidden permutation supplies training targets only, never forward inputs. A per-episode unknown arbitrary R without public anchors would be unidentifiable and is excluded.

Train separate normalized query-side and key-side matchers under explicit privileged correspondence labels. Freeze one common learned matcher, then derive B=Pq A Pkᵀ. All downstream interfaces receive the SAME predicted correspondence: attention bias receives B; message passing receives normalized B; address-context retrieval receives the weighted address list induced by B. Oracle correspondence is a separately labeled supplied upper bound.

Initially freeze A03 value-processing backbones. This isolates correspondence quality from changes to payload dynamics. Matching uses immutable identity features; it does not establish evolving latent semantic identification, language understanding or learned planning. A hard-routing comparator must explicitly choose a top predicted edge; treating every positive entry of a soft B as a hard edge would silently produce a complete graph.

Possible development controls are no-noise and declared observation noise, a consistently renamed/permuted key population, and an out-of-domain noise level. Noisy identities may be genuinely ambiguous; oracle labels reveal the constructed entity but do not make every realization inferentially identifiable. Match calibration, entity accuracy, complete route diagnostics, task accuracy and risk/coverage should be separate. A calibrated match posterior is not automatically a calibrated full-path probability.

This would test how common grounding errors interact with routing interfaces. It must not be interpreted as an architecture comparison if different arms receive different predictors. If all explicit interfaces are already at ceiling, a no-noise matcher demonstration alone may add little knowledge; uncertainty or support-size scaling must provide an informative question before spending confirmation budget.
