# A13 timing and contract profile

Frozen53bdfed completed32 largest-shape events×four policies. Full-process occupancy8.036676807seconds; internal6.634384889seconds. Instrumented forwards total6.308127374seconds, projecting100.93seconds for512joint events. With smaller shapes, export and variation, main estimate115–140seconds; retaincap180. Keep all four policies and existing main config unchanged; separate root release follows independent profile audit.

Input/output tensor hashes are identical, optimizer updates zero, and all three shared policies have exactly zero cross-head address-distribution discrepancy. Shared-hard and oracle-common local own-argmax payload mixing errors are zero. CUDA peak1,081,848,320bytes. Query-trajectory annotations remain separated from all-node averages.

Profile task counts are7/7/31/32 of32 for unchanged/shared-soft/shared-hard/oracle-common; complete suffix counts0/0/28/32. Queried mean-route full-path correctness is31/32 for the nonoracle policies and32/32 for the privileged oracle. Original destination-head full-path correctness is19/256head-events, using the same32-step queried support. These are small timing-population observations only, not the main result, a learned gate promotion or a basis for policy selection. Oracle routing is explicitly privileged.

Raw/receipt archive27a977f at `research/results/campaign-01/attention/a13-profile`; GPU verified free immediately after completion. Attention accounting1538.121903481seconds includes A08 profile's uncertain60-second upper bound. Main remains unlaunched pending separate release.
