# A09: predeclared graph-record exposure extension

New exploratory experiment, preserving failed A07. The registered trigger is verified on the actual monitoring population: CPU inference of the frozen1000-update checkpoint yields IID43.75% versus22.66% at500 (+21.09points), N64/D8 18.75% versus11.72% (+7.03points). Both exceed5points; final100training losses .9118 versus1.2119 at701–800 improve24.8%, exceeding10%. The CPU re-evaluation is labeled inference, not archived metric reconstruction; minor CPU/GPU numerical differences are possible. Targets/populations match. This replaces the earlier weaker curve-versus-final-population comparison as the extension justification.

Replay the original seed701, same model, initialization, training examples, optimizer and monitoring through1000, then continue to2000updates. Original optimizer/RNG states were not archived, so the whole prefix is recomputed and charged. Require an exact model tensor hash match with the original checkpoint at step1000 **before** continuation. Also compare fresh side-by-side logits from replay and archived checkpoint under identical edge-order RNG. Historical floating logits were not saved; this is a new exact checkpoint replay test, not a claim to reconstruct unavailable archived logits. Abort on mismatch rather than silently proceeding with a different prefix.

Save optimizer, CPU/CUDA RNG, model, config and step at1000/1500/2000 after monitoring so subsequent continuations can avoid replay. These checkpoint writes and guards do not modify the learning objective or parameter updates. No architectural or scale change. Monitoring uses the original173M populations; the final512-event development population uses new191M constructions, with unchanged seven conditions including the prospectively declared all-record-logit×2/address16 diagnostic. This remains exploratory, not confirmation.

Primary question: does another1000updates acquire the interface while learning curves are still improving? Preserve exact task, suffix, path and loss separately. Competence requires≥98%IID and≥95%N64/D8 under the original policy. If acquired, register matched3seed confirmation with strong A06 interfaces; if not, report this failed extension and inspect a one-hop/source-relation/content acquisition boundary. No automatic further exposure increase is authorized by this registry.

Based on A07's50.29seconds including expensive final evaluation, estimate65–100seconds for2000updates and extra state checkpoints, cap180seconds. Full replay is counted. Root release required; no GPU launched on registration. Frozen source/config/checkpoint hashes and a prefix-replay receipt will be archived. Retain the stronger supplied-neighborhood baselines; underacquired global record attention alone cannot establish universal programmable-attention superiority.
