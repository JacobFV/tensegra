# A05b: frozen keyed-address concentration diagnostic

Registered after A05-development-v2 and before these outcomes. V2 learned content selection, but at N128/D32/K8 the keyed neighborhood selector reaches21.88% task accuracy, versus52.34% with frozen content-scale16. Exact gather reaches100% under that same content intervention. This motivates isolating the additional finite address-read distribution rather than advertising a soft-bias advantage over an avoidably weak keyed baseline.

Load the unchanged seed501 context checkpoint SHA256 `0fc8976545d14b302c6a55e36c10d7b351daf8e9a0f75249a2ec09235642d5ab`. No optimizer steps or parameter updates. On512 new block-permutation-v2 graphs at N128/D32/K8, evaluate the four combinations:

| Address score scale | Content score scale |
|---|---|
| Learned checkpoint value (near8) | Learned checkpoint value (near8) |
| Learned checkpoint value | Fixed16 |
| Fixed16 | Learned checkpoint value |
| Fixed16 | Fixed16 |

The address scale controls cosine retrieval of public immutable node keys. Its query/key transforms were initialized in the supplied identity coordinate system and remained trainable. The content scale controls learned matching of desired attribute codes to retrieved attributes. Address sharpening changes both retrieved payloads and retrieved attribute representations; any improvement localizes the address-concentration path, not solely a downstream value read or an unchanged content matcher. Neither intervention changes graph strengthλ, graph edges, parameters, targets or the programmed reverse schedule. The exact-gather comparator remains the stronger supplied address-access reference; improving context does not establish an exclusive metric-programming advantage.

Use fresh development seed102,000,000 with identical events across all four cells. These are not confirmation constructions. Retain original and intervention task, all-node, complete suffix, mean-head route, selected-mass, latency and paired correct/wrong transitions. Verify initial/final tensor hashes match, record literal overrides, source/config/input-checkpoint hashes and preserve all four outcomes. No threshold fitting or hidden reset occurs.

This is a bounded inference-only diagnostic. Prior largest-shape timing suggests10–20seconds including startup and export; request a60second process ceiling. No extra profile run is needed for the already profiled architecture/shape. Root must release the GPU after source/tests/reviewer preflight. A successful development repair can enter a separately frozen three-seed confirmation; it cannot revise the original A05-v2 scores.
