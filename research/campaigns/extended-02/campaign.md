# Extended-02: learned computation allocation

Start: 2026-09-24T16:13:17Z. Baseline65d44ae6acb17cd53360e5350f9c297b0c5954d0. Root is the sole experiment/GPU coordinator. Main and all historical campaign bytes remain immutable during work; additions live on campaign/extended-02.

Question: can learned closed-loop policies and population selection improve verified workshop outcomes per resource over strong heuristic and independently trained alternatives, and transfer to new primitive combinations and resource conditions?

Current phase: A, protocol/environment acquisition and independent design review. No models launched. GB10 verified idle, CUDA2.14.0+cu130/Python3.12.3,122502MiB shared device capacity reported;281GB filesystem free. Existing environment reused.

## Boundaries
Semantic reduction, exact executor correctness, multiple-return addressing, metareasoning, composition, population advantage, and structural attention are separate claims. Typed structured observations first; no gold action sequence at evaluation. Exact payload storage is supplied; learned address selection is evaluated. Default experimental neural workspace width1024; lightweight control may have explicitly declared simpler computation, not a secretly narrow workspace. No pretrained models, external services, or candidate filesystem/network access.

## Sequence and allocation
1. Audited typed API, small workshop, independent validators, public-information references and leverage profile.
2. Supervised bootstrap plus closed-loop policy learning, lightweight versus recurrent controller; cold training diagnostic where feasible.
3. Six-member checkpoint population with explicit inheritance/mutation against equal-exposure multistart and conventional learner. Three independent population processes for any replicated evolution claim.
4. Resource/curriculum shifts, fresh construction/composition tests, return/tool interventions. Conditional attention and relation-prediction branches only after useful question/profiling.
5. Freeze best few comparisons; fresh512+ episodes/cell, all three lineages, audits/curves/closure.

Initial provisional allocation: protocol/referenceCPU4core-hours; policy developmentGPU3h/CPU12h; population comparisonGPU3h/CPU12h; transfer/interfacesGPU1.6h/CPU6.4h; reserveGPU2.4h/CPU9.6h; unallocatedGPU2h/CPU4h. These are ceilings within12GPUh/48CPUcoreh, not launch commitments. Major allocations require measured profile and registered protocol. Deadline2026-09-25T16:13:17Z.

## Decision discipline
Each failed development result selects a minimal discriminating follow-up. No speculative factorial sweep. Strong references may win. Confirmation never selects recipes. Costs include generation, teacher/selection episodes, solvers, failed jobs, inference and export. CPU user+system across process trees is separate from GPU occupancy and wall time. No blanket promotion from one interface to another.
