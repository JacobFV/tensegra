# RL01 prospective relation diagnostic review

Static source/contract/test review at `d8444291`, with historical field-loss, codec, actor and S22 controller dependencies inspected. No jobs launched and no outcomes inspected.

**No blocking objective or privileged-boundary mismatch identified.** `all_records` delegates to the historical objective. `relation_only` retains eight-field denominator, keeps source/target/role/slot losses only on EDGE records, zeros kind/value/copy, and restricts type loss to EDGE/EOS. This avoids silently scaling the four edge-field losses by8/5. The type term's normalization nevertheless changes from all records to EDGE/EOS, deliberately giving those positions the full type-term allocation; the comparison is objective reallocation, not equal gradient magnitude.

Both arms receive teacher-forced complete gold sequences in training. Local teacher-forced tuple accuracy also conditions on prior gold edges and must stay separate from free generation. The free controller receives only `NodePrefix`: correct node count/kinds/values/copy links. It forcibly supplies those node records, suppresses later NODE tags and lets the model choose EDGE/EOS and relation fields. No gold edge count, slot or stopping position was found on that path. This is privileged relation acquisition conditional on correct nodes, not public-text complete graph induction. Earlier teacher-forced evaluation creates local caches; free decoding initializes its own cache, with no retained gold-edge KV path identified.

Parent file SHA, tensor hash, broad arm/update4096/seed2101 metadata are enforced. Both copies reset AdamW and share initialization, fixed stream/order, exposure, architecture and prescribed endpoint. One inherited parent lineage remains one lineage. Reading archived parent metadata is not proof the remote checkpoint exists; root must verify the bound file before launch.

Generation uses the existing bounded generator, independent answer validator and alpha equivalence. Train1024, known512 and omitted512 are disjoint via a shared seen set; prepare recomputes alpha hashes and rejects duplicate/cross-split examples. The exclusion inventory and later S21 rows are hash pinned, but **configuration completeness remains external**: a nonempty exclusion_sources list alone does not prove every required historical split was listed. Audit its inventory against the S21 archive before generation. Unknown unarchived constructions remain a disclosed limit. Seed inequality alone is not the semantic split.

Profile subsets are selected by public token length, not outcome, and use the same1024-wide actor. Main always evaluates full512 panels at0/256/1024. Both training and greedy evaluation/export need to fit the combined budget; profile per-arm and checkpoint0 decoding since poor models can run to the160-record cap. The fixed main exposure cannot be shortened post-outcome and relabeled the same experiment. Source dictionaries bind dependencies; checkpoint/data hashes belong in the frozen resolved config.

The >=52/512 omitted complete / >=26 advantage / <=26 known-loss signal is an exploratory promotion rule only. Preserve strict invalid-output zero credit and per-field/local metrics if whole graphs remain wrong. Do not count local teacher-forced edges as additional independent examples. Confidence calibration is unmeasured because compact argmax records omit logits. This branch tests loss support on a supplied-node boundary, not programmed attention or agent scheduling.

E04 pricing clarification received from root: all public `compute_price` cells are0, so different neural/reference tariffs do not change modeled E04 utility. Actual CPU/GPU and latency remain separate comparisons; this does not yet test price-sensitive compute allocation.
