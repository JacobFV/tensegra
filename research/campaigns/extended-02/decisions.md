# Decisions

-2026-09-24: verified clean baseline65d44ae6 and idle GB10; reuse coordinator, additive worktrees. Read prior claim map and independent receipts; no historical reruns.
-Structured observations isolate orchestration from failed text transfer. Public references must establish solvability and cost leverage before training. Six-memberPBT versus matched multistart is the preferred decisive evolutionary comparison.

-First actual-environment profile: lightweight2.21M/recurrent26.36M, bothwidth1024;3updates12teacherworlds each succeeded mechanically. PeakCUDA0.168/0.673GB. No learningclaim at3updates. Register600updatepaired acquisition before population. Profile reference percallspawn~0.6s motivates tested persistentexecutor; current exactselection task is feasibility, not nonconstantobjective optimization.

## E03 acquisition / E02 budget diagnosis
Both1024-width controllers acquire the easy teacher sequence (128/128 shared DEV; one seed). E04 frozen transfer runs with identical48-step caps and compute_price0, actualcost separate. Profile8/cell shows recurrent sizefailure, lightweight tightbudget/obstaclefailure; no recipe changed before fullDEV.
E02 fresh512 2x2: original tight138, fallback455, remainingbudget501, both501; standard512all. Use remaining-budget public catalogue for future resource-learning versions; retain original failures. OneCLI launch failed missing --config and is charged. Nine population runtime smoke fixtures pass, including resume/state inheritance.

## E05 input contract diagnosis
Frozen600-update lightweight mixedbootstrap ends57/128 DEV, witherratic100-stepcurve. Recurrentmatched600continuesunchanged. Exactcounterexamples atfrozena9337b71 prove equal completeencodedhistories/candidateinputs butdifferent publicteacher actionkinds. ThisdoesnotexplainallDEVerrors ormake taskunsolvable; it rulesoutperfectimitation underthatencoding. Pause largepopulationoptimization ofthisrecipe.
VersionedE06inputrepair: typedpublic relationalmemory, sameinputs tobothfamilies, no solvedgreedy/pathfeatures; stableopaquehandle equality, explicitorderededges/parentrefs, overflowfails, scalarbytecode+numericfeatures. Keyedcandidatebinding is suppliedaddressprior andwillbedisclosed. Ordinarycontext consumerfirst; zero-memory matchedparameterablation. Counterfactualfit andprofile precedefulltraining. This is aninputcontract intervention, notprogrammableattentionevidence.

E05 final recurrent46/128 versus lightweight57/128. Independent raw audit finds repeated budget16 timeouts dominate57/76 failed episodes; all submitted reductions remain correct. Do not attribute these errors to missing relational inputs without intervention. A separate public-state budget-label audit will localize escalation acquisition. RL01 profile24.17s; conservative full-study estimate625s fits780s cap, so paired fixed1024-update study launched.

## Closure at user request
Stop new experiments to conserve credits. Complete source fixes, collect receipts, render existing curves, run bounded CPU verification and independent closure review. Defer E06 acquisition/main populations/confirmation explicitly; do not label them failed or passed.

## Continuation (phase 2) — 2026-09-24, resumed by user instruction after closure
- User instructed resumption "to completion" after the credit-conservation closure. The original budget window (start 16:13:17Z, deadline 2026-09-25T16:13:17Z) and ceilings stay in force; spent GPU/CPU carries over. Verified 18:28Z: no campaign processes on gb10-direct, GPU idle, tree clean at 49cece64; 90 tests pass on GB10.
- Closed phase-1 results are not rewritten. Phase-2 experiments are E07+ with new output roots.
- E07 (input contract, 2x2): exact E05 recipe/seeds/DEV panel; only interface varies: {legacy summaries, public-tree memory} x {features v1, v2}. v2 = v1 prefix + supplied public relational facts (log budgets, budget vs. prior attempts on the same draft, timeout dominance, item conflict counts incl. non-pending, within-category public cost rank, one-step route lookahead). No solved subset/path/feasibility label or teacher action. Disclosed as a supplied input repair, not learned relation discovery.
- E07 legacy-v1 reproduced E05 exactly (57/128 final) under the new source: determinism check for the v1 path.
- E08 design profile (not evidence): small PBT, 2 supervised + 2 actor-critic rounds, six diverse initial (lr, entropy) rows, to check whether RL fine-tuning helps or collapses before registering the main population comparison.
- E07 results (DEV 128, E05 panel; final 600-update endpoint): legacy-v1 57/128 (reproduces E05), **legacy-v2 119/128**, recurrent-v2 116/128 (vs phase-1 recurrent-v1 46/128). Memory arms hit the 2400 s wall cap after 300/600 updates (v1 0.891, v2 0.875 at 300 vs legacy-v2 0.922 at 300); ~10x slower per update. The input repair that mattered for budget acquisition is the supplied relational feature set; lossless memory did not outperform it at matched updates. Memory interface deprioritized (cost), not refuted.
- E08 profile (actor-critic v1): collapse in the first RL round (5/6 members -> 0.0; absorbing abstain; untrained critic; summed entropy bonus). Registered actor-critic v2: decision-mean, per-batch standardized advantages, KL(round-start || current) trust region. RL design profile: stable 0.92-1.0 over 150 updates for lr 3e-6..3e-4.
- Leverage profile (dev seeds 450M): teacher 0.93-1.0 on tool-valid conditions; bootstrap 0.80 vs RL 0.97 on in-distribution 4x4; RL overspends on obstacle+expensive (util .887 vs teacher .947); tight64 .78-.82 vs .96; learned policies keep calling an invalid solver on 25-item worlds while the teacher falls back to greedy. -> E08 RL/selection uses a harder leverage-region mixture; held-out transfer set fixed before main outcomes.
- GPU accounting for phase 2: device occupancy = union of GPU-job wall intervals on the single GB10 (concurrent jobs share one device). The per-process wall sum (phase-1 convention) is reported alongside as an upper bound. Changed because 5-9 concurrent small jobs would otherwise charge 5-9x real device time.
- E08 main registered (phase2/E08-protocol.md) and launched: 3 modes x 3 independent replicates, bank import, 5 RL rounds x 60 updates.
- E11 (exploratory, one seed per family): lightweight vs recurrent single-learner RL from their E07 v2 bootstraps (identical supervised stream), identical RL stream/hyperparameters (row 0) and E08 mixture. Tests whether the 12x-parameter recurrent workspace earns its compute under RL.
- E12 (adaptive vs fixed curriculum): E08 PBT configs plus curriculum_mutation (per-member training-mixture weights, inherited with one component x0.5/x2 on replacement, separate RNG stream). DEV selection anchor unchanged (fixed uniform mixture), paired with E08 PBT per replicate.
- RL02 (semantic branch, bounded): pointer/common-address source/target head over supplied NODE records vs re-run RL01 all-record comparator; design and pre-registered promotion rule in semantics/RL02-design.md (implemented by a delegated worker; reviewed by root). Remote CPU tests 18/18. Profile 36.9 s. Job wall caps raised from design 120/780 s to 900/2400 s solely because the GPU is shared with concurrent jobs; no protocol change.
- RL02 result: promotion rule fails (omitted 0/512 both arms). Pointer improves known retention (501 vs 489) and omitted teacher-forced tuples (+1,642), but free decoding degenerates into duplicate edges (351/512). Semantic branch closed for this campaign; no automatic extension.
