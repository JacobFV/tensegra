# Decisions

-2026-09-24: verified clean baseline65d44ae6 and idle GB10; reuse coordinator, additive worktrees. Read prior claim map and independent receipts; no historical reruns.
-Structured observations isolate orchestration from failed text transfer. Public references must establish solvability and cost leverage before training. Six-memberPBT versus matched multistart is the preferred decisive evolutionary comparison.

-First actual-environment profile: lightweight2.21M/recurrent26.36M, bothwidth1024;3updates12teacherworlds each succeeded mechanically. PeakCUDA0.168/0.673GB. No learningclaim at3updates. Register600updatepaired acquisition before population. Profile reference percallspawn~0.6s motivates tested persistentexecutor; current exactselection task is feasibility, not nonconstantobjective optimization.

## E03 acquisition / E02 budget diagnosis
Both1024-width controllers acquire the easy teacher sequence (128/128 shared DEV; one seed). E04 frozen transfer runs with identical48-step caps and compute_price0, actualcost separate. Profile8/cell shows recurrent sizefailure, lightweight tightbudget/obstaclefailure; no recipe changed before fullDEV.
E02 fresh512 2x2: original tight138, fallback455, remainingbudget501, both501; standard512all. Use remaining-budget public catalogue for future resource-learning versions; retain original failures. OneCLI launch failed missing --config and is charged. Nine population runtime smoke fixtures pass, including resume/state inheritance.

## E05 input contract diagnosis
Frozen600-update lightweight mixedbootstrap ends57/128 DEV, witherratic100-stepcurve. Recurrentmatched600continuesunchanged. Exactcounterexamples atfrozena9337b71 prove equal completeencodedhistories/candidateinputs butdifferent publicteacher actionkinds. ThisdoesnotexplainallDEVerrors ormake taskunsolvable; it rulesoutperfectimitation underthatencoding. Pause largepopulationoptimization ofthisrecipe.
VersionedE06inputrepair: typedpublic relationalmemory, sameinputs tobothfamilies, no solvedgreedy/pathfeatures; stableopaquehandle equality, explicitorderededges/parentrefs, overflowfails, scalarbytecode+numericfeatures. Keyedcandidatebinding is suppliedaddressprior andwillbedisclosed. Ordinarycontext consumerfirst; zero-memory matchedparameterablation. Counterfactualfit andprofile precedefulltraining. This is aninputcontract intervention, notprogrammableattentionevidence.

E05 final recurrent46/128 versus lightweight57/128. Independent raw audit finds repeated budget16 timeouts dominate57/76 failed episodes; all submitted reductions remain correct. Do not attribute these errors to missing relational inputs without intervention. A separate public-state budget-label audit will localize escalation acquisition. RL01 profile24.17s; conservative full-study estimate625s fits780s cap, so paired fixed1024-update study launched.

## Closure at user request
Stop new experiments to conserve credits. Complete source fixes, collect receipts, render existing curves, run bounded CPU verification and independent closure review. Defer E06 acquisition/main populations/confirmation explicitly; do not label them failed or passed.

## Continuation (phase 2) — 2026-09-24, resumed by user instruction after closure
- User instructed resumption "to completion" after the credit-conservation closure. The original budget window (start 16:13:17Z, deadline 2026-09-25T16:13:17Z) and ceilings stay in force; spent GPU/CPU carries over. Verified 18:28Z: no campaign processes on gb10-direct, GPU idle, tree clean at 49cece64; 90 tests pass on GB10.
- Closed phase-1 results are not rewritten. Phase-2 experiments are E07+ with new output roots.
- E07 (input contract, 2x2): exact E05 recipe/seeds/DEV panel; only interface varies: {legacy summaries, public-tree memory} x {features v1, v2}. v2 = v1 prefix + supplied public relational facts (log budgets, budget vs. prior attempts on the same draft, timeout dominance, item conflict counts incl. non-pending, within-category public cost rank, one-step route lookahead). No solved subset/path/feasibility label or teacher action. Disclosed as a supplied input repair, not learned relation discovery.
- E07 legacy-v1 reproduced E05 exactly (57/128 final) under the new source: determinism check for the v1 path.
- E08 design profile (not evidence): small PBT, 2 supervised + 2 actor-critic rounds, six diverse initial (lr, entropy) rows, to check whether RL fine-tuning helps or collapses before registering the main population comparison.
- E07 results (DEV 128, E05 panel; final 600-update endpoint): legacy-v1 57/128 (reproduces E05), **legacy-v2 119/128**, recurrent-v2 116/128 (vs phase-1 recurrent-v1 46/128). Memory arms hit the 2400 s wall cap after 300/600 updates (v1 0.891, v2 0.875 at 300 vs legacy-v2 0.922 at 300); ~10x slower per update. The input repair that mattered for budget acquisition is the supplied relational feature set; lossless memory did not outperform it at matched updates. Memory interface deprioritized (cost), not refuted.
- E08 profile (actor-critic v1): collapse in the first RL round (5/6 members -> 0.0; absorbing abstain; untrained critic; summed entropy bonus). Registered actor-critic v2: decision-mean, per-batch standardized advantages, KL(round-start || current) trust region. RL design profile: stable 0.92-1.0 over 150 updates for lr 3e-6..3e-4.
- Leverage profile (dev seeds 450M): teacher 0.93-1.0 on tool-valid conditions; bootstrap 0.80 vs RL 0.97 on in-distribution 4x4; RL overspends on obstacle+expensive (util .887 vs teacher .947); tight64 .78-.82 vs .96; learned policies keep calling an invalid solver on 25-item worlds while the teacher falls back to greedy. -> E08 RL/selection uses a harder leverage-region mixture; held-out transfer set fixed before main outcomes.
- GPU accounting for phase 2: device occupancy = union of GPU-job wall intervals on the single GB10 (concurrent jobs share one device). The per-process wall sum (phase-1 convention) is reported alongside as an upper bound. Changed because 5-9 concurrent small jobs would otherwise charge 5-9x real device time.
- E08 main registered (phase2/E08-protocol.md) and launched: 3 modes x 3 independent replicates, bank import, 5 RL rounds x 60 updates.
- E11 (exploratory, one seed per family): lightweight vs recurrent single-learner RL from their E07 v2 bootstraps (identical supervised stream), identical RL stream/hyperparameters (row 0) and E08 mixture. Tests whether the 12x-parameter recurrent workspace earns its compute under RL.
- E12 (adaptive vs fixed curriculum): E08 PBT configs plus curriculum_mutation (per-member training-mixture weights, inherited with one component x0.5/x2 on replacement, separate RNG stream). DEV selection anchor unchanged (fixed uniform mixture), paired with E08 PBT per replicate.
- RL02 (semantic branch, bounded): pointer/common-address source/target head over supplied NODE records vs re-run RL01 all-record comparator; design and pre-registered promotion rule in semantics/RL02-design.md (implemented by a delegated worker; reviewed by root). Remote CPU tests 18/18. Profile 36.9 s. Job wall caps raised from design 120/780 s to 900/2400 s solely because the GPU is shared with concurrent jobs; no protocol change.
- RL02 result: promotion rule fails (omitted 0/512 both arms). Pointer improves known retention (501 vs 489) and omitted teacher-forced tuples (+1,642), but free decoding degenerates into duplicate edges (351/512). Semantic branch closed for this campaign; no automatic extension.
- E08 main completed (9/9 exit 0). Finalists frozen via registered rule (DEV utility: pbt .936/.920/.927, multistart .936/.923/.925, single .949/.933/.928).
- E09 v1 sealed jobs FAILED (exit 1) at condition 16/26: JSON list call_budgets for the budget-menu transfer condition was passed to WorldSpec, which requires a tuple. Configuration bug (root's). Failed attempts are retained and charged. Fix: evaluator normalizes JSON call_budgets to tuple; regression test builds every registered sealed condition through the evaluator path. Re-run as e09v2-* with identical frozen finalists/conditions. Partial v1 outputs were not inspected for outcomes.
- Sealed E09 (256 worlds/condition): PBT advantage NOT supported (PBT-multistart +0.0007/-0.0332/+0.0033; pooled -0.0097 [-0.012,-0.007]); single learner best in all 3 replicates; single-r0/r1 exceed the teacher by +0.003 IID (CI excludes 0). Mechanism: finalists bimodal on greedy-first (try direct commit before solver). Tool-first finalists fail with no tools (~0) and on 25-item tool-invalid worlds, and perseverate on corrupted returns. PBT populations converge to tool-first by round 2 in all 3 E08 and all 3 E12 replicates; greedy-first members had lower round-0 DEV utility and were replaced.
- E11: lightweight RL 0.943 IID (greedy-first); recurrent RL collapses to never calling tools (0.771 IID, below the no-tool reference 0.805). E12 adaptive curriculum: no advantage (0.928/0.927/0.928 vs 0.929/0.896/0.928).
- Budget at 21:40Z: CPU 29.4/48 core-h; GPU device occupancy 3.3/12 h (per-process upper bound 24.6 h for phase 2; convention disclosed). E13 registered (phase2/E13-protocol.md) as the last exploratory branch; rest reserved for audit and closure.
- Independent phase-2 audit merged (review/phase2-independent-audit.md): all 8 claims confirmed/qualified; raised step-cap asymmetry (learned arms capped at 48 decisions while three sealed conditions allow 64; references got 64). Disclosed in report; affects learned-vs-reference comparisons on those cells and confounds tool-first 0/256 on the 25-item condition; not learned-vs-learned comparisons.
- E13 result: rule formally met (2/3 greedy-first with no-tools >= .7), substantively populations flipped to tool-avoidance (calls ~0; IID .834/.792/.813). Reported as both.
- Budget at 22:20Z: CPU 30.7/48 core-h; GPU device 3.9/12 h. E14 (prospective confirmation on fresh lineages, fresh sealed seeds 80M+) registered and launched; cost estimate ~2 core-h. Remaining budget after E14 is reserved for closure.
- E14 (fresh lineages, fresh sealed seeds 80M+): PARTIALLY CONFIRMED by registered rule (1/3 all criteria; r1 no-tools 0.699 < 0.70; r0 tool-first; mean gap -0.004). Direct-first lineages match teacher (+0.001, CI includes 0). Report downgraded from "exceeds" to "matches". Phase 2 stops here: main questions at an evidential boundary; remaining budget reserved.

## Phase 3 (continuation) — 2026-09-24 ~23:10Z
- User accepted device-occupancy GPU accounting and asked to continue with further branches, documenting as we go. Ceilings/deadline unchanged (48 CPU core-h, 12 GPU device-h, deadline 2026-09-25T16:13Z). Used at start: CPU 32.3, GPU 4.5.
- Next branches by information value/cost: E15 depth-vs-breadth selection (successive halving, with/without behavioral niche protection) on E08 banks; then E16 new-primitive composition world.
- Run jobs with low concurrency: contention quadrupled CPU per RL update in phase 2.
- E15 implemented: population mode `halving` (survivors inherit eliminated slots sequentially) with optional supplied-niche protection (k>=2 only). 119 CPU tests pass. Registered phase2/E15-protocol.md; six runs (plain/niche x r0-r2) on E08 banks.
- E16 built: modular staged workshop (workshop-modular-v1; select/route/assign over constrained_subset/shortest_path/csp), m1 features (stage-typed candidates; supplied), modular references, evaluator/population wiring; 138 CPU tests pass. Leverage profile (dev seeds 460M): teacher 1.00 on all 15 train/held-out sequences; no-tool greedy 0.36-0.55 whenever assign is involved; hard assign (6x4) budget-bound for all (0.81-0.89), labelled separately. Registered phase2/E16-protocol.md; 3 fresh bootstrap lineages launched.
- E15 runs done (6/6 exit 0; 1,953-2,441 core-s each at 6-way concurrency). Development: halving keeps member 0 (profile-tuned row) and makes it finalist in 5/6; plain finalists greedy-first r0 1.00, r1 0.88, r2 0.00 (mirrors single learners from the same member); niche protection kept direct-first members alive but final DEV-utility pick chose tool-first in 2/3. Sealed evaluation running (e09v2-sealed-e15).
- E16 bootstraps: DEV success non-monotone across 100-update slots (final 0.94/0.84/0.79); latest used per protocol. E16 RL: all three lineages 1.0 DEV on train compositions by round 2. Sealed evaluation running (e16-sealed, seeds 90M+).
- E15 sealed (70M+ worlds): halving-plain IID .933/.936/.929, transfer .894/.852/.818; niche .933/.928/.929. Registered depth rule NOT supported (robust direct-first finalists 1/3 per arm). But halving-plain beats PBT 3/3 (pooled IID +0.0149 [+0.0124,+0.0175], transfer +0.0519) and multistart 3/3 IID (+0.0052 [+0.0033,+0.0071]); below the pre-tuned single learner 3/3 (-0.0060). Halving selected the profile-tuned hyperparameter row (member 0) on its own in 5/6 runs. Niche protection: -0.0024 vs plain (hurt in r1 only). Interpretation: depth-allocating selection > copy-based selection at equal updates; the robust mode still depends on the surviving lineage's trajectory.
- E16 sealed (90M+): RL 1.000 on all IID compositions; held-out R->S .96/1.0/.90, R->A 1.0, A->S .77/.94/.00; triples with S before A .88-1.00, S after A .78-.93 (r0/r1), .00 (r2). Registered rule: compositional transfer SUPPORTED (2/3 lineages pass) with a reproducible failure boundary. Localization: after assign, failing lineages repeatedly apply the CSP return as select ("return type mismatch" 12,195x in rl-r2) until the step limit; same error at lower rate in R->S. Bootstraps weaker (triples down to 0.10). Budget at 00:47Z: CPU 38.1/48, GPU 5.8/12.
- E17 registered (phase2/E17-protocol.md): E16 + 0-2 wrong-typed distractor returns in training worlds. Launched bootstraps.
- E16 endpoints on distractor worlds (0-2 wrong-typed prior returns): collapse even on IID pairs (1.000 -> 0.32-0.38; equal to the zero-distractor fraction). Teacher 1.000. Return binding in E16 controllers = "apply whatever record exists".
- E17 sealed: A->S .773/.941/.000 (E16) -> 1.000/1.000/.805; S-after-A triples -> .82-1.00; distractor worlds 1.000 (r0,r1), .80-1.00 (r2). Registered rule SUPPORTED on the mean reading (+0.36; min lineage .805 >= .8; IID pairs mean .980 >= .95); lineage r2 individually has S->R .824 (new). Localization: r2 perseverates on rejected direct select commits (capacity/funds) instead of switching to the solver, in ~18% of select-containing worlds; not composition-specific.
- Budget at 01:15Z: CPU 39.0/48, GPU 6.2/12. E18 registered (phase2/E18-protocol.md): held-out primitive (assign) — zero-shot vs 120-update adaptation vs scratch. Base lineages launched. After E18: phase-3 independent audit and closure (remaining CPU reserved).
- E18 training done. Base (no-assign) RL endpoints DEV 1.0/1.0/0.82 on their mixture. Adaptation DEV curve (full mixture): after 20 supervised updates adapted .99/.88/.74 vs scratch .15/.24/.51; by 40 updates scratch catches up; at 120 adapted .85/.98/.79 vs scratch .87/1.0/.87. Sealed evaluation launched with registered 120-update endpoints plus zero-shot endpoints and UNREGISTERED descriptive 20-update checkpoints (labelled).
- E18 sealed (assign-containing conditions, 120-update registered endpoint): adapted - scratch +0.018 / +0.461 / -0.191 -> PARTIAL by rule (1/3 >= +0.10; 1/3 <= 0). Zero-shot 0.00-0.14 (non-identifiable as declared). Unregistered descriptive 20-update point: adapted .980/.668/.677 vs scratch .060/.021/.194. Retention (select/route-only) after adaptation .896/.995/.891 vs zero-shot base .993/1.000/.894. Next: phase-3 independent audit, then closure.
- Phase-3 independent audit merged (review/phase3-independent-audit.md): all numbers confirmed; corrections incorporated — E16/E17 rule outcomes labelled by aggregation reading (E16 aggregate 2/3, per-condition 1/3; E17 mean pass, per-lineage fail r2 IID .941), E17 r2 localization magnitudes corrected (13.2% failure; 38-50 rejections/episode), E15 r0 vs PBT CI includes 0; disclosed spec_hash difference across E16/E17 roots (identical worlds) and training-stream reuse between RL lineage 0 and bootstrap lineage 2.
- Phase 3 continued on user instruction ("continue working, don't wait on us"). Registered E19 (same-type distractor diagnostic) and conditional E20 (m2 provenance features + same-type distractor training). 140 CPU tests pass.
- E19 (seeds 96M+): E17 RL endpoints on same-type (wrong-provenance) distractor worlds: mean success .367/.367/.347 (per-condition .27-.54, ~ zero-distractor fraction), below no-tool greedy (.626); teacher 1.000. m1 has no provenance input, so this is an input gap. E20 trigger (<0.9) met; E20 bootstraps launched (m2 + wrong- and same-type distractors).
- E20 sealed: same-type distractor conditions 1.000 in all 3 lineages (E17: .367/.367/.347); no retention loss; r2 S->R perseveration gone. Rule SUPPORTED 3/3 (combined m2 + data repair, not separated). Residual: E20 r2 triples with S after A .809-.852, all failures direct-commit perseveration (37-45 rejections/episode), no return-binding errors.
- Budget at 03:40Z: CPU 42.1/48, GPU 8.2/12. Registered E21 (phase2/E21-protocol.md): E15 halving + E13 robustness-visible selection panel. ~1 core-h. Keep >=1 core-h for final audit/closure.
