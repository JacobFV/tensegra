# Completed Stage 6 controlled study

Frozen producer: `c258785ee6b76f2d492f722e93a1b1539ac6de76`. Artifact-only merger/analyzer: `3434f17`, SHA256 `b3f123edcc4ec57d3aedf8623b65587b55bb79da2160ef313270927f8ec2bcbb`, staged under remote `/tmp`. All three seed jobs exited successfully before merging. The frozen checkout was never modified during analysis.

The completed study has 45 runs (15 variants × seeds 0/1/2), 256 updates per run, 11,520 training rows, 780 evaluation rows and 9,360 evaluation condition-examples. Every declared `economy_v1` cell is present: step-zero minimum/maximum depth anchors, intermediate minimum-depth checkpoints, final depths 4/8/16/32, seven final OOD conditions, and local final anchor interventions plus both privileged oracle protocols. There are 12 examples per seed/cell. This is one paired study partitioned across disjoint seeds, not three independent studies.

Final local policy task accuracy is 1/36, 0/36, 1/36 and 2/36 at depths 4, 8, 16 and 32 (2.78%, 0%, 2.78%, 5.56%; nonzero cells have sample SD 4.81 percentage points across three seeds). Exact protected result and complete trajectory accuracy are zero in every local depth cell. All local examples halt at the minimum two microsteps; each depth has 72 deferred runtime events and no executed events across 36 examples. The conditional task-given-exact-result denominator is zero, so its rate is null.

All 14 variants other than fixed_compute produce the same ordered numeric/decision outputs as local across the 12 final seed×depth cells. Fixed_compute matches 2/12 cells and spends 80 microsteps per example; its task accuracy is 0%, 5.56%, 2.78%, 0% across depths, and complete trajectories remain zero. The occasional protected scalar agreement under fixed_compute does not establish a correct sequence of operations. `prediction-agreement.json` records the comparison population. Backend labels distinguish exact runtime, learned protected values and no runtime; numeric agreement in the learned backend is not exact interpreter invocation.

Privileged `oracle_minimal` executes the correct exact result and complete path in 36/36 examples at both depth 4 and 32, using 8 and 36 microsteps; learned task output is only 1/36 at each depth. Privileged 80-step `oracle_trace` also has 36/36 exact results/paths at both depths, but learned task output is 0/36. These rows are isolated under `privileged_oracles`, not counted as learned policy success.

Frozen event-drop/shuffle/wrong-value, graph permutation/drop, runtime-off and readiness-0.95 interventions have zero paired task-accuracy effect on local at the final anchors. Readiness-0.5 at depth 32 changes task accuracy by −1/36 (mean paired −2.78 pp, SD 4.81 pp). Because unmodified local executes no events in these cells, null event effects do not establish causal use or robustness of returned values. Likewise, identical annealed outputs do not establish transferred symbolic skills. Readiness Brier scores for local are approximately 0.519, 0.530, 0.539 and 0.541 by depth; calibration uses posterior proposal-readiness targets, not task correctness.

The independent lowering audit is archived separately at `../../../stage6-lowering-independent-audit.json` and explained in `../../../stage6-lowering-audit.md`. It covers the 5,940 final primary condition-examples (all variants, four depths plus seven OOD conditions, excluding interventions/oracles). It found 1,215 actual executions, all arithmetically correct for their actual arguments, but zero correct ordered bindings among 1,111 executions with recognized task identities and zero complete correct trajectories. This is a task-lowering failure, not an arithmetic-engine failure; execution-conditioned task claims retain their actual null denominators.

Integrity checks passed for shared frozen sources/git/runtime versions, canonical config and seed-only shard differences, paired initialization/training/evaluation data identities, all row checkpoint-state hashes, and all 225 producer-declared checkpoint file hashes. Each original shard manifest is preserved byte-for-byte under `shards/NNN/`; its raw rows are archived as gzip. The merged manifest records original/config/source/log/checkpoint hashes in `combined_from` and the canonical seed union. `archive-manifest.json` verifies local archive hashes. Checkpoint binaries remain remote; independently verified hashes and byte counts are retained.

`summary.json` is the full compact-cell machine report (no copied per-example traces), `key-results.json` selects important metrics, `runtime-cost.json` preserves function timing populations, and `failure-examples.json` contains three explicitly scoped trace excerpts. `metrics.jsonl.gz` and the per-shard gzip files preserve raw evidence. `figures/` contains standalone heatmaps, loss/weight curves, calibration, task/compute depth curves and policy-versus-oracle figures. The supplemental plotting script only reads the summary. Primary methods/results belong in the repository's Stage 6 report.

The three concurrently run shard elapsed times sum to 4,860.28 seconds; this is not parallel wall time or CPU time. Recorded training-function wall times sum to 3,901.44 seconds and evaluation/training function wall times together sum to 4,823.28 seconds, including host contention. The coordinator observed roughly 27 minutes of parallel elapsed time. No training, inference or model loading was performed by the analyzer.
