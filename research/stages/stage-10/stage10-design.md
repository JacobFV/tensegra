# Stage 10: identity invariance, readout consistency, and edge acquisition

Baseline `60ad5ee`; dedicated branch `feat/stage10-interface-contracts`. Preserve every Stage1–9 source/result/report byte. Standard workspace width1024. This stage follows the user's three specific diagnoses; it does not assume a universal failure of recurrent computation.

## Scope and hypotheses

A. Observation identity addresses a ledger; content determines compatibility. Compare protected content-only matching (ID features excluded from semantic computation) with raw-ID matching trained under independent per-episode bijective renaming and the correlated-ID training reference. The public empty-ledger prior is supplied in every arm. Programmed invariance and learned invariance are distinct. Evaluate all frames, particularly intermediate retractions, same-ID duplicates, distinct-ID equal content, renamings and unseen ID/content combinations. No new null head.

B. Information decodable at each delay need not admit one time-independent decoder. On the same frozen Stage9 backbones, train equal-capacity scalar decoders at each source delay and evaluate every target delay on the same held-out events. Then compare one shared-delay decoder with a short-delay pooled reference. Preserve scalar exact accuracy, non-value metrics, and full joint outcomes separately. All included training delays are in-distribution for that decoder. No encoder/backbone changes, copying, lifecycle training or returned-fact-use tasks.

C. Inspect the five remaining argument-edge mistakes using labels, relation margins, gradients and available trajectories. Use a single representative slot configuration because the edge head is independent. Predeclare a bounded paired fitting study after diagnosis; additional updates are a new experiment, never a retrospective Stage9 gate pass. Report raw and training-calibrated results. Eight privileged fixed graphs test fitting, not language understanding or generalization. No public-text sweep.

## Protocol and budgets

Track-specific protocols specify observables, privileged targets, supplied versus learned mechanisms, validation-only selection, immutable source/config freeze, untouched test sets, exposure, stop rules and metrics before main execution. New training initialization seeds20/21/22 are distinct from development seeds101/102. Frozen Stage9 backbones retain their actual seeds10/11/12. Fresh data uses the10000000-series namespace with explicit split/event hashes. Require512 examples per meaningful belief/return validation/test cell; tiny graph fitting support is disclosed as8 and cannot satisfy a transfer gate.

Retain a conservative **180 GPU-minute ceiling**, with caps A45, B35, C30, reserve70. Expected use is much lower; these are caps, not target expenditure. Profile before main freeze. Root serializes GPU jobs and authorizes each release. No worker launches training before release. Record job time, optimizer presentations, distinct examples, parameters, token allocation, CUDA allocation and RSS separately. Existing GB10 environment only; no dependency upgrades or local heavy training.

Preflight: SSHgb10-direct resolves to promaxgb10-4dfb;119GiB RAM,115GiB available,462GB disk available, no active GPU process. Existing ~/topoformer-stage8-cuda Python is retained. More precise process/memory measurements accompany profiles.

## Gates and stopping

Historical gates remain unchanged. Belief posterior thresholds retain unique accuracy>.98IID/>.95OOD, mean frameL1<.05 and impossible mass<.01, initial frame included, every required seed/cell. Report architectural renaming equality separately from acquired matching.

Return matrix is diagnostic. A shared decoder may improve scalar access without passing full return retention: at16, type/primitive>.99 and exact value/required identities>.98 per seed/condition remain required. No marginal-product estimates. Missing unary arg1 excluded only from its required operand denominator.

Graph fixed-fit gate requires8/8 complete graphs for every prescribed seed at the predeclared checkpoint/decoder policy. Failing the bounded fitting budget stops the track; no iterative search for a passing recipe. Passing is restricted acquisition, not held-out semantic transfer.

No successful diagnostic authorizes autonomous composition, supervision withdrawal, pretrained integration or additional runtime capabilities. Finish with an evidence-based recommendation.

## Claim-specific prerequisites

- A role-separation pass supports bookkeeping-label invariance in this public ledger task. It does not establish retraction learning in arbitrary latent recurrence or open-world probability calibration.
- B cross-delay decoding tests accessibility and shared readout geometry. Full event retention is relevant to this proposed runtime-return interface, not a universal prerequisite for demonstrating topology-biased attention on relational tasks.
- C privileged edge fitting tests decoder acquisition/calibration. Public text requires a separate surface-to-graph experiment with observable targets and adequate semantic diversity.
- None of A/B/C is a direct intervention on programmable QK geometry. Prior topology experiments retain their own scope; these studies neither replace nor invalidate them.

Independent raw-metric/provenance review, full regression tests and historical-byte verification precede merge/push. Use research/stages/stage-10, research/results/stage10 and research/tools only. Keep all failures and selection decisions.

## Profiled releases

Semantic profile:30 warm updates in0.566 training seconds/1.311 wall seconds; main was frozen at1200 updates ×3 seeds with five-minute cap. Completed in49.83 wall seconds; no added semantic experiment follows its restricted pass.

Belief development/profile total61.63 seconds. Both repair arms passed the registered64-event clean acquisition check; main freeze requires the separate final development check and exact ledger-only renaming test. The complete nine-run evaluation is estimated16–20 minutes, inside the45-minute track cap. It follows return capture in the serialized queue.

Return profile:1.768 seconds,447MB peak CUDA allocation and1.60GB RSS. Source33e48f4 approved for the unchanged three-backbone seven-by-seven matrix and matched-row shared decoder; estimate2–3 minutes, hard five-minute run cap. All fitting and selection policies were declared before untouched test evaluation.
