# Stage 10 belief role separation

## Development and resource evidence

The preregistered continuation condition passed at development seed101 after300 updates per arm. On64 separate development examples, ledger-only and randomized-ID models both select64/64 clean and64/64 renamed proposals. Ledger-only probability change under renaming is exactly zero. Raw-ID correlated reference selects64/64 clean but3/64 renamed proposals. These are development observations, not the main competence result.

The randomized-ID model's maximum individual probability change under renaming is .1484 despite correct final selections. This is why exact architectural invariance and learned posterior fidelity must remain distinct. Its mean posterior L1 is .00366 clean and .00309 renamed. Ledger-only L1 is .00151 in both. No readiness/execution conclusion follows.

All arms have identical initial tensor hashes and identical semantic training-stream hashes. Their public ID streams differ only in the registered randomized arm. Each arm saw9,600 unique exact public constructions in development; these are nonce/candidate identities, not alpha-distinct algorithmic structures. The randomized arm encountered28,999 numeric handles; ID4 was absent, ID100 present. Historical unseen-ID labels are therefore not used as statements about current training coverage.

Profile plus development model runtime totals61.63seconds. Development runs use approximately15.6–15.8seconds each, with564,333,056 peak allocated CUDA bytes and approximately2,031,108KiB peak process RSS. Three512-example N16 profile cells took3.35–3.40seconds to evaluate/export per arm. Primary workspace width1024, FF width2048, four residual phases,16,867,395 allocated parameters. Ledger-only masks16,384 ID-input weights; parameter inventory matching does not imply equal useful feature capacity. No attention memory allocation is added.

Four mechanical tests pass. Exact prior is supplied during training and evaluation in every arm using finite logits, with finite backward verified. This differs from the historical Stage8 learned-prior recipe uniformly across all new arms.

## Frozen main protocol

Three arms × paired initialization seeds20/21/22,1000 updates ×32 fresh episodes each, fixed final checkpoint, no selection. Total planned optimizer exposure288,000 presentations. N8/N16 are mandatory, N32 separately additional OOD. Fifteen conditions × two untouched validation/test splits ×512 episodes per cell. Source and nine configs are frozen before main inference. Estimated complete main runtime16–20minutes, within45-minute track ceiling; no extra sweep or recipe adjustment is authorized. The supplied empty-ledger prior, protected ledger rules and architectural ID-invariance are reported separately from learned matching and probability calibration.

Main outcomes pending; all gates remain unpassed until every seed/cell and independent raw audit complete. Composition and supervision withdrawal remain blocked.
