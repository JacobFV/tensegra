# Stage 10 independent review

Status: pre-experiment audit plan. Baseline `60ad5ee`. Independent inventory verifies **2,844 tracked files byte-identical**, including prior source, reports, raw results and gates. Review performs no GPU work. Stage10 changes must be additive and versioned; independent CPU checkpoint replay is labeled separately from archived prediction reconstruction.

## Prespecified scope and resource boundary

Three narrow experiments only: (A) belief observation IDs as ledger addresses versus raw-ID neural features under randomized-ID training; (B) frozen-return cross-delay readout matrix plus one shared decoder; (C) localization of the five remaining argument-edge errors followed by bounded seed-robust fixed-node fitting with a single edge-head recipe. No new runtime composition, supervision withdrawal, pretrained integration or broader architecture. Actual experimental workspace remains1024. Mechanical mathematical fixtures may use explicitly named smaller dimensions.

Inherited ceiling is180GPU minutes; initial track allocations45/35/30 plus70reserve. Root profiles and serializes GPU work. New initialization seeds20/21/22 and10M-series data are separate from named frozen Stage9 seeds10/11/12. A fixed-set graph study contains8graphs and cannot satisfy language/generalization gates by reusing those examples.

## Claim-specific prerequisite map

| Claim being considered | Relevant evidence / prerequisite | What these gates do not establish |
|---|---|---|
| Supplied topology changes learning when aligned with the generating process | Matched structural-attention experiment and topology controls | Return retention or semantic graph induction is not a universal prerequisite for this original programmable-attention hypothesis |
| Observation IDs serve as stable addresses without spurious payload shortcuts | Explicit ledger semantics, ID–payload counterfactuals, matched observable/training comparisons | Ledger correctness is programmed; no open-world probabilistic calibration claim |
| A learned workspace permits stable access to returned facts | Frozen cross-delay decoding, shared-decoder tests, independent values/contexts and interventions | Good per-delay probes do not prove one decoder generalizes or that information is never lost |
| A calibrated semantic edge decoder acquires a fixed graph set | Complete edge/slot reconstruction across prescribed seeds under one frozen training/calibration policy | Privileged one-hot node fitting is not surface understanding, structural transfer or universal graph reasoning |
| A future runtime pipeline composes learned interfaces reliably | Direct composed tests with separately established lowering, execution and return contracts | No isolated pass in Stage10 authorizes composition or supersedes prior blocks |

Stage10 interface failures must not be generalized into universal failures of neural recurrence or used to retroactively invalidate Stage1/2 attention results. Conversely, these experiments cannot validate metric programming when no structural-attention intervention is tested.

## Track A review requirements

Separate ID semantics from evidence content. The ledger needs stable observation addresses for duplicate insertion and retraction. Payload compatibility need not consume the numeric bits of an address. Compare ledger-only IDs against raw neural ID features with randomized training IDs under matched task/public information, exposure and initialization where parameter shapes permit. Disclose any parameter and architectural differences. Preserve an unchanged historical/raw-ID reference as named diagnosis when used.

Randomization must preserve within-episode identity under duplicates and retractions. Train/validation/test ranges and ID–payload coverage must be explicit. Renaming and equal-content controls must separate new addresses, new address/payload combinations, and multiplicity of deterministic constraints. Private support, hidden winning candidates and contradiction labels may enter targets only. All official posterior metrics retain initial frames; architectural prior passes remain separate from learned calibration. Exact ledger operations cannot count as acquired neural bookkeeping.

## Track B review requirements

A full matrix `decoder trained at delay s` → `workspace evaluated at delay t` must use the same underlying test events across columns and frozen checkpoint states. Fit normalization and decoder coefficients on probe training data only; select regularization on declared validation data only. Export all matrix cells, including off-diagonal failures, paired event hashes and exact scalar predictions.

The separate shared decoder has one coefficient set/readout policy across every evaluation delay. Declare its training-delay support before outcomes. Evaluation within that support is interpolation or trained-length testing; only longer/unseen delays qualify as length extrapolation. Do not select a different decoder using the test delay. Distinguish finite-label reconstruction from unseen numerical labels. Keep all non-value fields/gates visible if claiming complete retention. No exact-copy/oracle-facet diagnostic can pass workspace-only reconstruction.

## Track C review requirements

First reconstruct the five edge errors in the frozen seed10 result: typed source/target/role labels, score margins, threshold context, gradient/loss contributions and available training trajectory. A final-checkpoint gradient is local pressure, not optimizer history. Separate raw-zero decisions from training-only calibrated decisions. Existing slot-head mismatch does not establish impossibility of masked final graph recovery.

Any new fitting recipe must freeze budget, calibration policy, selection and seeds before its final outcome. One edge-head intervention, not another four-slot-arm factorial. Privileged fixed node identities and training graph labels are explicit. Thresholds may fit training labels but never heldout labels, and their policy remains identical across graphs. Require every graph and seed for restricted complete acquisition; missing or failed seeds cannot pass. Do not escalate to public language training from a tiny fixed-set result without a separate decision.

## Independent audit deliverables

Reconstruct raw counts, calibration selections and complete matrices independently from model metric code. Check paired events, checkpoint/source/config/data hashes and coefficient identity. Preserve every failed recipe. Verify old baseline bytes again after final changes. Final report must distinguish programmed guarantees, learned interfaces, calibration, generalization and composed competence, and must attribute conclusions only to the claim each experiment actually tests.

## Prelaunch implementation review

A `InvariantBeliefModel` masks only the appended16neural ID coordinates in the ledger-only arm; inherited integer IDs still control exact first-write/removal. All three arms retain equal initial tensors and allocated capacity. Independent handle randomization preserves duplicates/retractions without consulting payload/targets. The public prior uses finite−1e9 logits, avoiding the inherited cross-entropy `0 × −inf` failure while yielding exact floating-point zero null probability. Finite-backward and masked-gradient tests are present. Initial-frame inclusion and programmed-versus-learned invariance remain explicit.

B protocol fits source-delay normalization and regularization only from training/calibration data at that source delay. The shared comparison matches2,048events and8,192rows while varying delay coverage, with no delay32selection for pooled heads. A source32diagonal fit remains diagnostic. Request retained original non-value/joint counts so scalar improvement cannot masquerade as complete retention. No architecture or backbone update is planned.

C frozen exposure protocol changes only the fixed update cap300→1200 on one additive/edge-conditional affine decoder, with three fresh seeds and one eight-graph fitting set. Fixed final1200gate, raw/calibrated parallel scoring and full FP32 score/target exports permit independent trajectory checks. No interim outcome selects a checkpoint or further budget. The1024dimension here is privileged node input, not an unmeasured recurrent workspace.

The five historical false-positive argument edges independently match Stage9's calibrated raw error set and checkpoint SHA. Their signed margins, closed-form weighted logit gradients and all saved raw binary trajectories reconstruct exactly. Earlier logits/optimizer states were not saved and cannot be inferred from those trajectories. Parameter-gradient descent direction still requires separate CPU replay before being independently endorsed.

A separate CPU replay independently reconstructs the full edge-parameter gradient from explicit sigmoid score derivatives, then checks each erroneous score by a symmetric finite difference along that gradient. All five descent directions are negative and match reported directional derivatives within7.82e-8; gradient norm and checkpoint hash agree. The replay takes0.59CPU seconds on the linked host. This confirms local optimization pressure, not an Adam trajectory or future convergence guarantee.
