# Stage 3 artifact analysis

`python -m topoformer.grounding_analysis METRICS.jsonl OUTDIR --seeds 0 1 2` reads committed metrics only. It requires a complete variant × condition × expected-seed grid, one shared configuration/source identity, finite metrics and matching training-schedule/evaluation-data hashes before paired comparisons. Common initial parameter hashes are checked separately because initialization ablations intentionally differ. Missing distractor-null accuracy when no distractors exist is N/A, not zero. Seed summaries use sample SD; no inferential significance or confidence claim.

The report separates task accuracy, complete pre/post grounding paths including destination, complete attention argmax paths, and pre-step-only grounding. Probability and entropy diagnostics remain separate. Deterministic direct/one-hot attention oracles are privileged algorithms. Known and graph-input controls use fixed cosine soft identity matching. Primary learned soft begins with an identity-coordinate prior; random initialization remains a separately labeled variant. Names are random continuous identities, not lexical language.

Optional Matplotlib figures are standalone PNG/SVG depth, size, corruption and distractor curves plus recurrent-step diagnostics. Every figure fixes other experimental factors rather than implying a joint depth × size sweep. First-example trajectory tables are selected by seed/condition, never by success. Resource tables distinguish instrumentation-inclusive run time and plain inference time; process peak RSS is a high-water mark, not model memory allocation. Raw model records preserve strengths and temperatures.

Validation: nine focused tests passed remotely on GB10, covering sample SD, incomplete/duplicate seed grids, nonfinite metrics, mismatched evaluation pairing, unavailable distractor metrics, mixed provenance, and explicit metric semantics. No training is performed by the analyzer. Source schema locked against runner commit `2220217`; actual artifacts will be checked once the run is complete.

Actual six-run, seed-0 IID pilot schema integration passed: summary/report generation completed on remote analysis scratch without altering training source. No figures are emitted for a single-condition grid. Graph-input retrieves relation messages through current-node keys, so next-token attention diagnostics are not comparable as traversal-success measures across this baseline and direct structural attention. Task accuracy and state-grounding diagnostics remain distinct.

The keyed supplemental family (`soft_keyed`, `none_keyed`, `graph_input_keyed`) receives its own within-family paired comparisons and figures. The shared graph-free content identity prior changes the architecture; these rows must not be interpreted as matched comparisons to the primary family. A regression test prohibits cross-family paired effects even if both families are present in a single configuration. Ten analyzer tests pass remotely after this extension.

Rendering revision after completed main/keyed runs: include `soft_strength4` in primary depth/size/corruption/recurrent-step curves, include `graph_input_keyed` first-example trajectories, and record the analyzer file SHA-256 separately from immutable training source provenance. Numeric aggregation is unchanged; regenerate analysis/figures without rerunning training. Twelve analyzer tests pass remotely, including exact input/code artifact hashes and keyed graph-input trajectory rendering.

Added standalone `training-curves-primary.{png,svg}` and `training-curves-keyed.{png,svg}` from already preserved `training.curve` records. They show fixed-validation task accuracy and exact grounding-path completion versus optimizer step with seed mean ± sample SD, preserving initial routing and later deterioration without smoothing or checkpoint selection. Checkpoint aggregation rejects duplicates and incomplete seed coverage. All 13 main and 3 keyed variant checkpoint sequences validated against real raw artifacts; pilot plot generation succeeded. Fourteen analyzer tests pass. This is rendering of existing observations and requires no training rerun.
