# Stage 11: trained horizons and public-text acquisition

Baseline `e10b80a`; branch `feat/stage11-acquisition-horizons`. Preserve all historical files. Standard model workspace width is 1024. This stage follows the user's three priorities without adding a memory system, numerical encoding, runtime primitive, pretrained model or autonomous composition. Supervision withdrawal remains blocked.

## Independent tracks

**Beliefs:** freeze the Stage 10 ID-separated interface as the preferred restricted reference. Opaque observation handles address exact ledger entries; semantic matching excludes their numeric spelling. This guarantee does not apply to meaningful entity identities, roles or timestamps, and does not imply posterior correctness. Keep randomized training and its rare intermediate counterfactual tails as evidence. No further belief training is needed; readiness/execution require their own future predeclared conditional-error study.

**Returns:** test a single training-support intervention with architecture and losses unchanged. Prefer paired continuation from the same Stage 9 checkpoint: short-horizon continuation versus continuation covering 0/1/2/4/8/16, equal optimizer updates, fresh examples, initialization and objective. Keep an unmodified frozen checkpoint on the same new test data. Training through 16 makes 16 a trained horizon; 32 remains extrapolation. Do not drop zero-step ingestion or non-value fields. The frozen Stage 10 ridge study remains a diagnostic reference, not an objective-matched causal arm. Track recurrent compute separately because equal examples/updates do not equal FLOPs. No added consistency loss or multiple architectural interventions.

**Semantics:** the fixed-node fitting prerequisite is cleared, so independently resume small public-text acquisition. Audit observability, vocabulary/copy targets, canonical alignment, capacity and semantic diversity first. Use the existing small width 1024 actor and corrected slot objective; public input never includes gold graph/node features. First require complete acquisition of a fixed public-text graph set under a bounded budget. Only then consider fresh known-renderer constructions, and only after that held-out surfaces/renamed identities. These phases have separate gates. Retain the completed supplied-node result as a localization reference, not another memorization sweep. Do not wait for the unrelated return gate; do not launch a large corpus sweep automatically.

## Controls, selection and interpretation

Each track registers exact hypotheses, inputs/privileged targets, supplied versus learned behavior, comparison, selection rules, split identities, source/configuration freeze, exposure and stopping conditions before main work. Use at least three paired main replicas and 512 examples per meaningful generalization cell. Tiny fixed-set acquisition may use 8–16 graphs, explicitly not generalization. Frozen checkpoint seeds keep their historical names; any new initialization seeds use 30/31/32. The subprotocols declare independent development sources: semantic profile seed 301 and archived return seed 0. The initially reserved seeds 101/102 were unused. Fresh data namespaces begin at 11,000,000. Report actual distinct semantic constructions, not just presentations or surface count.

Keep initial/final behavior, per-field accuracy, full joint accuracy and paired interventions visible. Exact half-unit classification is not MAE. Graph components do not substitute for complete graph accuracy. Any threshold fitting uses a declared training/calibration partition only, with fixed policies for heldout data. Fit on fixed data is not fresh semantic acquisition; known renderer is not unseen renderer. Stop a dependent track at its registered failed gate rather than extending optimization until it passes.

## Budget and machine

Retain the conservative **180 GPU-minute ceiling**: returns 75, semantics 75, shared reserve30. Expected use is substantially below that ceiling. Profile representative backward/evaluation steps before fixing main budgets; root serializes GPU jobs. No worker starts GPU work before queue release. Charge job walltime conservatively and record training/evaluation/export separately where practical. Parameter counts, memory-token allocation, CUDA allocated bytes, process RSS and device capacity are distinct.

Preflight verified `gb10-direct` = `promaxgb10-4dfb`, 119 GiB RAM/115 GiB available, 455 GB free storage, no active GPU process. Reuse `~/topoformer-stage8-cuda` and existing CPU environment without dependency upgrades. No local heavy training.

## Claims and advancement

A known invariance can be supplied without supplying an answer. Broader return training can be tested without assuming information destruction. Public-text graph induction can proceed because its own decoder fitting prerequisite passed; exact return retention is not its prerequisite. Conversely, none of these tracks directly compares ordinary versus graph-biased attention. Maintain a separate prerequisite map for future QK-geometry studies and do not turn these runtime interfaces into universal gates for that original hypothesis.

No isolated success authorizes autonomous composition, readiness-controlled execution, supervision withdrawal or pretrained integration. Finish with a recommendation supported by each independent track. Preserve failed recipes, raw compressed predictions/targets, event identities, source/config/data/checkpoint hashes, curves, independent audits and concise reports under the existing research layout. Merge/push after final verification under existing authorization.
