# Stage 7 independent interface review

Status: source contracts reviewed; acquisition artifacts pending independent audit. No reviewer training or local Torch workload. Passing implementation tests does not certify acquisition. Existing Stage1–6 source is the semantics reference.

## Current scope and gate interpretation

A receives shuffled typed instruction records and a destination query. Learned retrieval selects a record and learned pointer binding selects its ordered operands. This supports local instruction selection under larger record sets and changed dependency wiring; it does not demonstrate language parsing, graph traversal, arithmetic execution, or full compositional evaluation. Names and values are unused randomized metadata. A2 requires strict A validation competence across every configured seed and both IID/OOD cells, including noncommutative order. A pilot success alone does not authorize A2 or composition.

B calibrates generator-supplied correlated support scores. Correctness is sampled after support construction, with overlapping classes and high-support stable wrong cases. The generator has a sharp latent probability transition at .9985. This controlled supplied-score experiment cannot certify calibrated confidence from learned proposals. Exact schema validity is supplied, and the factor product is a score. Thresholds are chosen on validation, frozen for test, with matched local/global/product threshold selection. Report precision, executable recall, every denominator, empirical factor correlations, reliability, and mixed-group behavior. Autonomous candidate coupling requires both actual proposal competence and separately validated proposal-derived confidence; the present B diagnostic alone does not supply the latter.

C1 supervises workspace reconstruction of perfect typed returns. Exact external-register integrity is architectural and separate from learned workspace value/type/primitive/identity recovery. Release stops record availability without erasing the workspace; stale latent information remains possible. The task and validity heads are untrained diagnostics in C1. Late-use training and multi-operation composition remain blocked until C competence. Random unrelated distractors test interference, not preservation of a separately queried unrelated fact.

D receives supplied certified typed evidence and persistent current snapshots, with an explicit public deadline. Learned stop uses its own logits, not the core timing bias. Role, certification, and queried-name marginal counts remain constant across arrival; their conjunction identifies usable evidence. No-solution terminates at the supplied deadline and must predict reject. Test is evaluated only after a frozen final main budget. Passing this independent diagnostic does not authorize runtime composition.

E deduplicates complete alpha-normalized ordered graph strings before splitting. Repeated identity, values, edge roles and slots are preserved. Its decoder metric compares full canonical structure under the pinned compiler traversal, not arbitrary isomorphism or denotational semantics. Values use a train-only vocabulary and unknown values fail exact semantics. Visible-token pointers canonicalize repeated spellings. The fixed evaluation prefix and subsequent nested training prefix keep evaluation constructions stable and disjoint across cardinality budgets, provided data seed, difficulty and evaluation count remain fixed. Actual visited graphs and optimizer/token exposures must accompany nominal cardinality. Gate F stays false absent paired learning trends above baselines and renderer/lexicon transfer; presence alone cannot pass.

## Issues found and dispositions

| Issue | Disposition |
|---|---|
| A initially supplied already-selected gold role queries; claimed OOD composition axes were unused metadata. | Replaced with complete shuffled instruction records and learned retrieval; narrower scope disclosed. |
| A empty gate passed; expected seeds/splits and noncommutative support were not checked. | Empty/missing cells fail; seed × IID/OOD matrix checked. |
| B initial correct/incorrect support intervals were disjoint and factors independent. | Shared latent, correlated jitter, overlapping correctness and high-support stable wrong cases added; synthetic calibration law disclosed. |
| C release/overwrite globally reset the workspace. | Reset removed; release invalidates stores only, with residual information retained. |
| C wrong provenance used an off-support negated vector. | Replaced by another valid provenance identity. |
| C empty retention/use gates passed. | Empty or missing support fails. |
| D mask revealed arrival, misleading value was exactly `1-b`, then replacement changed role counts. | All-visible shuffled rows, independent misleading values, balanced key/certification swap; prior shortcuts removed. |
| A delta used ternary sign for compare; C compare witness evaluated to the opposite boolean. | Both now use frozen runtime boolean `left < right`; C adds direct ProtectedSession witness verification. |
| A DAG allowed boolean compare outputs as numeric operands. | Numeric register pool excludes compare outputs. |
| E changing train size moved heldout examples into training. | Fixed heldout prefix reserved before nested training prefixes. |
| E initially lacked raw predictions and could accept unknown values or alternative repeated-token positions incorrectly. | Gzip raw predictions/targets added; unknown semantics fail closed and token pointers canonicalized. |

## Independent verification completed

Standard-library AST extraction ran actual gate/helper functions without importing Torch. Initial empty A/C/use failures were reproduced, then updated empty A/C/use/D and missing C/use inputs correctly failed. The actual compare helper matched boolean less-than for all 49 pairs in [-3,3]². A threshold check with equally scored correct/wrong candidates rejected all candidates rather than fabricating >99% precision. Complete graph keys preserved alpha-renaming equality and distinguished repeated identity and swapped ordered slots.

Source inspection confirms forward paths do not read private proposal labels or halting arrival targets. C decodes semantic outputs from workspace only; exact scalar return fields do not bypass it. D's hidden share is overwritten in every insufficient prefix and throughout no-solution episodes. Owner additionally reports a causal paired missing-share-seed regression over every public tensor, with changed answer targets and unchanged insufficient prefixes. That remote test report is separate from reviewer-executed checks.

C owner reports a regression executing 256 generated return witnesses through the frozen ProtectedSession, checking result values/types across all five primitives (commit `07885d3`). A/B and D acquisition evaluate validation only; frozen main budgets reserve test. C final main configs must supply disjoint untouched test seeds. Raw counts will be independently recomputed when artifacts arrive.

## Remaining audit and claim limits

Inspect acquisition curves, source/config/data/initial/checkpoint hashes, raw target/prediction counts, intervention pairings, and all expected seed/condition denominators. Confirm main budgets freeze before test outcomes are inspected. A/B supplied typed semantics, C perfect events/protected storage, D certified evidence/deadline, and E canonical decoder order are strong representational priors. None should be described as independently discovered semantics.

Numerical gates are empirical point thresholds; passing a small finite sample does not establish a population precision lower bound. Preserve support counts, preferably at least 512 validation examples per required condition as coordinator guidance. No gate failure may be compensated by another interface, and dependent phases remain blocked until their own prerequisites are demonstrated.

## Acquisition artifact audit: A/B seed 0

For `.development/results/stage7-ab/ab-acquisition`, independently recomputed full ordered proposal accuracy from raw primitive/pointer predictions for every saved split/control cell: all match. Final IID and OOD validation each have 512/512 full ordered proposals correct; this is a one-seed acquisition diagnostic, not the complete prespecified paired main gate. Missing/permuted/reversed controls are retained in raw outputs.

Independently reconstructed B validation threshold selection, local/global/product selected counts, true positives, precision, executable recall, Brier score and all reliability bins from raw candidate records: all match. Calibrated local selection yields 17/17 correct but only 17/258 executable candidates (6.59% recall), correctly failing the >=50% usable-coverage gate. Global selection yields no accepted candidates. Product selection yields 4/258 executable recall. Test evaluation outputs are absent, as required for acquisition. Composition/progressive flags remain false.

Runner and readiness source hashes match current source; proposal source has subsequent A2 additions. Owner identifies acquisition source as commit `b290dce` (same tracked source at `e68d50d`) and is archiving exact snapshots to match manifest hashes. Source changes after a run must never be silently presented as the run's implementation.

For E's initial 200-update diagnostic, fixed-eight-example semantic exactness and ordered-edge F1 remain zero; training typed-edge F1 is approximately .04274 despite node F1 .95534. Gate F correctly stays false. Only aggregate curves are currently available locally for this artifact; full raw recomputation awaits compressed records. A revised active-pair loss is explicitly versioned separately and does not retroactively alter the initial failure.

## Frozen main A/B audit

The independent standard-library audit script is `stage7-artifact-audit.py`; detailed recomputed results are `stage7-ab-independent-audit.json`. Main artifact config and all three exact archived source hashes match their manifest. Smoke and acquisition exact source snapshots also now match all manifest hashes, resolving the prior provenance request.

Across seeds 0/1/2, all four IID/OOD validation/test cells contain 1024 examples and achieve 1024/1024 full proposals; primitive, destination, each required operand and noncommutative order are all 100%. Noncommutative support ranges from 544 to 628 per cell. Recomputed 576 component/readiness aggregate metrics plus reliability bins match saved results. The strict paired validation Gate A is independently verified; the coordinator may authorize actual progressive typed-record evidence studies. A separate GRU over supplied likelihoods/two fixed hypotheses is only a posterior-filtering diagnostic and cannot establish that progressive proposal capability.

Supplied-support B fails every main seed. Test precision is 143/147 (97.28%), 216/222 (97.30%), and 154/155 (99.35%); executable recall is 143/1001 (14.29%), 216/984 (21.95%), and 154/999 (15.42%). These fail the >=50% recall criterion, and the first two also fail >99% precision. Validation-selected thresholds are held constant on test. This does not certify confidence from actual learned proposal outputs; that extension awaits separate review. Runtime composition remains blocked.

## Additional independent raw checks

Regenerated all 15 main A train/validation/test datasets with the exact archived pure-Python generator: every data hash and every saved primitive/pointer target matched. Pooled heldout full proposal correctness across six seed × split cells is 100% complete, 0.553% missing, 0.618% permuted, and 0% reversed. Thus the complete-evidence result is causally sensitive to its operation/ordered argument evidence.

The C acquisition archive (`stage7-c-acquisition-artifacts.tar.gz`) was read without executing checkpoint code. All four exact source hashes, config hash and three checkpoint file hashes match; all three arms share the same initialization hash. Recomputed 1,116 saved evaluation rows, each with the six retained fields, joint correctness, task correctness and task-given-retained counts: all match. Every heldout retention/use gate is false. Logged fixed-set acquisition success is distinct from these heldout failures; raw fixed-set training predictions are not present, so that specific logged training success was not independently reconstructed.

Recomputed node, typed-edge and ordered-edge counts/precision/recall/F1, node-type accuracy, exact copy, entity-equivalence and canonical semantic equality for 64 compact raw failure rows (32 each from E's 200- and 2000-update diagnostics). Every metric matches. This validates sampled failure records, not every aggregate curve. Omitted inactive-node edges do not affect the declared scoring rule.

Actual proposal-confidence source review found schema validity initially reading original operation labels hidden by missing/permuted evidence. This was corrected before running: an immutable register-type table is explicitly public in every frame, and schema uses only that table. Regression compares private operation mutations with unchanged public evidence. The six confidence factors otherwise come from frozen learned proposal logits and past/current public predictions. Scope is calibration on a declared deletion/permutation mixture, with independent examples grouped four at a time; it is not calibration of competing hypotheses within one world or arbitrary naturally occurring proposal errors. Runtime composition remains separately blocked pending actual calibration results and other interfaces.
