# Stage7 A/B acquisition scope

A consumes supplied typed instruction records (destination key, operation cue, ordered operand keys), shuffled with distractor instructions, and a public destination query. Two learned attention stages retrieve the matching record and bind ordered register pointers. Output labels never enter model forward. The task measures typed instruction selection and binding, not language understanding, graph traversal, arithmetic execution or runtime composition. IID has leaf/chain dependencies; moderate OOD adds larger register/instruction sets and two-derived-operand wiring. Names/value ranges are randomized metadata-only invariance axes and cannot support value/name comprehension claims. Compare outputs are boolean sinks; arithmetic operands use numeric registers.

B consumes generator-supplied confidence factors, exact schema validity and a stability feature that is independent of future evidence. Shared latent evidence makes the factors correlated. Correctness is sampled after factors, with overlapping support and stable high-support wrong cases. The synthetic calibration law has a sharp probability transition at latent .9985: this tests score calibration, not evidence inference or candidate construction. The product is only a score. Calibration parameters train on calibration data; thresholds freeze on validation. Local/global/product thresholds use the same precision requirement. Mixed readiness groups are separately scored. Schema validity is architectural and separately logged.

Tiny delta diagnostic predicts an inserted value from supplied operation and operands. Removal identities and output type are supplied, not predicted. Compare preserves the existing boolean less-than semantics. There is no new rewrite runtime.

`python -m topoformer.interface_study --config configs/stage7-ab-smoke.json --output DIR` writes source/config/data/model hashes, raw predictions, per-component losses, step-zero curves and gate support counts. Test outcomes are evaluated only with `main_budget_frozen=true`; acquisition defaults to validation only. A gate requires every configured seed and both IID/OOD validation cells, strict component >99%, full >98%, noncommutative order >99%. Empty/missing cells fail. Progressive and runtime composition remain blocked. Smoke/acquisition configs are provisional, not frozen main budgets.

Focused remote verification: 14 tests passed in isolated `/tmp/stage7-ab.xUdUYD`; no local Torch. Initial full suite collection failed because scripts were absent from the temporary transfer; complete transfer and rerun required. Review corrected compare sign-vs-boolean mismatch, typed DAG operands, missing gate cells, and unsupported metadata-OOD claims before experiments.

Acquisition (committed source b290dce) completed in 10.25s: A seed0 100% all components/full/noncommutative on train256, IID512, OOD512. B validation failed coverage:17/17 selected correct,17/258 executable recalled (6.59%). Delta heldout inserted-value accuracy48.78%. No A2 authorized from single-seed acquisition. Root froze main seeds0/1/2 with unchanged1000A/1000B/500delta steps, train256, evaluation1024 and readiness1024 groups per partition; main evaluates test once at final checkpoints. Calibration failure retained; no post-outcome model adjustment before this frozen main.

A full temporary-snapshot regression run reported492passed/4failed: binding and grounding source identity expected a .git directory absent in transfer; halting missing_share_seed and retention comparison semantics reflected concurrent source/test revisions. These are unresolved in this snapshot and must be rerun after complete synchronized transfer.

A2 preparation adds a two-hypothesis ordered-swap posterior scaffold. Public likelihood frames update a shared GRU state; private normalized posterior targets never enter forward. It cannot produce unsupported crossed operand pairs. This is explicitly a narrower GRU calibration recurrence, **not** the existing four-phase thinking cell. Its wrapper refuses missing/failed expected seed×IID/OOD gate cells. No A2 training has been run.16 focused remote tests pass, including causal-prefix equality and gate rejection. Main source remains immutable e68d50d regardless of these preparation edits.

Main completed43.66s and independent audit verified all3seeds×IID/OOD validation/test1024each at100% full/component/noncommutative accuracy. This supports the disclosed typed local instruction retrieval/binding prerequisite. Supplied-support B still fails in all seeds; it does not authorize real execution. Main artifacts include exact e68d50d source snapshots, raw predictions and derived reversed-valid-target scores.

Actual proposal-confidence extension freezes A checkpoints and calibrates primitive/destination/ordered-pointer support, exact schema, and current/past stability. The past frame has missing operation/argument evidence; current is complete/missing/permuted. Exact register types are a public immutable table provided in every frame, an explicit architectural prior. Regression proves changing hidden original operation metadata under identical missing public frames does not change factors. Calibration/validation/test use fresh seeds. Groups are independent examples grouped four, not competing interpretations of one world. Any pass is limited to this prespecified corruption mixture, because complete A already has near-ceiling correctness. Percondition metrics are mandatory.20 focused remote tests pass before its frozen run.

Actual learned proposal-confidence results (7e0f322, frozen1024examples×6conditions per partition,1000 calibration updates): test precision/recall seed0=99.45%/52.31%, seed1=99.02%/81.78%, seed2=99.12%/38.40%. Paired GateB FAILS because seed2 executable recall<50%; no runtime coupling. Mixed-group local advantages52.25/81.61/38.18pp over matched global scalar. Candidate correctness includes destination and exact schema. Complete-evidence typed proposal success does not transfer automatically to readiness. Calibration law/synthetic supplied-support diagnostic remains a separately failed control. Tiny inserted-value delta accuracy48.78/26.83/34.15% across seeds; this is not arithmetic competence.

Standalone figures: `results/stage7-ab/ab-main/proposal-readiness.{png,svg}` and `results/stage7-ab/actual-confidence/actual-confidence.{png,svg}`. Derived reports retain reliability bins, frozen thresholds, support denominators, reversed valid-target scores and confident failures. Large raw JSON is gzip-compressed losslessly; source snapshots retain exact frozen modules and manifests retain uncompressed/raw hashes. Main delta evaluates41 heldout constructions per seed, so its small diagnostic denominators are explicit.

Complete synchronized regression snapshot with local git identity:509 tests passed plus6 subtests (7.18s remote,2threads). Earlier4 snapshot failures resolved on synchronized sources; no Stage1–6 source changes were needed by this track.

Actual A2 frozen follow-up uses partial public instruction records, not the optional likelihood-filter scaffold. Five frames reveal operation atstep1/2 and ordered roles atstep3/4; distractor record count4training/6OOD. Eleven public candidate encodings supply5operations×2operand assignments plus reject; the unordered operand pair and finite candidate set are strong supplied priors. Unary neg alternatives are neg(a)/neg(b), with second operand masked in both hypotheses and records. The model reuses four existing workspace blocks to update persistent current candidate states. No private posterior is supplied as input. After independently audited full3seedGateA, root authorized1000updates×3seeds,train256/eval512,batch16,width24.23focused tests pass, including hidden future prefix invariance and unary schema. This does not authorize runtime composition.
