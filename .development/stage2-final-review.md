# Stage 2 independent final review

## Review status

**Whole-study verdict: APPROVED.** Training/data/model integration, completed raw artifacts, revised analysis, and final report pass the scientific review. All identified reporting and presentation findings are resolved. No blocking or moderate finding remains.

Scope read: `stage2-design.md`, `plan-02.md`, `stage2-methods.md`, prior core/runner reviews, `training-review.diff`, current data/model/runner source and its shared pilot dependencies, and the integration tests. The audited training revision is `ee49edc`; later analysis-only changes do not alter the reviewed training implementation. Per the review assignment, no established tests were rerun and no training or product-code edits were made.

## Findings

1. **Low, resolved — clarify robot replication in the methods prose.** The original `stage2-methods.md`, “Training and pairing,” said there were “three independently seeded processes/training runs per reported condition.” In the uniform robot conditions, the graph and coefficients are identical across seeds: `src/topoformer/data.py:68` builds the morphology deterministically without using the generator, and `src/topoformer/study_data.py:28` reuses it. Seeds vary trajectories and model initialization, not the uniform robot process. The signed robot family does vary coefficients because `study_data.py:40–44` samples them. The methods now distinguishes these replication scopes. No rerun was needed.

## Scientific integration checks

- **Within-condition pairing is exact.** `study.py:189–201,314–321,411–412` constructs a fresh common model using the same seed and copies every matching shared tensor, including the optional identity table. Every variant reconstructs the same graph/window schedule from the same local generator (`204–209,319`). Each step uses the graph index to select both its examples and its supplied topology (`338–340`). Extra graph-input and coefficient parameters are explicit. Node-identity controls have identical parameter counts and full initial state across none/soft4/hard; transfer never creates the table.
- **Count nesting is real and does not leak larger-budget statistics.** The common fixed-process pool is generated once at the maximum required count (`135–148`) and training uses prefixes (`167–177,408`). Normalization is fit to the smallest training prefix (`161–164`), with distinct trajectory RNG seeds for validation and test. Across-count schedules have different sampled index ranges; the design and methods accurately limit exact schedule matching to within a count/case. Reused seeds alone would not guarantee independently regenerated trajectory prefixes, but the implementation uses a shared pool and slices it, which does.
- **Transfer has one shared model and disjoint graph identities.** `study.py:150–160` creates graph-indexed datasets and checks graph-hash intersections across splits; `314–348` trains one model over uniformly sampled graph indices. Fixed and mixed training cases evaluate the same test graph/trajectory seeds. Models contain no node-count-specific parameter in these cases. Sparse graph probability is `min(degree/(n-1),1)` (`study_data.py:23–25`), so expected nonself indegree remains fixed as size grows. The full preset uses sparse transfer only (`study.py:283`); fixed robot experiments must not be advertised as morphology transfer.
- **Supplied topology cannot change the data-generating process.** `_graph` returns corrupted supplied tensors (`study.py:181–186`); labels and trajectories remain attached to the original `Dynamics`. Fixed-process validation/test preserve the original graph seed, so the seeded corruption is identical across those splits and variants. The corruption implementation clones input, preserves self support, counts original directed nonself edges, draws additions from original nonedges, and records realized precision/recall (`study_data.py:50–106`). Clean-trained runtime corruption is a separate evaluation label and occurs after training (`study.py:421–430`).
- **Generating dynamics and graph directions agree throughout.** `data.py:90–93` computes target row `i` from source column `j` via `state @ weights.T`; attention uses row query/column key and graph-input uses `adjacency @ histories`. Signed absolute row mass is bounded below 0.8, giving the stated transition Lipschitz upper bound 0.9. This is a sufficient contraction condition, not a claim that graphs are identifiable from the sampled histories.
- **Information asymmetry is explicit.** Typed models receive actual process coefficients and form positive/negative magnitude channels (`study_model.py:76–87`), not just adjacency. This information is labeled in rows (`study.py:436`). The full preset uses typed only on clean signed fixed processes. Graph-input is a normalized neighbor-history aggregation before unbiased attention (`study_model.py:72–75`); it is correctly described as input message passing. Added-edge behavior for typed models remains outside the study's declared comparison.
- **Deterministic rollout is indexed correctly.** The observed prefix is `series[:, :history]`; the stochastic target begins at `series[:, history]` (`study.py:251–255`). `deterministic_future` starts from the last observed state and applies the noise-free transition once for the first prediction. Model recursion shifts normalized history one step, appends its own prediction, and is unnormalized only for metric comparison (`232–241,254–265`). Neither target future nor deterministic future is fed into the model. Oracle deterministic error is zero by construction and its stochastic error is correctly only a diagnostic.
- **One-step baselines and thresholds are training/validation grounded.** Oracle predictions unnormalize the last observed state before the transition. The normalized zero predictor is implemented algebraically as target plus `mean/std` (`study.py:219–226`). Thresholds use validation oracle and zero errors, invalidate nonpositive gaps, and preserve nonattainment as null (`351–361`). Test evaluation occurs only after all optimizer updates and validation checkpoints.
- **Operational provenance is adequate for this run.** Initial/shared/final/checkpoint model hashes, schedule hash, split seeds, graph/weight hashes, normalization, counts, timing, and process RSS are retained. The row is complete only if both the optimizer budget and all scheduled test evaluations finish (`431–434`). Dense attention and the cooperative deadline are accurately disclosed. Hashes are audit fingerprints, not saved reloadable model checkpoints.

## Independent raw-result pass

The completed raw summary and JSONL are available under `results/stage2/`. Provenance identifies the clean training checkout `ee49edce9c9f26a3328b71e777cf30c47e1c76d5`, Python 3.12.3, Torch 2.14.0+cpu, and CPU execution. The recorded totals are 570 completed model runs, 1,094.456 seconds and 462.879 MiB process peak RSS. The parent separately checked exhaustive row/file/finite/pairing/threshold integrity; this pass used direct standard-library calculations from raw rows, independently of the analysis module, for representative scientific conclusions.

No raw scientific-validity or provenance defect was found. Required interpretation checks for the narrative follow:

- **Efficiency threshold attainment is transient at the smallest budget.** Both efficiency suites, both domains, and all seeds give soft4/hard `n_epsilon=8` at gap10, but every one of those eight-trajectory runs is above gap10 again at step 600. This is an observed first crossing, not retained accuracy after full training. Identity-free sparse none first crosses at 16 trajectories for all seeds; identity-enabled sparse none first crosses at 32/16/32. Robot none is censored for seeds 1/2 in both suites. At gap25 and gap50, every tested mode already crosses at eight trajectories and step 25, so these thresholds show no measured savings at the study's resolution.
- **Very small budgets can reverse final-error rankings.** At eight trajectories, identity-free sparse final test normalized MSE averages 0.7181 for none, 0.7591 for soft4, and 0.7854 for hard. At 128 trajectories it is 0.626624/0.594014/0.593990. A claim that topology improves all sample budgets would be false.
- **Runtime corruption uses genuinely matched supplied graphs.** Direct checks found identical supplied-graph hashes between clean-trained runtime and retrained evaluations for soft1/soft4/hard at 50% drop, 50% add, and mixed25 in both domains/all seeds. Under runtime drop50, soft4/hard test normalized MSE is 0.628344/0.636314 (sparse) and 0.537259/0.545347 (robot), whereas retraining yields 0.616702/0.621574 and 0.532578/0.534470. Rounding is visible in sparse drop50 realized recalls 0.5000/0.5135/0.4872 and robot mixed25 precision/recall 16/22, rather than exactly 0.75.
- **Transfer supports graph-conditioned generalization without uniqueness of attention bias.** For fixed-12 training, test normalized MSE at 128 nodes is 0.583417 none, 0.546471 soft4, 0.537152 hard, 0.583325 permuted4, and 0.538216 graph_input. Each number first averages eight test graphs within each seed, then averages three seed means. Hard and graph-input outperform finite bias here. This is compatible with dilution of a fixed finite bias among more off-graph keys but does not isolate that mechanism causally. Mixed-size training does not improve these endpoint mean errors.
- **Typed signs/magnitudes add useful information, with a clear qualification.** Signed sparse means are 0.726977 none, 0.714902 soft4, and 0.702647 typed; signed robot means are 0.633773/0.610647/0.573811. These compare methods with different information inputs; they cannot show that adjacency alone recovers arbitrary signed coefficients.
- **Learned-alpha rollout deterioration must be reported.** Learned attention improves one-step normalized MSE from 0.626624 to 0.610690 (sparse) and 0.560035 to 0.528116 (robot), while deterministic rollout worsens from 0.047373 to 0.174351 and 0.099847 to 0.319522. Stochastic rollout also worsens, from 0.958348 to 1.075977 and 0.942964 to 1.115723. There are 3/24 negative terminal coefficients in sparse and 6/24 in robot. Layer means rise from 0.387674 to 0.462913 in sparse and fall from 0.395469 to 0.260217 in robot; no common monotone depth trend is supported.
- **Correct topology does not win every rollout comparison.** At 128 trajectories, permuted4 has lower stochastic rollout error than soft4/hard in both domains, despite worse one-step error. One-step prediction, deterministic recursive error, and stochastic recursive error require distinct conclusions.
- **Repeated controls do not add independent replication.** Uniform efficiency at 128 trajectories, clean corruption, and learned-suite fixed variants reuse the same datasets, initialization and schedules, producing identical results. The 570 runs cover study conditions but are not 570 statistically independent experimental replicates. The effective descriptive replication remains three seeds per condition.

## Final analysis and narrative pass

Reviewed analyzer `7e202ca`, the scoped analysis re-review, refreshed aggregate artifact (all test/runtime rows now correctly labeled step 600), and narrative `ba11375`. No analysis-math, selection, or pairing defect was found on the completed full-study artifacts. The revised implementation retains all 18 evaluation metric fields explicitly, stratifies them, checks shared initialization/schedule provenance for paired effects, matches graph/trajectory references, averages graphs within seed, uses only step-600 validation for soft-strength selection, and preserves censoring. All top-level artifact exclusions are zero.

Independent report arithmetic checks directly against raw rows passed for **226 mean/SD table cells**, all transfer percentage-effect ranges, and all supplemental terminal gap10 sample counts. These checks did not call the analyzer and did not rerun training or established tests. The raw coefficient matrices and representative rollout/corruption observations also agree.

The following bounded presentation corrections were requested and subsequently resolved:

1. **Moderate — scope the headline to conditions supported by the data.** The lead says topology “consistently improves finite-budget one-step prediction,” but sparse eight-trajectory final means are worse for soft4/hard than none. Explicitly scope the headline to the 128-trajectory and transfer comparisons and report the small-budget reversal.
2. **Moderate — do not call a validation crossing an evaluated early-stopping result.** The runner tests only step-600 models. The transient `n_epsilon=8` result is a validation first crossing; neither a selected early checkpoint's test performance nor a complete early-stopping policy was measured.
3. **Low — qualify cross-process and mechanism language.** “Signed coefficients make the task harder” compares separately normalized processes with different oracle noise floors; use the directly measured higher normalized one-step errors. Preserve the finite-bias dilution explanation as a hypothesis, including the milestone conclusion.
4. **Low — state selection and final provenance in the narrative.** Explain that fixed-strength sensitivity rows are retained, primary strength uses across-seed step-600 validation means, and runtime corruption inherits clean-training selection. Update the analyzer revision and full-test count to the final verified values.
5. **Low — distinguish runtime severity in corruption plot labels.** Nontransfer x labels in `study_analysis.py:778–779` contain mode/corruption/node count but omit evaluation fraction. Thus the clean-trained panel repeats indistinguishable drop/add labels for 10/25/50% runtime corruption. Include evaluation fraction (and preferably split) in those labels. This does not change any metric.

### Resolution and final evidence

Narrative revisions `153bd24` and `5179786` scope the headline, explicitly report the eight-trajectory ranking reversal, call the threshold an observed validation first crossing rather than a tested early-stopping policy, qualify the signed-process comparison and dilution hypothesis, and state the validation-only selection protocol. The selected sparse corruption exceptions match the analysis artifact. Clean-condition strength selection is explicitly scoped to the corruption suite.

Analyzer `6d404bc` adds split and evaluation fraction to corruption labels. The refreshed full-study SVG now contains distinct runtime-corruption and 10%/25%/50% labels. The change is presentation-only and leaves metric aggregation and pairing untouched. Exact-source full-test evidence is **143 passed in 1.98 seconds**, recorded in the final provenance artifact; established tests were not redundantly rerun by this reviewer.

Final artifact checks directly confirmed:

- 570 complete runs, every top-level analysis exclusion count zero, and every test/runtime aggregate labeled step 600.
- Analysis provenance bound to `6d404bcc2943a159d898f5a169fb9fd4be335dda`; raw training provenance bound to `ee49edce9c9f26a3328b71e777cf30c47e1c76d5`.
- All **52 derived artifact files** match the byte counts and SHA-256 values in `analysis/provenance.json`; both raw files match `audit.json`.
- All **18 narrative artifact links** resolve locally.
- The independently checked 226 report numeric cells, transfer percentage ranges, terminal threshold counts, and representative raw scientific results remain unchanged by the presentation fixes.

**Final recommendation: the Stage 2 implementation, completed experiment, derived artifacts, and detailed report are ready for integration.** This approves the bounded descriptive claims in the report, including its negative results and deferred large-model milestone; it does not expand them into asymptotic sample complexity, statistical significance, physical robotics, or general pretrained-model claims.
